# 复审 · 第二轮修复清单

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-08-31 |
| **前置** | 第一轮 11 项（H1~H4 / M1~M4 / L1~L3）已全部修复并验证通过 |
| **本轮范围** | **2 项必修 + 1 项可选**，均为第一轮修复后的残留缺口 |

> **先说结论**：第一轮修复质量良好，11 项全部落实，附录 A 的 15 项正确实现无一被破坏。
> 本轮只处理两个残留缺口，工作量都很小。
>
> **性能问题（NFR-PERF-02）本轮不修**，已记录为已知问题，见文末。

---

## 本轮总览

| 编号 | 严重度 | 问题 | 主要文件 |
|---|---|---|---|
| R1 | 🟡 必修 | `force` 标记未持久化，崩溃恢复后全量重建退化为增量 | `services/index_service.py`、Alembic 迁移 |
| R2 | 🟢 必修 | BM25 缓存的事后过滤导致限定单篇时检索退化为纯向量 | `rag/retriever.py` |
| R3 | ⚪ 可选 | 加分阶段耗时日志，为将来排查性能留数据 | `api/ask.py`、`rag/agent.py` |

> 编号 R = Round 2，避免与第一轮的 H/M/L 混淆。

---

## 🟡 R1 · `force` 标记未持久化

**文件**：`backend/app/services/index_service.py`（`_recover_pending`）+ 新增 Alembic 迁移

### 当前行为

```python
def _recover_pending() -> None:
    """启动时扫描 queued/running 的任务重新入队（崩溃恢复）。"""
    ...
    for t in rows:
        _queue.put_nowait((t.id, src_type, t.src_id, False))   # ← force 写死为 False
```

而 `index_tasks` 表的实际列为：

```
id, src_type, src_id, status, error, chunk_total, chunk_new,
created_at, started_at, finished_at
```

**没有 `force` 列。**

第一轮修复实现了「队列元组携带 force」，但未实现「随任务持久化」。

### 为什么必须改

违反 RAG-INC-03 / FR-IDX-06 在崩溃恢复场景下的语义。

**失败场景**（恰是全量重建存在的唯一理由）：

```
更换 embedding 模型
  → 触发全量重建（多个源入队，force=True）
  → 处理到一半进程崩溃 / 重启
  → _recover_pending 把剩余任务以 force=False 重新入队
  → 剩余源走增量：文本未变 → 指纹未变 → 复用旧模型算出的向量
  → 索引中一部分是新模型向量、一部分是旧模型向量，且无任何报错
```

混合了两个模型向量的索引，检索结果会静默劣化——**没有任何迹象提示出了问题**，这是最难排查的一类故障。

### 怎么改

1. 新增 Alembic 迁移，给 `index_tasks` 加列：

   ```sql
   ALTER TABLE index_tasks ADD COLUMN force BOOLEAN NOT NULL DEFAULT false;
   ```

2. `models.py` 的 `IndexTask` 增加对应字段
3. `enqueue(..., force=...)` 写入该列
4. `_recover_pending` 从 `t.force` 读取，不再写死 `False`

### 验收标准

| # | 操作 | 期望 |
|---|---|---|
| 1 | 触发全量重建，**在处理过程中重启后端** | 恢复后剩余任务的 `chunk_new == chunk_total`（仍按 force 执行） |
| 2 | 普通发布文章（改一个错别字），重启后端 | 恢复后仍按增量执行，`chunk_new < chunk_total` |
| 3 | 查 `index_tasks` 表 | 全量重建产生的行 `force = true`，普通索引产生的行 `force = false` |

> 第 2 条是回归验证：**不得为修 R1 而破坏 R6 增量索引**。

---

## 🟢 R2 · BM25 缓存的事后过滤导致限定单篇时检索退化

**文件**：`backend/app/rag/retriever.py`（`retrieve`）

### 当前行为

第一轮 M1 的修复把 BM25 改为「对**全量块**建一份缓存索引 → 检索 → 事后按 `flt` 过滤」：

```python
bm25, all_chunks = _get_cached_bm25(db)      # 全量索引，不含过滤
...
for i, score in bm25.search(query, settings.retrieve_top_k * 3):
    c = all_chunks[i]
    if not _match_flt(c, flt, src_ids):       # ← 事后过滤
        continue
```

缓存方案本身是对的，`* 3` 的超额召回也是有意的缓解。

### 为什么要改

超额召回只是缓解，不能消除。

**失败场景**：访客在某篇文章页发起「只问这篇文章」（FR-ASK-14）。若该文章的块不在全库 BM25 的前 30 名内，**BM25 一条结果都不剩**，混合检索静默退化为纯向量检索。

向量那一路是在数据库层应用 `flt` 的（正确），所以不会返回空结果——但这恰恰掩盖了问题：**答案照样出来了，只是质量降了，没有任何迹象**。

而「在某一篇文章里找一个精确术语」正是 BM25 相对向量检索最有优势的场景（R4 / RAG-RETR-02 的立论基础）。

### 怎么改

当过滤条件足以把候选集缩到很小时，不走全局缓存，直接在候选块上临时建 BM25：

```python
if flt and flt.src_id is not None:
    # 单源候选块很少（通常几个到几十个），临时建索引开销可忽略
    chunks = load_chunks(db, flt)
    bm25 = BM25([c.content for c in chunks])
else:
    bm25, chunks = _get_cached_bm25(db)   # 全局缓存路径不变
```

也可扩展到 `dir_prefix` / `tag_ids` 命中集合较小的情形，但**一期只需覆盖 `src_id`**（FR-ASK-14 的路径）即可。

> 不要为此放弃全局缓存——无过滤查询是主路径，M1 的性能收益必须保住。

### 验收标准

| # | 操作 | 期望 |
|---|---|---|
| 1 | 在某文章页对该文**内部的一个精确术语**发起「只问这篇」 | 返回的来源中确有该文章的块，且 BM25 有贡献（可临时打印两路各自的命中数确认） |
| 2 | 不带 scope 的普通提问 | 仍走全局缓存路径，无每次重建（M1 的收益保住） |
| 3 | 发布新文章后不带 scope 提问 | 新块可被检索到（缓存失效仍正确） |

---

## ⚪ R3 · 分阶段耗时日志（可选）

**文件**：`backend/app/api/ask.py`、`backend/app/rag/agent.py`

### 背景

复审实测显示 NFR-PERF-02（首字 ≤3s）未达标：

| 问题 | sources 到达 | 首个 delta |
|---|---:|---:|
| 混合检索是怎么做的？ | 5.92s | **7.01s** |
| 增量索引怎么省 API 调用？ | 2.82s | **3.61s** |

**本轮不修**（见文末）。但那 5.92 秒是三段的合计——工具判定 LLM 调用、embedding 调用、检索——**未做拆分**，无法判定瓶颈究竟在模型 API 还是自身代码。

### 怎么改

在 `run_agent` / `ask.py` 中记录并 log 四段耗时：

```
tool_decision_ms   工具判定 LLM 调用
embed_ms           query 向量化 API 调用
retrieve_ms        向量检索 + BM25 + RRF
first_token_ms     生成首 token
```

只写日志即可，不必下发给前端。

### 为什么值得做

成本极低（几行），但把「是模型慢还是我们慢」这个问题变成**可查证的**。等将来内容量上去、真要优化时，数据已经在了，不用从头测。

### 验收标准

- 提问一次后，后端日志中出现四段耗时
- 不影响响应内容与事件顺序

---

## 已知问题（本轮明确不修）

### NFR-PERF-02 首字延迟未达标

**现状**：实测首字 3.6s ~ 7.0s，需求要求 ≤3s。且这是在**仅 14 个块**的数据集上。

**已确认不是 BM25 导致**：第一轮 M1 的缓存修复正确，14 个块下 BM25 开销可忽略。瓶颈在 `sources` 事件之前的那一段。

**结构性原因**：R5（按需检索做成模型可调用的工具）要求**两次串行 LLM 往返**——工具判定必须完整返回后，才能开始检索与生成。

**当前判断**：主要耗时可能来自模型 API 本身的响应延迟（未经拆分验证，见 R3）。

**将来若要优化，备选方案**：

| 方案 | 做法 | 说明 |
|---|---|---|
| A · 投机并行 | 工具判定的同时并行跑 embedding + 检索 | **即使瓶颈是模型延迟也依然有效**，因为它省的是两段的重叠时间。代价是不检索时白费一次 embedding 调用 |
| B · 判定用更快的模型 | 工具判定本质是二分类，不必用主生成模型 | 需另配模型 |
| C · 放宽指标 | 修改 NFR-PERF-02，或改为约束「首个状态提示 1s 内」 | 需求文档需同步更新 |

**复测前提**：内容量达到数百块之后再测才有参考价值。当前 14 块的数据完全不能代表上线后的表现。

---

## 第一轮修复的复审确认（不要回退）

以下均已逐条验证，**第二轮修复不得破坏**：

| 项 | 验证方式 | 结论 |
|---|---|---|
| H1 未检索路径受 R1 约束 | 实测问「Python 的 GIL 是什么」 | `used_retrieval=false`，回「知识库中没有相关内容」，未解释 GIL ✓ |
| H2 两条路径统一流式 | 实测未检索路径 | 逐字流式，无整段 yield ✓ |
| H3 force 全量重算 | 代码验证 | force 先清旧块再全量重算；非 force 分支未动 ✓ |
| H4 文档引用跳转 | 代码验证三处 | `initialPage` 1基→0基、`id="chunk-N"`、hash 自动切视图+滚动 ✓ |
| M1 BM25 缓存 | 代码验证 | 全局缓存 + 锁 + 失效接入 ✓ |
| M2 error 事件 | 代码验证前后端 | 后端 try/except 发 error；前端 `stream.ts:63` 已有分支 ✓ |
| M3 token 统计 | 实测 done 事件 | `tokens: {prompt: 662, output: 170}` ✓ |
| M4 status 时序 | 实测事件流 | `deciding → retrieving → generating` ✓ |
| A4 事件顺序 | 实测原始流 | `status×2 → sources → status → delta×99 → done` ✓ |
| L1/L2/L3 | 代码验证 | 均已修复 ✓ |
| 附录 A 全部 15 项 | 逐条回归 | 无一被破坏 ✓ |

另外，第一轮新增的 `_rebuild_sync()` + CLI 入口（`python -m app.services.index_service --rebuild`）**不在清单要求内**，但正确解决了「启动自检拒绝启动 → 进不了管理页 → 无法重建」的死循环，属于有价值的主动补充，予以保留。

---

## 修复完成后

提交复审时提供：

1. R1、R2 的验收结果（对照各自的验收标准逐条填写）
2. R1 第 2 条、R2 第 2/3 条三项**回归验证**结果
3. R3 是否实施
4. 任何未修复项及原因

修完这两项即可合并到 `main`。

> **合并后的第一件事**：导入真实内容（数十篇量级）。
> 本轮及上一轮的全部验收都在 14 个块上完成，只能证明代码路径连通，
> 无法证明检索质量与性能表现。内容到位后需重测 R4（混合检索质量）与 NFR-PERF-02。
