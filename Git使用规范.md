# Git 使用规范

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-08-31 |
| **读者** | 承担编码工作的人或工具 |

---

## 0. ⛔ 最重要的一条规则

> ## **可以提交，不许推送。**
>
> `git push` 一律由仓库拥有者本人执行。
>
> 任何工具、任何情况下都**不得执行** `git push`、`git push --force`、`gh pr create`，
> 也不得添加或修改 remote。

原因：推送是不可逆的对外动作。一旦把密钥、个人数据或未审查的代码推上公开仓库，
即使随后删除，内容也可能已被缓存或索引。本项目是**要公开的简历项目**，这条尤其重要。

其余 Git 操作（`add` / `commit` / `branch` / `checkout` / `merge` / `log` / `diff`）均可自由使用。

---

## 1. 仓库现状

```
路径：   E:\Study\Code\Blog
主分支： main
远程：   尚未配置（由拥有者本人添加）
首次提交：docs: 需求、技术方案、环境说明与开发实施说明
```

已完成的仓库配置：

| 配置 | 值 | 作用 |
|---|---|---|
| `core.quotepath` | `false` | 中文文件名在 `git status` 里正常显示，不再是一串转义码 |
| `core.autocrlf` | `false` | 换行符交给 `.gitattributes` 统一管理 |
| `core.eol` | `lf` | 检出为 LF，杜绝 CRLF 进入 Linux 容器 |

---

## 2. 绝不能进仓库的东西

`.gitignore` 已配置，但**提交前仍应扫一眼 `git status`**，工具生成的文件容易夹带。

| 类别 | 具体内容 | 后果 |
|---|---|---|
| **密钥** | `.env`、`*.key`、`*.pem` | ⚠️ 泄露 DeepSeek / 千问 API Key，会被他人盗用产生费用 |
| **运行时数据** | `backend/data/`（上传文件、配图、向量）、`pgdata/`、`*.db` | 体积大；可能含个人内容 |
| **依赖目录** | `node_modules/`、`.venv/`、`__pycache__/` | 体积巨大且可由 `requirements.txt` / `package.json` 重建 |
| **构建产物** | `.next/`、`out/`、`build/` | 生成物 |
| **工具本地配置** | `.vscode/`、`.idea/`、`.claude/`、`.cursor/` | 与项目无关的个人偏好 |

**`.env.example` 是例外，必须提交** —— 它是密钥清单的模板，只写键名不写值：

```
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=
DATABASE_URL=postgresql://blog:dev@127.0.0.1:5433/blog
ADMIN_USERNAME=
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
```

---

## 3. 分支策略

按《开发实施说明》的阶段划分，一个阶段一个分支：

```
main                    始终保持可运行状态
 ├── stage-0-skeleton   项目骨架
 ├── stage-1-posts      文章
 ├── stage-2-docs       知识库
 ├── stage-3a-index     索引管线
 ├── stage-3b-retrieval 检索
 ├── stage-3c-ask       问答
 └── stage-3d-polish    收尾
```

**流程：**

1. 从 `main` 切出阶段分支
   ```bash
   git checkout main
   git checkout -b stage-1-posts
   ```
2. 在该分支上按任务逐个提交
3. 阶段完成 → 自验 → **交付评审**
4. 评审通过后，由**仓库拥有者**决定合并与推送

> **评审未通过前不要合并回 `main`。** `main` 上的东西默认是"审过的、能跑的"，
> 这个约定在你需要回退时是唯一可靠的锚点。

---

## 4. 提交规范

### 4.1 粒度

**一个任务一次提交。** 任务号即《开发实施说明》§3 里的 `T` 编号。

不要攒着一次提交一整个阶段 —— 评审时无法定位问题引入点，回退时只能整块丢弃。
也不要一个任务拆成十几个"改了个字"的提交。

### 4.2 格式

```
<类型>: [<任务号>] <一句话说明>

<为什么这么做 / 关键实现点，2-5 行>

对应需求：<需求编号>
```

**类型**取以下之一：

| 类型 | 用于 |
|---|---|
| `feat` | 新功能 |
| `fix` | 修复缺陷 |
| `refactor` | 重构，不改变外部行为 |
| `docs` | 文档 |
| `chore` | 构建、依赖、配置 |
| `test` | 测试 |

### 4.3 示例

```
feat: [T1-7] 文章硬删除与二次确认

删除时在同一事务内级联清理三处：chunks 表中的块、
正文引用的已上传图片、以及磁盘上的图片文件。
需求要求硬删除而非软删除，因此不可恢复，前端加二次确认。

对应需求：FR-POST-14 / FR-POST-15
```

```
feat: [T3a-5] 增量索引：按内容指纹跳过未变化的块

指纹取 sha256(context_prefix + content)。重新索引时先比对，
仅对新增与变化的块调用千问 embedding。
实测修改一篇文章通常只有 2-3 块变化，API 调用量下降一个数量级。

对应需求：RAG-INC-01
```

### 4.4 提交信息的语言

**中文。** 项目文档全中文，提交信息保持一致。

### 4.5 关于 AI 协作署名

> ## **本项目不使用任何 AI 协作署名。**

提交信息中**不得出现** `Co-Authored-By:` 尾注，也不得以其他方式把 AI 工具标注为作者或共同作者。所有提交只署仓库拥有者一人。

原因：GitHub 会解析 `Co-Authored-By:` 尾注，把该身份计入仓库的 **Contributors 列表**，并显示在每条提交记录上（"某某 and xxx"）。本项目作为公开展示的作品，作者署名只保留本人。

**注意默认行为**：部分 AI 编码工具会**自动**在提交信息末尾追加此类尾注。使用前应确认并关闭该行为，不要等提交完再回头清理 —— 一旦推送到远程，移除就需要重写历史并强制推送。

---

## 5. 编码过程中的日常流程

```bash
# 1. 确认在正确的分支上
git branch --show-current

# 2. 做完一个任务后，先看改了什么
git status
git diff

# 3. 确认没有夹带不该提交的文件
#    尤其检查有没有 .env、data/、node_modules/
git status --short

# 4. 暂存并提交
git add <具体文件>            # 优先指定文件，谨慎使用 git add -A
git commit

# 5. ⛔ 到此为止。不要 push。
```

> **优先 `git add <文件>` 而不是 `git add -A`。**
> `-A` 会把工作区所有未忽略的新文件一并纳入，包括工具临时生成的、调试用的、
> 忘了删的。指定文件虽然啰嗦，但每次都会迫使你看一眼自己到底改了什么。

---

## 6. 交付评审时

在《开发实施说明》§6.2 要求的基础上，额外提供：

```bash
git log --oneline main..<阶段分支>     # 本阶段的提交列表
git diff --stat main..<阶段分支>       # 改动概览
```

评审会基于分支间的差异进行，因此**保持提交历史清晰可读**本身就是交付质量的一部分。

---

## 7. 出问题了怎么办

### 7.1 ⚠️ 不小心提交了 `.env` 或密钥

**这是最严重的情况，按以下顺序处理：**

1. **立刻去服务商控制台吊销并重新生成该密钥。**
   这一步不能省 —— 只要密钥在本地历史里存在过，就必须当作已泄露处理。
2. 从历史中移除文件。若尚未推送且只在最近一次提交里：
   ```bash
   git rm --cached .env
   git commit --amend
   ```
   若已在更早的提交中，需要重写历史（`git filter-repo` 等），**交由仓库拥有者处理**。
3. 确认 `.gitignore` 已覆盖该文件。

> 顺序很重要：**先吊销密钥，再清理历史。** 反过来做，清理期间密钥仍然有效。

### 7.2 提交信息写错了

尚未推送时，改最近一次：

```bash
git commit --amend
```

### 7.3 提交了不该提交的文件（非密钥）

```bash
git rm --cached <文件>
git commit --amend
```

### 7.4 想撤销最近一次提交但保留改动

```bash
git reset --soft HEAD~1
```

> `--soft` 保留工作区改动，`--hard` 会**直接丢弃**。
> 不确定用哪个时，一律先用 `--soft`。

---

## 8. 推送与上 GitHub（仅供仓库拥有者参考）

以下操作**由拥有者本人执行**，此处仅作流程备忘。

```bash
# 添加远程
git remote add origin git@github.com:<用户名>/<仓库名>.git

# 首次推送
git push -u origin main
```

**公开仓库前的检查清单：**

| | 检查项 |
|---|---|
| ☐ | 全库搜索确认没有任何真实密钥（含历史提交） |
| ☐ | `README.md` 已就绪 —— 招聘方大概率只看这一份 |
| ☐ | 确认文档中的个人路径（如 `D:\ENV\...`）是否要保留 |
| ☐ | 决定是否添加开源许可证 |
| ☐ | 仓库描述与 topics 填写完整 |

> `环境说明.md` 含本机的具体安装路径。作为个人记录很有价值，
> 但公开时是否保留、还是改写成通用描述，值得先想一下。
