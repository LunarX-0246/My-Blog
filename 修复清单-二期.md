# 二期评审 · 修复清单

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-09-01 |
| **评审范围** | 二期阶段 4、5、6、7、8（阶段 9a 未开工，符合范围约定） |
| **结论** | 红线基本守住，一期 25 条回归全过；**1 项必修，4 项应修，2 项次要** |

> **给执行修复者**：每条都给出文件、行号、当前行为、失败场景、修法与**可执行的验收标准**。
> 修完请逐条填写验收结果。
>
> **附录 A 列出本次验证正确的实现，修复时不得破坏。**

---

## 修复总览

| 编号 | 严重度 | 问题 | 主要文件 |
|---|---|---|---|
| B1 | 🔴 必修 | 多次 `search_kb` 时引用编号撞车、来源被覆盖 | `rag/agent.py`、`rag/generator.py` |
| B2 | 🟡 应修 | 缓存部分（T7-3~T7-5）的自验从未执行 | 无代码改动，补自验 |
| B3 | 🟡 应修 | 三条分支未合并，`交付记录.md` 将冲突 | Git 操作 |
| B4 | 🟡 应修 | `generator.py` 中两个 Prompt 已成死代码 | `rag/generator.py` |
| B5 | 🟡 应修 | 搜索每次请求重建 BM25 | `services/search_service.py` |
| B6 | 🟢 次要 | 缓存命中时整段 yield（**需决策**） | `api/ask.py` |
| B7 | 🟢 次要 | 浏览计数每次起一个线程 | `services/view_service.py` |

> 编号 B = 二期，避免与一期的 H/M/L、复审的 R 混淆。

---

# 🔴 B1 · 多次检索时引用编号撞车、来源被覆盖

**文件**：`backend/app/rag/agent.py:142, 176, 177, 204`、`backend/app/rag/generator.py:44`

## 当前行为

```python
# agent.py:142
sources: list[ScoredChunk] = []

for _ in range(settings.agent_max_rounds):        # AGENT_MAX_ROUNDS=5
    ...
    for tc in resp.tool_calls:
        if tc.name == "search_kb":
            # agent.py:176 —— 覆盖，不累积
            sources = retriever.retrieve(db, args.get("query") or question, flt=flt)
            # agent.py:177 —— 编号每次从 [1] 重新开始
            content = build_context(sources) if sources else "未检索到相关内容。"
```

`build_context`（`generator.py:44`）固定 `enumerate(sources, 1)`，每次调用都从 `[1]` 起编。

## 实测证据

用项目自身代码模拟模型两次调用 `search_kb`：

```
第 1 次 search_kb 的工具返回：      第 2 次 search_kb 的工具返回：
  [1] 文章A > 混合检索                [1] 文章B > 增量索引
  [2] 文章A > RRF
```

模型最终看到的上下文里 **`[1]` 出现两次，指向完全不同的内容**；
而 `AgentResult.sources`（`agent.py:204`）只保留第二次的结果。

## 失败场景

触发条件：模型在一轮对话中调用两次 `search_kb`。`AGENT_MAX_ROUNDS=5` 明确允许，
比较类问题（「X 和 Y 各自解决什么问题」）很容易触发。

| 模型引用 | 实际结果 |
|---|---|
| `[3]`（来自第一次检索） | 前端 `sources` 中没有 3 号，角标点不动 |
| `[1]`（想指第一次的文章A） | **前端渲染成文章B 的链接，跳到错误的来源** |

**第二种更严重** —— 它看起来是正常工作的，用户不会察觉自己被指向了错误的出处。

这等于把一期 H4 修好的「答案可溯源」在多工具场景下重新弄坏。而「可验证的出处」
是本项目相对通用对话产品的核心优势（《需求文档》§1.2）。

## 怎么改

1. `sources` 改为**累加**而非覆盖：`sources.extend(new_sources)`
2. `build_context` 接受起始编号参数（如 `build_context(sources, start=len(sources)+1)`），
   或统一在循环外集中编号，保证**全局编号唯一且与 `AgentResult.sources` 的下标一一对应**
3. 注意去重：两次检索可能召回同一个块，需按 `(src_type, src_id, seq)` 去重后再编号

## 验收标准

| # | 操作 | 期望 |
|---|---|---|
| 1 | 提一个会触发两次检索的比较类问题（如「混合检索和增量索引各自解决什么问题」） | 日志中 `search_kb` 被调用 ≥2 次 |
| 2 | 检查该次回答的 `sources` 事件 | 包含**两次检索的全部来源**，编号连续不重复 |
| 3 | 逐个点击回答中的引用角标 | 每个角标跳转到**正确**的原文位置 |
| 4 | 两次检索召回同一个块时 | 该块只出现一次，不重复编号 |
| 5 | 单次检索的普通问题 | 行为与修复前一致（回归） |

> 第 3 条必须逐个点，不能只看数量对不对 —— B1 的危害正是「数量看着对、指向是错的」。

---

# 🟡 B2 · 缓存部分的自验从未执行

**无代码改动，补做自验即可。**

## 背景

T7-3 ~ T7-5（问答结果缓存）是在原会话因上下文超限中断后，由评审方代为提交的
（commit `09c9cf9`，分支 `stage-7-stats-cache`）。后续会话未对其自验，
`交付记录.md` 中标注为「其自验记录另行补充」。

因此《二期实施说明》§5 阶段 7 要求的这条**关键验证从未执行**：

> 改一篇文章后再问同一问题 → 确认缓存已失效、重新生成（验 **N7**）

N7 恰恰是缓存最容易出错的地方 —— 失效没接上，就会返回引用了已删除块的过期答案。

## 验收标准

| # | 操作 | 期望 |
|---|---|---|
| 1 | 同一问题连问两次 | 第二次命中缓存，`done` 事件含 `cached: true`，**日志中无新的模型调用** |
| 2 | **改一篇文章后再问同一问题** | **缓存已失效，重新生成，无 `cached` 标记** | 
| 3 | 上传一份文档后再问同一问题 | 同上，缓存已失效 |
| 4 | 删除一篇文章后再问同一问题 | 同上，且答案不再引用被删内容 |
| 5 | 带 history 的多轮提问 | **不进缓存**（现实现只缓存无历史的单轮问题） |

---

# 🟡 B3 · 三条分支未合并，`交付记录.md` 将冲突

## 现状

三条分支均从 `main` 切出、互不知情，且**都修改了 `交付记录.md`**：

| 分支 | 内容 | 提交数 |
|---|---|---|
| `stage-7-stats-cache` | T7-3 ~ T7-5 问答缓存 | 1 |
| `stage-7-stats` | T7-1 ~ T7-2 浏览次数 | 3 |
| `stage-8-import` | T8-1 ~ T8-3 导入脚本 | 3 |

代码文件经探测无冲突，但 `交付记录.md` 三方各写各的，合并时需人工合并。

## 建议的合并顺序

```bash
git checkout main

git merge --no-ff stage-7-stats-cache     # 先合缓存（T7-3~T7-5）
git merge --no-ff stage-7-stats           # 再合浏览次数（T7-1~T7-2）
                                          # 交付记录.md 若冲突，两段内容都保留
git merge --no-ff stage-8-import          # 最后合导入脚本
                                          # 同上
```

合并后需核对 `交付记录.md` 中阶段 7 的记录**同时包含** T7-1/T7-2 与 T7-3/T7-5 两部分，
且阶段 8 的记录完整。

## 验收标准

- `git diff --stat main stage-7-stats` / `stage-7-stats-cache` / `stage-8-import` 均为空
- `交付记录.md` 中阶段 7 段落同时含 T7-1~T7-2 与 T7-3~T7-5 的完成情况与自验记录
- 合并后后端能正常启动，`GET /api/health` 返回 200

---

# 🟡 B4 · 两个 Prompt 已成死代码

**文件**：`backend/app/rag/generator.py:8`（`SYSTEM_PROMPT`）、`:20`（`NO_RETRIEVAL_PROMPT`）

## 当前行为

二期把三种情形的约束合并进了 `FINAL_PROMPT`（`generator.py:31`），
`agent.py` 只引用 `FINAL_PROMPT` 与 `build_context`。

`SYSTEM_PROMPT` 与 `NO_RETRIEVAL_PROMPT` 现在**没有任何引用点**。

## 为什么要改

留着它们的风险很具体：将来有人为了调整回答风格去改 `SYSTEM_PROMPT`，
**改完发现行为没变**，然后开始怀疑缓存、怀疑模型 —— 而真正生效的是 `FINAL_PROMPT`。

这正是一期 H1 的形状：**Prompt 内容正确，但没有接上**。

## 怎么改

删除 `SYSTEM_PROMPT` 与 `NO_RETRIEVAL_PROMPT`，并在 `FINAL_PROMPT` 上方补一句注释，
说明它是唯一生效的最终生成约束，涵盖「检索 / 元数据 / 未用工具」三种情形。

## 验收标准

- `grep -rn "SYSTEM_PROMPT\|NO_RETRIEVAL_PROMPT" backend/` 无结果
- 问答行为不变：三种情形（触发检索 / 仅元数据工具 / 未用工具）的回答约束与修复前一致

---

# 🟡 B5 · 搜索每次请求重建 BM25

**文件**：`backend/app/services/search_service.py:87`

## 当前行为

```python
# search_service.py:87
bm25 = BM25([i.text for i in items])   # 每次搜索请求都全量拉取并重建
```

与一期 M1 修掉的是同一模式，新代码中再次出现。

区别：此处索引的是**整篇文章 / 整份文档**（非块），条目数远少于 chunks，
因此代价低于 M1 的场景 —— 但随内容增长仍是 O(n) 的 jieba 分词开销。

## 为什么要改

一期 M1 实测（块级）：500 条 198ms / 2000 条 809ms / 5000 条 1930ms。
搜索的条目数约为块数的十分之一量级，但内容更长，分词量并不成比例下降。

NFR-PERF-03 要求搜索结果 500ms 内返回。当前 15 个块测不出问题。

## 怎么改

复用一期 M1 的模式：进程内缓存搜索用的 BM25 索引，内容变更时失效
（`post_service` / `doc_service` / `index_service` 已有 `invalidate_bm25()` 的调用点，
可在同处一并失效搜索索引）。

**不要与检索用的块级 BM25 缓存混用** —— 两者的语料粒度不同（整篇 vs 块）。

## 验收标准

| # | 操作 | 期望 |
|---|---|---|
| 1 | 连续搜索 5 次不同关键词 | 首次可较慢，后续无重复建索引开销 |
| 2 | 发布一篇新文章后搜索其中的词 | 能搜到（缓存失效正确） |
| 3 | 删除一篇文章后搜索其中的词 | 搜不到 |
| 4 | 搜索只存在于草稿中的词 | 搜不到（回归，N6） |

---

# 🟢 B6 · 缓存命中时整段输出（**需决策**）

**文件**：`backend/app/api/ask.py`（缓存命中分支）

命中缓存时：

```python
yield _sse("delta", {"text": cached["answer"]})    # 整段，一个事件
```

形状与一期 H2 修掉的「整段 yield」相同，字面上与 FR-ASK-11「流式输出，逐字呈现」不符。

**但这可能是对的**：缓存答案瞬间完整出现，比把一段已知文本假装逐字播放体验更好。

**需要决策**：

| 选项 | 说明 |
|---|---|
| **A** 维持现状 | 缓存命中即时返回全文，前端以 `cached: true` 标注 |
| **B** 模拟流式 | 把缓存答案切片逐字下发，保持体验一致 |
| **C** 修改需求 | 在 FR-ASK-11 中明确「缓存命中除外」 |

若选 A，建议同时执行 C —— 否则文档与实现不一致，下次评审会再次被提出。

---

# 🟢 B7 · 浏览计数每次起一个线程

**文件**：`backend/app/services/view_service.py`

每次页面浏览 `threading.Thread(...).start()`。当前流量（日均几十 PV）完全无妨。

若将来流量上升，可改为批量累积后定期落库。**本轮不必处理**，记录备查。

---

# 附录 A · 本次验证正确的实现（不要破坏）

| 项 | 验证结论 |
|---|---|
| **N1** | `FINAL_PROMPT` 合并 R1/N1/H1 三分支，且在 `agent.py:201` 真的接上 |
| **N2** | `used_retrieval=("search_kb" in used_tools)`；前端区分「已检索知识库」/「查看了…」/「本次未检索知识库」 |
| **N3** | `qa_logs` 表字段确认无 IP / User-Agent |
| **N4** | 日志写在 `finally` 中，独立线程 + 独立 Session，成功 / 报错 / 客户端中断三种情况均落日志 |
| **N5** | 搜索复用 `rag/bm25.py`，未另起一套检索 |
| **N6** | 搜索只覆盖 `status=published` |
| **D4** | `list_posts` / `get_post_outline` / `get_post_section` **三个元数据工具全部过滤草稿** |
| **A4** | `sources` 事件先于 `generating` 状态与 `delta` 下发 |
| 多工具循环 | 有 `AGENT_MAX_ROUNDS` 上限，工具结果以 `role:tool` 正确回填，无死循环 |
| T7-1 | 用原子 SQL `view_count = view_count + 1`，避免读改写竞态 |
| T8 | 导入走 `index_source_sync` → `_index_source`，完整经过 chunker → embedder → store |
| 一期红线 | R1~R9 / A1~A6 / D1~D6 / E1~E5 **回归全过** |

---

# 附录 B · 未完成项（非缺陷）

| 项 | 说明 |
|---|---|
| **阶段 9a**（T9-1 ~ T9-5） | 未开工。续接提示词中明确约定「不要做阶段 9a」，**不属缺陷**。可作为下一批任务 |
| **阶段 9b**（T9-6 ~ T9-7） | 需阶段 8 导入真实内容后才有意义 |
| **NFR-PERF-02 首字延迟** | 已知未处理项，见《修复清单-第二轮》文末 |

---

# 修复完成后

提交复审时提供：

1. 每条修复项的**验收结果**（对照本清单各项的验收标准逐条填写）
2. B1 第 5 条、B5 第 4 条两项**回归验证**结果
3. B6 的决策选择（A / B / C）
4. 分支合并后的 `git log --oneline -15` 与 `交付记录.md` 阶段 7 段落
5. 任何未修复项及原因
