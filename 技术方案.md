# 个人博客与 RAG 知识库问答系统 · 技术方案

| | |
|---|---|
| **版本** | v0.1 |
| **日期** | 2026-08-31 |
| **作者** | LiRuxiang |
| **对应需求** | 需求文档 v0.3 |

> 本文档描述**怎么做**。需求本身以《需求文档》为准，两者冲突时以需求文档为准。
> 文中的 `FR-xxx` / `RAG-xxx` / `NFR-xxx` 均为需求文档中的需求编号。

---

## 1. 架构总览

### 1.1 部署架构

```
                          访客浏览器
                               │
                               │ HTTPS
                               ▼
┌──────────────────────────────────────────────────────────────┐
│  阿里云轻量应用服务器（2 核 / 2 GiB / 40 GiB，Ubuntu 22.04）  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                      Nginx（网关）                      │  │
│  │   /api/*  →  FastAPI    │    /*  →  Next.js            │  │
│  │   HTTPS 卸载 · Gzip · ★ SSE 关闭代理缓冲              │  │
│  └────────────────────────────────────────────────────────┘  │
│         │                              │                     │
│         ▼                              ▼                     │
│  ┌───────────────────┐        ┌──────────────────────┐       │
│  │  FastAPI (Python) │◄───────│  Next.js (Node)      │       │
│  │  业务 + RAG 全部  │  SSR   │  仅渲染，无业务逻辑   │       │
│  └───────────────────┘  取数  └──────────────────────┘       │
│         │                                                    │
│         ▼                                                    │
│  ┌────────────────────────────────────────────────────────┐  │
│  │            PostgreSQL 16 + pgvector                    │  │
│  │   业务表（文章/文档/标签/任务） + 向量表（chunks）      │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  文件存储：uploads/（原始文件） · images/（文章配图）         │
└──────────────────────────────────────────────────────────────┘
         │                                    │
         ▼                                    ▼
┌──────────────────────┐          ┌──────────────────────┐
│  千问 text-embedding │          │   DeepSeek Chat      │
│  （向量化）          │          │   （生成，流式）      │
└──────────────────────┘          └──────────────────────┘
```

### 1.2 三条请求路径

区分清楚这三条，是本方案最关键的部分。

| # | 场景 | 路径 | 说明 |
|---|---|---|---|
| ① | **SSR 渲染页面** | 浏览器 → Nginx → Next.js → **(127.0.0.1)** → FastAPI | 服务端到服务端，走内网。**必须转发浏览器的 Cookie**（见 §8.2） |
| ② | **客户端普通请求** | 浏览器 → Nginx → FastAPI | 上传、保存、删除等，同源，不经 Node |
| ③ | **流式问答** | 浏览器 → Nginx → FastAPI | **绝不经过 Node 中转**（见下） |

> **③ 为什么不能经过 Node**
>
> 每多一跳就多一层缓冲。Node 的流式转发极易写出隐性缓冲，表现为"转圈很久然后整段蹦出来"，NFR-PERF-02（第一个字 3 秒内出现）直接作废，而且这类问题在本机开发时完全不出现，只在部署后才暴露，排查成本极高。
>
> 同理，Nginx 侧必须 `proxy_buffering off`（见 §11.2）。

### 1.3 关键架构决策

| 决策 | 选择 | 理由 | 被否方案及原因 |
|---|---|---|---|
| 前端渲染方式 | **Next.js SSR** | 文章内容需被搜索引擎抓取；文章在运行时创建，静态导出不可行 | 纯静态 SPA —— SEO 作废 |
| 业务逻辑归属 | **全部在 FastAPI** | RAG 资产（切块 / BM25 / RRF / ReAct / 上下文压缩）全是 Python | Next.js API Routes —— 会导致业务逻辑两边漏 |
| 数据库 | **PostgreSQL + pgvector** | 一库两用；简历技术栈价值；可展示向量检索工程能力 | SQLite + numpy —— 资源更省但缺少可展示性（保留为对比后端，见 §6.5） |
| 向量检索 | **存储层抽象，双后端可切换** | 支撑对比实验（§12），同时证明架构可替换性 | 单一实现 —— 无法回答"为什么选它" |
| 关键词检索 | **Python 侧自实现 BM25** | 中文全文检索在 PG 需装 `zhparser`/`pg_jieba`；week10 已有成熟实现 | PG 全文检索 —— 中文效果一般且增加部署依赖 |
| 异步任务 | **进程内 asyncio 队列 + 任务表** | 2 GiB 内存下不值得为几十个任务引入消息中间件 | Celery + Redis —— 多两个常驻进程 |
| 模型调用 | **openai SDK + 自写降级封装** | week12 已验证；自己写降级逻辑比引库更能体现能力 | LiteLLM —— 多一层抽象，无额外收益 |
| 部署 | **Docker Compose** | 环境隔离、内存硬限制、跨机迁移；容器化本身是加分项 | 裸机 systemd —— 更省 50–80 MB 但少一项可展示能力 |

---

## 2. 技术选型

### 2.1 前端

| 项 | 选型 | 说明 |
|---|---|---|
| 框架 | Next.js 15（App Router）+ React 19 + TypeScript | |
| 样式 | Tailwind CSS | **只做暗色 → 直接把色板定义为暗色，全站不写一个 `dark:` 变体**，样式工作量减半 |
| Markdown 渲染 | `react-markdown` + `remark-gfm` + `rehype-slug` | `rehype-slug` 自动为标题生成 id，即 FR-VIEW-11 的锚点与引用落点 |
| 代码高亮 | `rehype-pretty-code`（Shiki） | 服务端高亮，浏览器不加载高亮器 |
| Markdown 编辑器 | `@uiw/react-md-editor` | 工具栏 + 分屏预览现成；图片粘贴/拖拽自行接 handler |
| PDF 阅读器 | **浏览器原生**（`<iframe>` + `#page=N`） | 零依赖满足 FR-VIEW-22；原选 `@react-pdf-viewer` 与 React 19 不兼容，见下方说明 |
| 状态管理 | **不引入** | 对话历史在 localStorage、登录态在 Cookie、筛选条件在 URL |
| 数据请求 | 原生 `fetch` | 不引 SWR / React Query |

### 2.2 后端

| 项 | 选型 | 说明 |
|---|---|---|
| 语言 | Python 3.12 | wheel 支持最成熟；**必须是独立的干净环境**（venv 或新建 conda env 均可），见 §10 |
| Web 框架 | FastAPI + Uvicorn | 原生支持流式响应 |
| ORM | SQLAlchemy 2.0 + Pydantic | pgvector 官方提供 SQLAlchemy 集成 |
| 迁移 | Alembic | 表结构会随开发演进，手工改 SQL 不可持续 |
| 数据库驱动 | `psycopg[binary]` + `pgvector` | |
| PDF 解析 | **PyMuPDF** | week10 已在用；**按页返回文本，页码天然可得**（RAG-CHUNK-07） |
| Markdown 解析 | `markdown-it-py` | 切块时取标题结构，比正则可靠 |
| 中文分词 | `jieba` | BM25 用；无 jieba 时降级为二元组（week10 已有此逻辑） |
| 向量运算 | `numpy` | 对比后端 + 融合排序用 |
| 模型调用 | `openai` SDK（兼容模式） | DeepSeek 与千问均兼容此协议 |

### 2.3 基础设施

| 项 | 选型 |
|---|---|
| 数据库镜像 | `pgvector/pgvector:pg16` |
| 反向代理 | Nginx（Alpine 镜像） |
| 编排 | Docker Compose |
| 证书 | Let's Encrypt（Certbot） |

### 2.4 明确不引入

| 不引入 | 原因 |
|---|---|
| LiteLLM | 多一层抽象；自写降级封装更能体现能力 |
| FAISS / Milvus / Chroma | pgvector 已覆盖；numpy 对比后端已足够 |
| Celery / Redis | 任务量极小，不值得两个常驻进程 |
| Redux / Zustand / Pinia | 无全局状态需求 |
| SWR / React Query | 数据流过于简单 |
| LangChain | 切块、检索、Agent 循环全部自实现，这是本项目的核心展示点 |

---

## 3. 目录结构

```
E:\Study\Code\Blog\
├── 需求文档.md
├── 技术方案.md
├── README.md
├── docker-compose.yml                 生产编排
├── docker-compose.dev.yml             本机只起 PostgreSQL
├── .env.example
│
├── frontend\
│   ├── src\
│   │   ├── app\
│   │   │   ├── (public)\
│   │   │   │   ├── page.tsx                    首页
│   │   │   │   ├── posts\page.tsx              文章归档
│   │   │   │   ├── posts\[slug]\page.tsx       文章详情
│   │   │   │   ├── tags\[slug]\page.tsx        标签页
│   │   │   │   ├── docs\page.tsx               知识库（目录树）
│   │   │   │   ├── docs\[id]\page.tsx          文档详情（双视图）
│   │   │   │   ├── ai\page.tsx                 AI 问答
│   │   │   │   └── about\page.tsx              关于
│   │   │   ├── (admin)\
│   │   │   │   ├── layout.tsx                  ★ 统一鉴权，只写一次
│   │   │   │   └── admin\
│   │   │   │       ├── posts\                  文章管理
│   │   │   │       ├── posts\[id]\edit\        编辑器
│   │   │   │       ├── docs\                   知识库管理
│   │   │   │       ├── index\                  索引管理
│   │   │   │       └── settings\               站点设置
│   │   │   └── login\page.tsx
│   │   ├── components\
│   │   │   ├── ask\        AskPanel / MessageList / Citation / SourceCard
│   │   │   ├── editor\     MarkdownEditor / ImageUploader / MetaForm
│   │   │   ├── doc\        DirTree / PdfViewer / TextView / ViewSwitch
│   │   │   ├── post\       Toc / CodeBlock / TagList / PrevNext / Related
│   │   │   └── ui\         Button / Dialog / Toast / StatusBadge
│   │   └── lib\
│   │       ├── api.ts      ★ FastAPI 调用封装（含 Cookie 转发）
│   │       ├── stream.ts   ★ SSE 解析
│   │       └── types.ts    与后端对齐的类型
│   ├── next.config.ts
│   ├── tailwind.config.ts
│   └── Dockerfile
│
└── backend\
    ├── app\
    │   ├── main.py                入口、路由挂载、★ 启动自检
    │   ├── config.py              配置与密钥（环境变量）
    │   ├── db.py                  引擎与会话
    │   ├── models.py              SQLAlchemy 表模型
    │   ├── schemas.py             请求/响应模型
    │   ├── deps.py                依赖注入：鉴权、限流、DB 会话
    │   ├── api\
    │   │   ├── auth.py
    │   │   ├── posts.py
    │   │   ├── docs.py
    │   │   ├── ask.py             ★ 流式问答
    │   │   ├── search.py          （二期）
    │   │   └── admin.py           索引管理、设置
    │   ├── rag\                   ★ 核心，不依赖 FastAPI
    │   │   ├── parser.py
    │   │   ├── chunker.py
    │   │   ├── embedder.py
    │   │   ├── bm25.py
    │   │   ├── store\
    │   │   │   ├── base.py        ★ 抽象接口
    │   │   │   ├── pgvector_store.py
    │   │   │   └── numpy_store.py
    │   │   ├── retriever.py
    │   │   ├── agent.py
    │   │   ├── memory.py
    │   │   ├── generator.py
    │   │   └── llm.py             ★ 模型调用与降级封装
    │   └── services\
    │       ├── post_service.py
    │       ├── doc_service.py
    │       ├── index_service.py   ★ 索引任务队列
    │       └── ratelimit.py
    ├── alembic\
    ├── experiments\               ★ 对比实验（§12）
    │   ├── bench_store.py
    │   └── report.md
    ├── data\                      运行时数据（不进 git）
    │   ├── uploads\               原始文件，扁平存储
    │   ├── images\                文章配图
    │   └── vectors\               numpy 后端的 .npy（对比实验用）
    ├── requirements.txt
    └── Dockerfile
```

**依赖方向（不允许反向）**

```
api\  ──→  services\  ──→  rag\
                       ──→  models
```

`rag\` 不 import FastAPI、不 import services。每个模块可独立 `python -m app.rag.retriever` 自检 —— 与 week10 各脚本可独立运行是同一模式，调检索质量时不该被 Web 层挡着。

---

## 4. 数据模型

### 4.1 实体关系

```
categories ──1:N──> posts ──1:N──> chunks
                      │
                      └──N:M──> tags <──N:M── documents ──1:N──> chunks

posts / documents ──1:N──> index_tasks
posts ──1:N──> images
```

### 4.2 建表 SQL

```sql
CREATE EXTENSION IF NOT EXISTS vector;

-- 索引状态枚举
CREATE TYPE index_status AS ENUM ('pending','queued','running','indexed','failed');
CREATE TYPE source_type  AS ENUM ('post','document');
CREATE TYPE post_status  AS ENUM ('draft','published');

-- ── 分类与标签 ─────────────────────────────────────────────
CREATE TABLE categories (
    id          SERIAL PRIMARY KEY,
    name        VARCHAR(64)  NOT NULL UNIQUE,
    slug        VARCHAR(64)  NOT NULL UNIQUE,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT now()
);

CREATE TABLE tags (
    id          SERIAL PRIMARY KEY,
    name        VARCHAR(64)  NOT NULL UNIQUE,
    slug        VARCHAR(64)  NOT NULL UNIQUE,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT now()
);

-- ── 文章 ───────────────────────────────────────────────────
CREATE TABLE posts (
    id            SERIAL PRIMARY KEY,
    title         VARCHAR(255) NOT NULL,
    slug          VARCHAR(255) NOT NULL UNIQUE,
    summary       TEXT         NOT NULL DEFAULT '',
    content_md    TEXT         NOT NULL DEFAULT '',
    category_id   INT          REFERENCES categories(id) ON DELETE SET NULL,
    status        post_status  NOT NULL DEFAULT 'draft',
    is_featured   BOOLEAN      NOT NULL DEFAULT false,
    read_minutes  INT          NOT NULL DEFAULT 0,
    view_count    INT          NOT NULL DEFAULT 0,
    idx_status    index_status NOT NULL DEFAULT 'pending',
    idx_error     TEXT,
    published_at  TIMESTAMPTZ,
    updated_at    TIMESTAMPTZ  NOT NULL DEFAULT now(),
    created_at    TIMESTAMPTZ  NOT NULL DEFAULT now()
);
CREATE INDEX posts_status_pub_idx ON posts(status, published_at DESC);

CREATE TABLE post_tags (
    post_id INT REFERENCES posts(id) ON DELETE CASCADE,
    tag_id  INT REFERENCES tags(id)  ON DELETE CASCADE,
    PRIMARY KEY (post_id, tag_id)
);

-- ── 知识库文档 ─────────────────────────────────────────────
CREATE TABLE documents (
    id            SERIAL PRIMARY KEY,
    original_name VARCHAR(255) NOT NULL,   -- 上传时的文件名，仅记录与下载用
    stored_name   VARCHAR(64)  NOT NULL UNIQUE, -- 服务器实际文件名，系统生成
    title         VARCHAR(255) NOT NULL,   -- 显示标题，可改
    dir_path      VARCHAR(512) NOT NULL DEFAULT '', -- ★ 逻辑路径，非物理目录
    description   TEXT         NOT NULL DEFAULT '',
    file_format   VARCHAR(16)  NOT NULL,   -- pdf / markdown / txt
    file_size     BIGINT       NOT NULL,
    page_count    INT,                     -- 仅 PDF
    parsed_text   TEXT         NOT NULL DEFAULT '', -- 供文本视图与切块
    view_count    INT          NOT NULL DEFAULT 0,
    idx_status    index_status NOT NULL DEFAULT 'pending',
    idx_error     TEXT,
    uploaded_at   TIMESTAMPTZ  NOT NULL DEFAULT now()
);
CREATE INDEX documents_dir_idx ON documents(dir_path);

CREATE TABLE document_tags (
    document_id INT REFERENCES documents(id) ON DELETE CASCADE,
    tag_id      INT REFERENCES tags(id)      ON DELETE CASCADE,
    PRIMARY KEY (document_id, tag_id)
);

-- ── 块（检索单元）★ 全系统核心表 ──────────────────────────
CREATE TABLE chunks (
    id             BIGSERIAL PRIMARY KEY,
    src_type       source_type  NOT NULL,
    src_id         INT          NOT NULL,
    seq            INT          NOT NULL,        -- 在源内的序号
    content        TEXT         NOT NULL,        -- 原始正文
    context_prefix TEXT         NOT NULL,        -- 标题路径 / 目录路径
    embed_text     TEXT         NOT NULL,        -- 实际送去向量化的文本
    fingerprint    CHAR(64)     NOT NULL,        -- ★ 增量索引依据
    page_no        INT,                          -- 仅 PDF
    anchor         VARCHAR(255),                 -- 标题锚点 / 文本视图锚点
    embedding      vector(1024),                 -- pgvector 后端使用
    created_at     TIMESTAMPTZ  NOT NULL DEFAULT now(),
    UNIQUE (src_type, src_id, seq)
);
CREATE INDEX chunks_src_idx  ON chunks(src_type, src_id);
CREATE INDEX chunks_fp_idx   ON chunks(fingerprint);

-- HNSW 索引：小数据量下 Postgres 可能仍走顺序扫描，属正常
-- 其收益拐点见 §12 对比实验
CREATE INDEX chunks_embedding_idx ON chunks
    USING hnsw (embedding vector_cosine_ops);

-- ── 文章配图 ───────────────────────────────────────────────
CREATE TABLE images (
    id          SERIAL PRIMARY KEY,
    stored_name VARCHAR(64)  NOT NULL UNIQUE,
    post_id     INT          REFERENCES posts(id) ON DELETE CASCADE,
    file_size   BIGINT       NOT NULL,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT now()
);

-- ── 索引任务 ───────────────────────────────────────────────
CREATE TABLE index_tasks (
    id          BIGSERIAL PRIMARY KEY,
    src_type    source_type  NOT NULL,
    src_id      INT          NOT NULL,
    status      index_status NOT NULL DEFAULT 'queued',
    error       TEXT,
    chunk_total INT,
    chunk_new   INT,                     -- 实际调用向量化的块数
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT now(),
    started_at  TIMESTAMPTZ,
    finished_at TIMESTAMPTZ
);
CREATE INDEX index_tasks_status_idx ON index_tasks(status, created_at);

-- ── 站点设置（预设问题、限流阈值等）────────────────────────
CREATE TABLE settings (
    key   VARCHAR(64) PRIMARY KEY,
    value JSONB NOT NULL
);

-- ── 问答日志（二期）────────────────────────────────────────
CREATE TABLE qa_logs (
    id             BIGSERIAL PRIMARY KEY,
    question       TEXT        NOT NULL,
    used_retrieval BOOLEAN     NOT NULL,
    hit_chunks     JSONB,                  -- [{chunk_id, score}]
    answer         TEXT,
    latency_ms     INT,
    tokens_prompt  INT,
    tokens_output  INT,
    error          TEXT,
    created_at     TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

### 4.3 关键字段说明

| 字段 | 作用 |
|---|---|
| `chunks.fingerprint` | `sha256(context_prefix + content)`。重新索引时对比指纹，只对新增/变化的块调用向量化 API（RAG-INC-01）。**这是控制 API 成本的核心机制** |
| `chunks.embed_text` | 实际送去向量化的文本 = `context_prefix + "\n" + content`。单独存一列是为了排查"为什么这个块召回不对"时能直接看到模型读到的是什么 |
| `chunks.src_type + src_id` | 多态关联。不拆成两张块表，因为检索时必须一次查全部（RAG-RETR-01） |
| `chunks.page_no` | 仅 PDF 有值。PyMuPDF 按页返回文本，解析时天然可得（FR-VIEW-22） |
| `chunks.anchor` | 文章块 = 标题的 slug id；文档块 = `chunk-{seq}` |
| `documents.dir_path` | **逻辑路径字符串**，如 `八斗学院/week10-RAG`。目录树由所有 `dir_path` 现推得出，不存在空目录（FR-DOC-11） |
| `documents.stored_name` | UUID + 扩展名。不用原始文件名：避免中文/空格/特殊字符的路径问题、同名冲突、路径穿越风险 |
| `index_tasks.chunk_new` | 本次实际调用向量化的块数。与 `chunk_total` 对比即可直观看到增量索引省了多少调用 |

---

## 5. API 接口清单

### 5.1 通用约定

- 前缀 `/api`，Nginx 按此路径分流
- 鉴权：登录后下发 `HttpOnly` + `SameSite=Lax` 的签名 Cookie
- 错误统一格式：`{"error": {"code": "...", "message": "面向用户的中文提示"}}`（FR-ASK-19、NFR-SEC-05）
- 时间一律 ISO 8601 带时区

### 5.2 认证

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/login` | 账号密码登录，失败计数与锁定（FR-AUTH-06） |
| POST | `/api/auth/logout` | 退出 |
| GET | `/api/auth/me` | 当前登录状态，供 SSR 鉴权使用 |

### 5.3 文章

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/posts` | 公开 | 列表，支持 `category` / `tag` / `status` / 分页 |
| GET | `/api/posts/{slug}` | 公开 | 详情（含 TOC 结构） |
| GET | `/api/posts/{slug}/related` | 公开 | 相关推荐，向量相似度（FR-VIEW-13） |
| GET | `/api/posts/{slug}/neighbors` | 公开 | 上一篇 / 下一篇（FR-VIEW-12） |
| POST | `/api/posts` | 博主 | 新建 |
| PUT | `/api/posts/{id}` | 博主 | 保存（含自动保存草稿） |
| POST | `/api/posts/{id}/publish` | 博主 | 发布，触发索引 |
| POST | `/api/posts/{id}/unpublish` | 博主 | 撤回为草稿，同步清索引（FR-POST-13） |
| DELETE | `/api/posts/{id}` | 博主 | 硬删除，级联清块与配图（FR-POST-14） |
| POST | `/api/posts/{id}/summary` | 博主 | AI 生成摘要（FR-POST-11） |
| POST | `/api/images` | 博主 | 上传配图，返回可引用 URL |

### 5.4 知识库

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/docs/tree` | 公开 | 目录树（由 `dir_path` 现推） |
| GET | `/api/docs` | 公开 | 列表，支持目录、格式、标签筛选 |
| GET | `/api/docs/{id}` | 公开 | 元信息 + 文本视图内容（按块） |
| GET | `/api/docs/{id}/raw` | 公开 | 原始文件流，供原文视图内联预览；带 `?download=1` 则触发下载 |
| POST | `/api/docs` | 博主 | 上传（单文件，≤50 MB） |
| PATCH | `/api/docs/{id}` | 博主 | 改标题 / 目录 / 描述 / 标签 |
| DELETE | `/api/docs/{id}` | 博主 | 删除，级联清块与原始文件 |

### 5.5 AI 问答（★ 重点）

```
POST /api/ask        Content-Type: application/json
                     Accept: text/event-stream

{
  "question": "……",
  "history": [ {"role":"user","content":"…"}, {"role":"assistant","content":"…"} ],
  "scope": { "post_slug": "xxx" }        // 可选，FR-ASK-14 限定范围
}
```

**SSE 事件协议**

```
event: status
data: {"stage":"deciding"}                 判断是否需要检索

event: status
data: {"stage":"retrieving"}               正在检索

event: sources                             ★ 必须在 delta 之前发送
data: {"used_retrieval":true,
       "sources":[
         {"n":1,"type":"post","title":"…","url":"/posts/xxx#rrf-融合",
          "excerpt":"…","score":0.82},
         {"n":2,"type":"document","title":"…","url":"/docs/12?page=7",
          "excerpt":"…","score":0.71}
       ]}

event: delta
data: {"text":"混合检索的核心是"}

event: delta
data: {"text":"把两路结果做 RRF 融合[1]。"}

event: done
data: {"latency_ms":2870,"tokens":{"prompt":1832,"output":416}}

event: error
data: {"message":"模型服务暂时不可用，请稍后再试"}
```

> **`sources` 必须先于 `delta` 下发。** 正文里的 `[1]` `[2]` 角标需要即时渲染成可点击链接，如果来源在最后才到，前端要么先渲染成纯文本再回填（会闪烁），要么把整段答案缓存到结束再显示（流式效果没了）。先发来源，角标一出现就能立即成为链接。
>
> `used_retrieval` 字段直接驱动 FR-ASK-10 的界面标注。

**其他问答接口**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/ask/presets` | 预设问题（FR-ASK-07） |

### 5.6 索引管理（博主）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/index/status` | 全部内容的索引状态 + 统计（FR-IDX-04、FR-IDX-07） |
| POST | `/api/admin/index/retry/{task_id}` | 失败重试（FR-IDX-05） |
| POST | `/api/admin/index/rebuild` | 全量重建（FR-IDX-06） |
| GET | `/api/admin/index/progress` | 重建进度（SSE） |

### 5.7 其他

| 方法 | 路径 | 说明 |
|---|---|---|
| GET/PUT | `/api/admin/settings` | 预设问题、限流阈值 |
| GET | `/api/search` | 二期 |
| GET | `/api/admin/qa-logs` | 二期 |

---

## 6. RAG 管线实现

### 6.1 数据流

```
文章(published)                    文档(PDF/MD/txt)
      │                                  │
      │                            parser.py
      │                       PDF → [(page_no, text)]
      └──────────────┬───────────────────┘
                     ▼
                chunker.py       按标题层级切 + 上下文前缀 + 指纹
                     ▼
                embedder.py      千问 text-embedding-v3（仅新增/变化的块）
                     ▼
                store/           pgvector 或 numpy
                     ▼
        ┌────────────┴────────────┐
   向量检索                   bm25.py
        └────────────┬────────────┘
              retriever.py        RRF 融合 + 范围过滤
                     ▼
                 agent.py         按需检索的工具调用循环
                     ▼
               generator.py       DeepSeek 流式 + 引用标注
```

### 6.2 解析（`parser.py`）

| 格式 | 处理 |
|---|---|
| PDF | PyMuPDF 逐页 `get_text()`，**返回 `[(page_no, text)]` 而非拼接的整串** —— 页码必须保留到切块阶段 |
| Markdown | 原样读取，交给 chunker 按标题切 |
| txt | 按空行分段 |

> 参考实现里常见的做法是把所有页拼成一个 `raw_text`，**页码在拼接的那一刻就永久丢失了**，FR-VIEW-22 的 PDF 引用跳页码将无法实现。这是必须避开的坑。

### 6.3 切块（`chunker.py`）

规则（对应 RAG-CHUNK-01 ~ 08）：

1. **文章**：用 `markdown-it-py` 取标题树，以 `##` / `###` 为界切分
2. **超长块**按段落二次切分，上限约 800 字
3. **代码块与表格整块保留**，不得从中间切开
4. **上下文前缀**
   - 文章：`文章标题 > 一级标题 > 二级标题`
   - 文档：`目录路径 > 文档标题 > 章节标题`，并并入 `description`
5. **`embed_text` = 前缀 + "\n" + 正文**，这才是送去向量化的内容
6. **`fingerprint` = sha256(前缀 + 正文)**
7. PDF 块记录 `page_no`；所有块记录 `anchor`

> 为什么必须有上下文前缀：块被切出来后即脱离原文，「它」「这个方法」「上面提到的」全部失去指代。标题路径是纯字符串拼接就能获得的语境补偿，是提升召回质量性价比最高的一招。

### 6.4 向量化（`embedder.py`）

- 千问 `text-embedding-v3`，1024 维
- 批量提交（每批 20），失败重试 3 次并退避
- **L2 归一化后存储** → 余弦相似度退化为内积，检索只需一次矩阵乘法
- 只对 `fingerprint` 未命中的块调用 API（RAG-INC-01）

### 6.5 存储层双后端（★ 核心设计）

```python
# rag/store/base.py
class VectorStore(ABC):
    @abstractmethod
    def upsert(self, chunks: list[ChunkVec]) -> None: ...

    @abstractmethod
    def delete_by_source(self, src_type: str, src_id: int) -> None: ...

    @abstractmethod
    def search(self, query_vec: np.ndarray, top_k: int,
               flt: SearchFilter) -> list[ScoredChunk]: ...

    @abstractmethod
    def rebuild(self, chunks: list[ChunkVec]) -> None: ...

    @abstractmethod
    def stats(self) -> StoreStats: ...
```

```python
@dataclass
class SearchFilter:
    src_types: list[str] | None = None    # ['post'] / ['document']
    tag_ids:   list[int] | None = None
    dir_prefix: str | None = None         # 只在某个知识库目录下检索
    src_id:    int | None = None          # FR-ASK-14 限定单篇
```

| 后端 | 实现 |
|---|---|
| `pgvector_store.py` | `ORDER BY embedding <=> :vec` + WHERE 条件过滤，HNSW 索引 |
| `numpy_store.py` | 启动时载入 `.npy` 到内存，矩阵乘法暴力检索（精确） |

通过 `config.py` 的 `VECTOR_BACKEND` 环境变量切换。**生产用 pgvector，对比实验用 numpy**（§12）。

> 这层抽象有三重价值：支撑对比实验；将来数据量增长换后端只改一个文件；本身就是"面向接口编程"的架构展示。

### 6.6 检索（`retriever.py`）

```
query ──┬──> embedder ──> store.search()  ──┐
        └──> bm25.search()                 ─┴──> RRF 融合 ──> top-k
```

- **RRF**：`score = Σ 1/(k + rank)`，`k=60`
- 两路都应用同一个 `SearchFilter`
- 返回带相似度分数（RAG-RETR-04），供日志与调试
- 只检索 `status='published'` 的内容（RAG-RETR-05）

> BM25 不可省略：技术内容中大量存在专有名词、函数名、报错信息、参数名，这类查询语义检索容易失准，而 BM25 一击命中。

### 6.7 按需检索（`agent.py`）

把检索做成一个**模型可调用的工具**，由模型自行决定是否调用（RAG-DEC-01）：

```python
TOOLS = [{
    "type": "function",
    "function": {
        "name": "search_kb",
        "description": "检索博主的文章与知识库文档。当问题涉及博主写过的内容、"
                       "技术观点或站内资料时使用；闲聊、常识性问题不要使用。",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {"type": "string"},
                "scope": {"type": "string", "enum": ["all", "posts", "docs"]}
            },
            "required": ["query"]
        }
    }
}]
```

- 模型未调用工具 → `used_retrieval=false`，界面明确提示「本次未检索知识库」
- 模型调用了工具 → 检索并回填，`used_retrieval=true`
- **一期只有 `search_kb` 一个工具**。二期扩展 `list_posts` / `get_post_outline` / `get_post_section` 时无需重构（RAG-DEC-03）

### 6.8 生成与引用（`generator.py`）

System Prompt 的硬约束（RAG-GEN-01、FR-ASK-15）：

```
你是这个技术博客的问答助手，只能基于下面提供的资料回答。

严格遵守：
1. 只使用「参考资料」中的内容作答，禁止使用你自己的通用知识补充或推测。
2. 每个关键论断后标注来源编号，如 [1]、[2]。
3. 如果参考资料不足以回答，直接说明「知识库中没有相关内容」，
   并推荐资料中最接近的 2-3 项。绝不编造。
4. 保持与博客一致的语气与术语。
```

> 常见教程模板里会写「若资料中没有提及，请结合你的通用知识予以说明」——**这与本项目的核心设计相悖**。访客若得到的是通用模型回答而非博主的观点，这个功能相对直接使用 DeepSeek 就没有任何意义了。

引用处理：检索结果按顺序编号 → 注入 Prompt → 模型输出 `[n]` → 后端把 n 映射为 URL 随 `sources` 事件下发。

### 6.9 上下文压缩（`memory.py`）

分层策略（RAG-GEN-05）：

| 层 | 处理 |
|---|---|
| 最近 2 轮 | 完整保留，超长的检索结果截断 |
| 更早的轮次 | 仅保留「问题 + 最终答案」，中间过程丢弃 |
| 超预算 / 超轮数 | 从最老的轮次整轮淘汰 |

> 依据：**刚发生的事需要细节，久远的事只需要结论。**「它的性能怎么样」要靠上一轮的完整上下文消解指代；三轮前检索到哪些片段，留个结论就够了。
>
> 与 week12 的差别：**服务端无状态**（FR-ASK-06，历史由前端传入），压缩在每次请求时现算。

### 6.10 从 week10 / week12 的搬运对照

| 目标文件 | 来源 | 改动 |
|---|---|---|
| `rag/bm25.py` | week10 `retriever.py` 的 `BM25` 类 | **原样搬**（含倒排索引与 jieba 降级） |
| `rag/retriever.py` | week10 `retriever.py` 的 RRF 融合 | 过滤条件从「地名匹配」改为 `SearchFilter` |
| `rag/parser.py` | week10 `parse_pdf.py` | 改为返回 `[(page_no, text)]`，保留页码 |
| `rag/embedder.py` | week10 `embedder.py` + week12 `agent_config.py` | 去掉本地 BGE，仅保留千问 API 路径 |
| `rag/agent.py` | week12 `react_function_calling.py` | 工具集换为 `search_kb`；改为流式 |
| `rag/memory.py` | week12 `memory.py` | 改为无状态，历史由请求传入 |
| `rag/llm.py` | week12 `probe_models.py` 的思路 | 扩展为「主模型失败自动降级到备用模型」 |
| `rag/chunker.py` | week10 `chunk_documents.py` | **重写**：改为按标题层级切，不用固定长度/语义切分 |
| `rag/store/numpy_store.py` | week10 `vector_backends.py` | 保留 numpy 路径，去掉 faiss / chroma |

---

## 7. 索引任务队列（`index_service.py`）

```
发布/上传 ──> 写 index_tasks(status=queued) ──> 放入 asyncio.Queue ──> 立即返回
                                                        │
                                            后台 worker 逐个消费
                                                        │
                        running ──> 解析 ──> 切块 ──> 比对指纹 ──> 向量化
                                                        │
                                            indexed  或  failed(+error)
```

设计要点：

1. **状态持久化在数据库**，不只在内存队列 —— 进程重启后可恢复
2. **启动时扫描** `status IN ('queued','running')` 的任务重新入队（`running` 说明上次是崩溃中断的）
3. **单 worker 串行消费** —— 向量化是外部 API 调用，并发只会更快触发限流，且 2 GiB 内存下不宜并行解析大 PDF
4. **失败记录原因**并可重试（FR-IDX-03、FR-IDX-05）
5. **全量重建**是一个特殊任务，遍历全部内容并忽略指纹（RAG-INC-03）

**启动自检**（FR-IDX-10）：`main.py` 在启动时校验 `settings` 中记录的向量模型标识与维度，与当前配置不一致则**直接启动失败并报错**，不允许带着错误的索引静默运行。

---

## 8. 前端实现要点

### 8.1 路由分组与鉴权

`(admin)/layout.tsx` 中统一校验：

```tsx
const me = await api.get('/auth/me');       // 服务端调用，带 Cookie
if (!me.ok) redirect('/login');
```

所有管理页面共用这一层，避免逐页漏写（NFR-SEC-05）。**服务端鉴权是必须的**，仅靠前端隐藏入口等于没有鉴权。

### 8.2 ★ SSR 的 Cookie 转发（最容易踩的坑）

Next.js 在服务端渲染时，是 **Node 进程**去调 FastAPI，浏览器的 Cookie 不会自动带上。必须手动转发：

```ts
// lib/api.ts
import { cookies } from 'next/headers';

export async function serverFetch(path: string, init?: RequestInit) {
  const cookieHeader = (await cookies()).toString();
  return fetch(`${INTERNAL_API_BASE}${path}`, {
    ...init,
    headers: { ...init?.headers, cookie: cookieHeader },
    cache: 'no-store',
  });
}
```

> **不做这一步的典型症状**：从站内点进管理页一切正常（客户端跳转时浏览器自带 Cookie），但**一刷新页面就跳登录页**（SSR 那次请求没有身份）。这个 bug 几乎人人都会遇到一次，提前知道能省很多时间。

`INTERNAL_API_BASE` 在容器内指向 `http://api:8000`，本机开发指向 `http://127.0.0.1:8000`。

### 8.3 流式问答（`lib/stream.ts`）

```ts
const res = await fetch('/api/ask', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ question, history, scope }),
});

const reader = res.body!.getReader();
// 按 SSE 格式解析 event / data，分派到 onStatus / onSources / onDelta / onDone
```

- 历史从 `localStorage` 读，每次请求带上（FR-ASK-06，服务端不存）
- 「清空会话」= 清 `localStorage` + 重置组件状态
- 收到 `sources` 后立即建立 `编号 → URL` 映射，后续 `delta` 中出现的 `[n]` 实时渲染为可点角标

### 8.4 文档双视图（`components/doc/`）

| 视图 | 实现 |
|---|---|
| 原文视图（默认） | PDF → `<iframe src="/api/docs/{id}/raw#page=N">`（浏览器原生阅读器）；MD → 渲染；txt → `<pre>` |
| 文本视图 | 渲染 `/api/docs/{id}` 返回的按块内容，每块 `id="chunk-{seq}"` |

引用跳转：URL 携带 `?page=7` → 原文视图把 iframe 的 src 拼成 `...raw#page=7`（1 基，PDF 规范的
Open Parameters）；`#chunk-12` → 切到文本视图并滚动。

> **为什么最后没用 `@react-pdf-viewer`**
>
> 选型时它是最省事的方案（`jumpToPage` 现成）。实际接上后：viewer 根节点渲染出 0 个子元素，
> **不报错、不进 loading、控制台干净** —— 页面上就是一片空白。根因是
> `@react-pdf-viewer/core@3.12.0` 与 React 19.1.0 不兼容。
>
> 换成浏览器原生阅读器后，`#page=N` 这个 PDF Open Parameters 参数直接满足跳页需求，
> 并且省掉了 `@react-pdf-viewer/core`、`@react-pdf-viewer/default-layout`、`pdfjs-dist`
> 三个依赖和 `public/pdf.worker.min.js`（约 1 MB）。
> 代价是失去了自定义工具栏与主题控制 —— 对「查看原文并核对引用」这个用途来说不重要。
>
> 后端配合：`/api/docs/{id}/raw` 按格式返回正确的 `Content-Type`（PDF 为 `application/pdf`），
> 且**默认内联预览**；只有带 `?download=1` 时才附 `Content-Disposition: attachment`。
> 早先无条件带 filename，浏览器一律当下载处理，原文视图永远是空的。

### 8.5 编辑器

- `@uiw/react-md-editor`，分屏预览
- 粘贴/拖拽图片 → `POST /api/images` → 在光标处插入 `![](url)`
- 自动保存：停止输入 3 秒后 `PUT /api/posts/{id}`，显示"已保存 HH:MM:SS"
- `beforeunload` 拦截未保存修改（FR-POST-07）

### 8.6 主题

Tailwind 配置中直接把默认色板定义为暗色（背景 `#111315`、正文 `#F1F0EB`，对齐参考站），**全站不出现一个 `dark:` 变体**。

---

## 9. 安全与限流

| 项 | 实现 |
|---|---|
| 密钥管理 | 全部走环境变量，`.env` 不进 git，仓库只保留 `.env.example`（NFR-SEC-01） |
| 登录 | 密码在配置中以哈希形式存放；失败 5 次锁定 15 分钟（FR-AUTH-06） |
| Cookie | `HttpOnly` + `Secure` + `SameSite=Lax` |
| 上传校验 | 校验扩展名、MIME、大小 ≤50 MB；**存储名系统生成**，杜绝路径穿越（NFR-SEC-03） |
| 正文渲染 | `react-markdown` 默认不执行 HTML；不开 `rehype-raw`（NFR-SEC-04） |
| 问答限流 | 单 IP 每小时 10 次（内存计数，单进程即可）；单问 ≤1000 字；每日全站总量熔断（FR-ASK-19~22） |
| 阈值配置 | 存 `settings` 表，改配置不改代码（FR-ASK-22） |
| 数据库暴露 | Postgres **不映射端口到宿主机**，仅容器内网可达 |

---

## 10. 本机开发环境

```
Windows 11
├── Docker Desktop → 只跑 PostgreSQL(pgvector) 一个容器
├── backend  : 独立 Python 3.12 环境 + uvicorn --reload   (127.0.0.1:8000)
└── frontend : npm run dev                                (127.0.0.1:3000)
```

**只把数据库放进容器**，前后端本地直跑，保留热重载与断点调试。

### 10.1 Python 环境的两条硬约束

隔离方式用 `venv` 或新建 conda env 均可，但必须满足：

1. **环境全新且干净，只装本项目依赖。** 不复用已有的学习/实验环境。原因是 Docker 镜像由 `requirements.txt` 构建 —— 环境里混入无关的重型依赖（如 torch、transformers、faiss），`pip freeze` 就会把它们带进镜像，体积从约 200 MB 膨胀到 3 GB 以上，而目标服务器只有 2 GiB 内存。手工挑拣依赖无法保证不漏，漏掉的部分只会在部署后以 `ImportError` 的形式暴露。
2. **一律使用 `pip install`，不使用 `conda install`。** 镜像内是 pip 安装，两边的包版本与构建方式必须一致，否则会出现"本机正常、镜像报错"且极难定位的问题。

```yaml
# docker-compose.dev.yml
services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: blog
      POSTGRES_USER: blog
      POSTGRES_PASSWORD: dev
    ports: ["5432:5432"]        # 仅本机开发映射
    volumes: ["./pgdata:/var/lib/postgresql/data"]
```

**避免 CORS**：`next.config.ts` 里把 `/api` 代理到后端，让本机也是同源，与生产行为一致：

```ts
async rewrites() {
  return [{ source: '/api/:path*', destination: 'http://127.0.0.1:8000/api/:path*' }];
}
```

启动顺序：`docker compose -f docker-compose.dev.yml up -d` → `alembic upgrade head` → 后端 → 前端。

---

## 11. 部署方案

> 一期在本机完成并验收后再执行本章（需求文档 §8.2）。

### 11.1 Docker Compose

```yaml
services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: always
    env_file: .env
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    command: >
      postgres -c shared_buffers=128MB
               -c max_connections=20
               -c work_mem=4MB
               -c effective_cache_size=512MB
    deploy: { resources: { limits: { memory: 400M } } }
    # ★ 不映射端口到宿主机

  api:
    build: ./backend
    restart: always
    env_file: .env
    depends_on: [postgres]
    volumes:
      - ./data:/app/data          # uploads / images
    deploy: { resources: { limits: { memory: 400M } } }

  web:
    build: ./frontend
    restart: always
    environment:
      INTERNAL_API_BASE: http://api:8000
    depends_on: [api]
    deploy: { resources: { limits: { memory: 300M } } }

  nginx:
    image: nginx:alpine
    restart: always
    ports: ["80:80", "443:443"]
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on: [api, web]
    deploy: { resources: { limits: { memory: 64M } } }
```

**前端镜像在本机构建后推送 / 或用多阶段 Dockerfile 在本机 build。绝不在服务器上执行 `next build`** —— Node 构建内存峰值轻松突破 1.5 GB，会直接拖死 2 GiB 的机器。

#### 镜像构建的三个决策

| 决策 | 选择 | 理由 |
|---|---|---|
| **后端 `--workers 1`** | 硬约束，不得改大 | BM25 索引缓存与 numpy 向量后端都是**进程内**状态。多 worker 会各持一份：内存翻倍（目标机器仅 2 GiB），且内容变更时 `invalidate` 只对当前进程生效，其余 worker 继续返回过期结果 —— 这是**正确性**问题，不只是性能问题。并发靠 async 处理模型 API 的 IO 等待即可 |
| **迁移写在后端 CMD 里** | `alembic upgrade head && exec uvicorn ...` | 单实例部署，无多副本并发迁移的竞争；迁移失败直接退出，避免出现「服务起来了但表结构是旧的」这种更难排查的状态 |
| **前端 `output: "standalone"`** | 已写入 `next.config.ts` | Next 只输出运行所需的最小依赖集，运行镜像无需完整 `node_modules`。实测前端镜像 335 MB；不开此项需带整个 `node_modules` |

**实测镜像体积**（两者均以非 root 用户运行）：

| 镜像 | 体积 |
|---|---|
| `blog-api`（python:3.12-slim） | 601 MB |
| `blog-web`（node:22-alpine，多阶段） | 335 MB |
| `pgvector/pgvector:pg16` | 621 MB |
| `nginx:alpine` | 94 MB |

合计约 1.65 GB 磁盘占用，40 GiB 系统盘充裕。**注意这是磁盘体积，与 §11.4 的运行内存预算是两回事。**

> 前端镜像从 338 MB 降到 335 MB，是移除三个 PDF 依赖与 `public/pdf.worker.min.js` 的结果。
> **只省了 3 MB**，看着与「删掉约 1 MB 的 worker 加三个包」的直觉不符 ——
> 原因正是 `output: "standalone"`：它只把**实际被引用到的**模块追踪进运行镜像，
> `pdfjs-dist` 的绝大部分本来就只存在于构建阶段，从没进过最终镜像。
> 省下的这 3 MB 基本就是 `public/` 下那个 worker 文件加上被追踪进去的 viewer 代码。
> 换个角度看，这是 standalone 确实在起作用的一个佐证。

### 11.2 Nginx 关键配置

```nginx
location /api/ {
    proxy_pass http://api:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

    # ★★ 流式问答的生死线
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 300s;
    chunked_transfer_encoding on;
}

location / {
    proxy_pass http://web:3000;
    proxy_set_header Host $host;
}

gzip on;
gzip_comp_level 6;
gzip_types text/plain text/css application/json application/javascript;
client_max_body_size 60M;      # ≥ 单文件 50 MB 上限
```

> `proxy_buffering off` 不加的后果：Nginx 会攒够一整块才转发，打字机效果消失，NFR-PERF-02 作废。**这个问题只在部署后出现，本机开发完全正常**，是最典型的"上线才炸"的坑。

### 11.3 Swap（必须）

```bash
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile && sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
sudo sysctl vm.swappiness=20
```

不是给常驻内存用的，是给**峰值兜底**：解析大 PDF、批量建索引时会有内存尖峰，有 swap 才不会被 OOM Killer 直接杀进程。

### 11.4 内存预算

| 组件 | 预估常驻 | 硬限制 |
|---|---|---|
| Ubuntu 22.04 基础 | 150 – 200 MB | — |
| Docker daemon | 50 – 80 MB | — |
| Nginx | ~20 MB | 64 M |
| Next.js | 150 – 200 MB | 300 M |
| FastAPI + numpy + BM25 | 200 – 250 MB | 400 M |
| PostgreSQL（调参后） | 250 – 350 MB | 400 M |
| **合计** | **约 820 – 1100 MB** | |
| **余量（2048 MB）** | **约 950 – 1200 MB** | |

### 11.5 备份与迁移

改用 PostgreSQL 后，数据由两部分组成（NFR-OPS-01）：

```bash
# 1. 数据库
docker compose exec -T postgres pg_dump -U blog blog | gzip > backup/db_$(date +%F).sql.gz

# 2. 文件
tar czf backup/files_$(date +%F).tar.gz data/uploads data/images
```

本机 → 服务器的首次迁移走同一套流程：本机 `pg_dump` + 打包文件目录 → 上传 → `psql` 恢复 + 解包。

---

## 12. 对比实验设计（`experiments/`）

**目的**：回答"为什么用 pgvector"这个问题，而不是停留在"教程这么写"。

| 项 | 设计 |
|---|---|
| **对比对象** | ① numpy 暴力检索（精确） ② pgvector 顺序扫描 ③ pgvector + HNSW |
| **数据规模** | **真实数据（约 5 千块）为主**，另做一档合成扩充（约 2 万块）看趋势即可 |
| **指标** | P50 / P95 检索延迟、**Recall@5**（以暴力精确检索为 ground truth）、常驻内存 |
| **固定变量** | 同一份题集、同一批向量、同一台机器、固定随机种子 |
| **产出** | `experiments/report.md`，同时发布为一篇博客文章 |

**不做大规模压测。** 本项目真实量级就是数千块，把这个量级测准、并给出一档趋势参考即可。为了凑出十万级数据去跑基准，既要额外的向量化开销，得出的结论对本项目也没有实际指导意义。

**合成数据的注意事项**：如果要做那一档扩充，**不能用纯随机向量** —— 1024 维空间里随机向量几乎两两等距，而 HNSW 恰恰依赖真实 embedding 的聚类结构工作，用随机向量会得出对 HNSW 明显偏低且失真的召回率。正确做法是**以真实向量为基底加小幅扰动重采样**，保住聚类结构。

**预期结论**（待实测验证）：本项目量级下暴力检索已经足够快且召回率 100%，HNSW 的收益拐点在更大的数量级上。

> 这份报告本身就是最好的简历材料：既证明会用 pgvector，又证明**知道它什么时候不该用**。而且它作为一篇博客文章会进入知识库，被自己的 AI 问答检索到，形成闭环。

---

## 13. 开发顺序

| 阶段 | 内容 | 完成标志 |
|---|---|---|
| **0** | 骨架搭建 | Docker 起 PG、Alembic 建表、FastAPI 与 Next.js 跑通、`/api/auth/login` 可登录 |
| **1** | 文章 | 编辑器可写可存可发布，前台可读，TOC / 上下篇正常 |
| **2** | 知识库 | 上传、解析、目录树、双视图、下载 |
| **3a** | 索引管线 | 切块 → 向量化 → 入库；索引状态可见、可重试、可全量重建 |
| **3b** | 检索 | 混合检索 + RRF 跑通，命令行可单测 |
| **3c** | 问答 | 流式输出、按需检索、引用可跳转、限流生效 |
| **3d** | 收尾 | 相关文章推荐（复用向量索引） |
| **—** | 对比实验 | `experiments/` 跑通并出报告 |
| **—** | 部署 | 备案完成后执行 §11 |

**3a 与 3b 必须分开做。** 检索质量的调试应当能脱离 Web 层，在命令行里反复跑，这是 week10 那套代码最值得保留的工作方式。

---

## 14. 风险与待验证项

| 项 | 说明 | 应对 |
|---|---|---|
| **PDF 解析质量** | 复杂排版（多栏、表格）解析结果可能较差，且系统内不可编辑 | 上传前自查；文本视图可直观看到 AI 读到的内容 |
| **原文视图与 AI 所见不一致** | 访客看原始 PDF，AI 读解析文本 | 提供文本视图作为对照通道 |
| **HNSW 在小数据量下不生效** | 几千行时查询计划器可能仍走顺序扫描 | 属正常现象，在 §12 实验中量化说明 |
| **千问 API 限流** | 批量索引时可能触发 | 批次间退避 + 失败重试 + 任务可重试 |
| **DeepSeek 服务波动** | 影响问答可用性 | `llm.py` 实现主备降级 |
| **2 GiB 内存余量** | 预算表为估算，未实测 | 部署后用 `docker stats` 实测校准；swap 兜底 |
| **本机与服务器环境差异** | 本机性能远高于目标机 | 内存相关判断一律以目标配置为准 |
| **前端构建内存** | `next build` 峰值 >1.5 GB | **绝不在服务器上构建** |

---

*本文档随开发推进更新。需求变更以《需求文档》为准。*
