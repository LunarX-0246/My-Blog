# 二期实施说明

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-09-01 |
| **读者** | 承担编码工作的人或工具 |
| **前置** | 一期 43 个任务已完成，两轮评审 14 项问题已修复并验证 |

> 本文档回答二期「怎么把它写出来」。需求由《需求文档》定义（二期即其中全部 P1 项，见 §8.3），
> 架构由《技术方案》定义。本文档只讲实施流程。

---

## 0. 本期已确定的三个决策

开始前先明确，避免实施中反复：

| 决策 | 选择 | 理由 |
|---|---|---|
| **问答缓存的失效策略** | **内容变更即清空全部缓存** | 精确失效需追踪「哪条缓存引用了哪些块」，复杂度高数倍；本项目内容更新频率低，全清的代价可忽略 |
| **是否实现更多模型可调用能力** | **实现，且优先级靠前** | 一期只有一个工具，多工具协作才是 Agent 的实质；一期架构已为此预留（RAG-DEC-03），现在做几乎零重构成本 |
| **内容导入的位置** | **作为对比实验实测部分（9b）的前置任务** | 对比实验需要真实数据量才有意义。当前知识库仅 15 个块、414 字，跑出来只会是「两种后端都是零延迟」这种无效结论 |

---

## 1. 文档体系与优先级

动手前必须读完：

| 顺序 | 文档 | 回答什么 |
|---|---|---|
| 1 | `需求文档.md` | 做什么。二期 = 其中全部 P1 项 |
| 2 | `技术方案.md` | 怎么设计。§12 是对比实验的设计 |
| 3 | `开发实施说明.md` | **一期的 25 条红线仍然全部有效** |
| 4 | `二期实施说明.md` | 本文档 |
| 5 | `修复清单.md` / `修复清单-第二轮.md` | 两轮评审发现的问题与「已验证正确、不要改动」清单 |
| 6 | `环境说明.md` / `Git使用规范.md` | 环境与提交规范 |

**冲突时优先级：需求文档 > 技术方案 > 本文档。**

若发现某条需求或设计不可行，**停下来提出，不要自行改设计后继续**。

---

## 2. ⚠️ 红线

### 2.1 一期的 25 条红线继续有效

`开发实施说明.md` §2 的 R1~R9 / A1~A6 / D1~D6 / E1~E5 **全部继续适用**。二期新增代码同样受其约束。

其中与二期直接相关、最容易被新代码破坏的：

| 红线 | 二期为何容易违反 |
|---|---|
| **R1** 回答只能基于检索内容 | 新增工具会引入新的作答路径，见 N1 |
| **R4** 必须混合检索 | 搜索功能若另起一套检索，会出现两套不一致的实现 |
| **A3** `rag/` 不得 import FastAPI / services | 新工具写在 `rag/` 里，容易顺手引入 db 会话之外的依赖 |
| **A4** `sources` 先于 `delta` | 多工具后事件流更复杂，顺序容易乱 |

### 2.2 二期新增红线

| # | 红线 | 常见错误做法 | 为什么 |
|---|---|---|---|
| **N1** | **新增工具不得成为绕过 R1 的新路径** | 模型调 `list_posts` 拿到标题清单后，基于自身通用知识展开评述 | 元数据类工具返回的是**事实**（标题、日期、数量），模型只能陈述这些事实，不得就其内容发挥。Prompt 必须约束 |
| **N2** | **`used_retrieval` 的语义不得被稀释** | 调了 `list_posts` 就标成「已检索知识库」 | 该字段对用户表示「答案有站内内容依据」。元数据工具与内容检索需在界面上区分，不能混为一谈 |
| **N3** | **问答日志不得记录可定位访客身份的信息** | 把 IP、User-Agent 落库 | FR-LOG-05。IP 仅用于限流计数，保留在内存，不写数据库 |
| **N4** | **日志写入不得阻塞问答响应** | 在 SSE 流中同步写库 | 会拖慢首字延迟；且写库失败不应导致问答失败 |
| **N5** | **搜索必须复用现有 BM25 实现** | 新引入搜索引擎，或另写一套分词与打分 | R7/R8 的延伸：检索能力自实现是本项目的展示点，且两套实现必然不一致 |
| **N6** | **搜索只覆盖已发布内容** | 直接查全表 | FR-SEARCH-05、D4。草稿不得被搜到 |
| **N7** | **缓存必须在内容变更时失效** | 只设过期时间 | 否则会返回引用了已删除块的过期答案 |
| **N8** | **对比实验不得用纯随机向量测召回率** | `np.random.randn(n, 1024)` 造数据测 Recall | 1024 维空间里随机向量几乎两两等距，而 HNSW 依赖真实 embedding 的聚类结构工作，会得出严重失真的结论。需以真实向量为基底加扰动重采样 |
| **N9** | **对比实验必须固定随机种子、同一题集、同一机器** | 每次跑用不同数据 | 否则结论不可复现，也不可比 |

---

## 3. 任务分解

阶段编号接续一期（一期到阶段 3d）。**每阶段完成后提交评审，通过后再进入下一阶段。**

### 阶段 4 · 问答日志（最优先）

> **为什么排第一**：它必须赶在真实流量到来之前上线。上线后才补，那段时间访客问了什么、
> 检索命中如何，**数据永久丢失**。《需求文档》§10 已将此列为风险。

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T4-1 | `qa_logs` 表已在一期建好，核对字段是否满足需求；如缺则加 Alembic 迁移 | 技术方案 §4.2 |
| T4-2 | 问答结束后**异步**写日志：时间、问题、是否检索、命中块及分数、回答、耗时、token | FR-LOG-01、**N4** |
| T4-3 | 流中断 / 出错时同样落日志，记录错误信息 | FR-LOG-01 |
| T4-4 | 日志列表页（仅博主）：时间倒序，可展开查看单次问答完整细节 | FR-LOG-02 |
| T4-5 | 筛选：是否检索、是否报错、时间范围 | FR-LOG-03 |
| T4-6 | 汇总统计：总提问数、检索命中率、平均耗时、累计 token | FR-LOG-04 |
| T4-7 | 确认不记录任何可定位访客身份的信息 | FR-LOG-05、**N3** |

### 阶段 5 · 多工具 Agent

> 一期只有 `search_kb` 一个工具。本阶段扩展到四个，让 AI 能回答**检索答不了的元数据问题**
> （「你写过几篇关于 RAG 的」「最近一个月更新了什么」「把那篇的目录列出来」）。
> 这是 RAG-DEC-03 预留的演进方向，一期架构已为此设计。

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T5-1 | `list_posts(tag?, category?, date_from?, date_to?, limit?)`：返回标题、日期、标签 | RAG-DEC-03 |
| T5-2 | `get_post_outline(slug)`：返回该文标题树 | RAG-DEC-03 |
| T5-3 | `get_post_section(slug, anchor)`：返回某一节完整正文 | RAG-DEC-03 |
| T5-4 | 多工具循环：支持模型在一轮内多次调用、串联使用（先 outline 再 section） | RAG-DEC-03 |
| T5-5 | Prompt 约束：元数据工具的返回只能被陈述，不得据此发挥 | **N1** |
| T5-6 | 前端区分展示：内容来源（可跳转引用） vs 元数据结果（清单/目录） | **N2** |
| T5-7 | 工具调用过程在 `status` 事件中可见（如「正在查看文章目录…」） | FR-ASK-18 |

> **T5-3 是「先粗后精」的关键**：先检索定位到某节，再把整节完整取回，
> 比一次性返回十个碎片更准，也更省 token。

### 阶段 6 · 全站搜索

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T6-1 | 搜索接口：复用现有 BM25，覆盖文章与知识库文档 | FR-SEARCH-01、**N5** |
| T6-2 | 结果含关键词高亮的匹配片段 | FR-SEARCH-02 |
| T6-3 | 按来源类型与标签筛选 | FR-SEARCH-03 |
| T6-4 | 无结果时引导至 AI 问答 | FR-SEARCH-04 |
| T6-5 | 只覆盖已发布内容 | FR-SEARCH-05、**N6** |
| T6-6 | 导航栏搜索入口 + 搜索结果页 | FR-VIEW-05 |

### 阶段 7 · 访问统计与问答缓存

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T7-1 | 文章与文档浏览次数计数 | FR-STAT-01 |
| T7-2 | 管理列表展示浏览次数，可按此排序 | FR-STAT-02 |
| T7-3 | 问答结果缓存：问题归一化后作键 | FR-ASK-23 |
| T7-4 | **内容变更时清空全部缓存**（决策 A） | **N7** |
| T7-5 | 命中缓存时不产生新的模型调用，且界面标注为缓存结果 | FR-ASK-23 |

### 阶段 8 · 内容导入（对比实验的前置）

> 当前知识库仅 15 个块、414 字，且全是测试夹具。
> 对比实验、检索质量评估、性能验收在此数据量下**全部无效**。

| 任务 | 内容 |
|---|---|
| T8-1 | 写一个批量导入脚本：读取指定目录下的 Markdown 文件，按文件名/首个 h1 取标题，自动生成 slug，导入为已发布文章并触发索引 |
| T8-2 | 脚本支持 `--dry-run` 预览将导入什么，避免误灌 |
| T8-3 | 导入后核对：块总数、索引状态、随机抽查几个块的上下文前缀是否正确 |

> **实际导入哪些内容由博主决定**，脚本只负责能力。现成素材：三份 `实验报告.md`
> （合计约 7 万字、69 个二三级标题），预计可产出数百个块。

### 阶段 9a · 实验框架与后端一致性（**不依赖数据量，可随时开始**）

> 本阶段与阶段 4~8 无依赖关系，可提前进行。
>
> **两个背景事实**：
> 1. `numpy_store.py` 已实现但**从未被真正执行过** —— `VECTOR_BACKEND` 一直是 `pgvector`。
>    本阶段是它的第一次真实运行。
> 2. 「两个后端在同一数据上返回一致结果」是**正确性**问题，与数据量无关，
>    当前 15 个块即可验证。此时不测，等有数据后差异会被误当成「实验结果」，极难定位。

| 任务 | 内容 | 对应设计 |
|---|---|---|
| T9-1 | `experiments/bench_store.py`：可切换 numpy / pgvector 顺序扫描 / pgvector+HNSW 三种后端 | 技术方案 §12 |
| T9-2 | 指标采集：P50/P95 检索延迟、Recall@5（以暴力精确检索为 ground truth）、常驻内存 | §12 |
| T9-3 | **合成数据生成器**：以真实向量为基底加扰动重采样。**真实样本不足时必须明确报错并拒绝运行**，不得退化为纯随机向量 | **N8** |
| T9-4 | **后端一致性校验**：同一数据、同一查询下，numpy 暴力检索与 pgvector 顺序扫描的 top-k 结果集必须一致（允许分数存在浮点误差，但命中集合与排序须相同） | 技术方案 §6.5 |
| T9-5 | 固定随机种子、题集与运行环境记录，保证结论可复现 | **N9** |

> **T9-3 的报错要求是刻意的。** 在没有真实数据时编写实验代码，最容易发生的错误就是
> 顺手写一个 `np.random.randn(n, 1024)` 当占位数据。1024 维空间中随机向量几乎两两等距，
> 而 HNSW 依赖真实 embedding 的聚类结构工作，用随机向量会得出对 HNSW 严重偏低且失真的
> 召回率结论 —— 且看不出任何异常。**必须在代码层面堵死这条路，而不是靠记得。**

### 阶段 9b · 实测与报告（**需阶段 8 的真实内容**）

| 任务 | 内容 | 对应设计 |
|---|---|---|
| T9-6 | 三种后端各跑一轮，真实数据为主，另做一档合成扩充看趋势 | §12 |
| T9-7 | 产出 `experiments/report.md`，含结论与 HNSW 收益拐点分析 | §12 |

> **不做大规模压测。** 本项目真实量级就是数千块，把这个量级测准并给一档趋势参考即可。

---

## 4. 编码约定

与一期一致（见 `开发实施说明.md` §4）：中文注释、说明「为什么这么做」、类型标注、
面向用户的错误为可理解中文、可调参数集中在 `config.py`、一个任务一次提交。

**新增可调参数一律加入 `.env.example`**，并保持与 `config.py` 的键名一致。

---

## 5. 每阶段自验方法

提交评审前自己先跑一遍并记录结果。

| 阶段 | 自验内容 |
|---|---|
| 4 | 提问 3 次（含 1 次触发检索、1 次不触发、1 次人为制造失败）→ 日志页三条都在且状态正确 → 展开可见命中块与分数 → **查库确认无 IP/UA 等身份信息** → 汇总统计数字与实际相符 |
| 5 | 分别提问「你写过几篇关于 X 的」「把某篇的目录列出来」「某篇某节讲了什么」→ 三个工具各被正确调用 → **追问一个元数据工具返回范围之外的问题，确认模型不发挥（N1）** → 界面上元数据结果与内容引用区分展示 |
| 6 | 搜一个精确技术词 → 文章与文档结果都出现、关键词高亮 → 搜一个只存在于草稿里的词 → **确认搜不到** → 搜一个不存在的词 → 引导至 AI 问答 |
| 7 | 同一问题连问两次 → 第二次命中缓存且无新模型调用 → **改一篇文章后再问同一问题 → 确认缓存已失效、重新生成** → 浏览文章后计数 +1 |
| 8 | `--dry-run` 预览 → 实际导入 → 块总数与预期量级相符 → 抽查 3 个块的上下文前缀正确 → 用导入后的内容提问，答案带正确引用 |
| 9a | **同一份数据跑 numpy 与 pgvector 顺序扫描，逐条比对 top-k 结果集是否一致**（当前 15 块即可）→ 合成数据生成器在真实样本不足时确实报错拒绝运行 |
| 9b | 三种后端各跑一轮 → 报告含延迟、召回率、内存三项 → 结论中明确指出 HNSW 的收益拐点 |

> **阶段 5 的 N1 验证和阶段 7 的缓存失效验证最容易被跳过，也最容易做错，必须实测并记录原始输出。**

---

## 6. Git 与交付

沿用 `Git使用规范.md`：

- 一阶段一分支：`stage-4-qalog`、`stage-5-tools`、`stage-6-search`、`stage-7-stats-cache`、`stage-8-import`、`stage-9a-bench`、`stage-9b-report`
- 一个任务一次提交，格式见 §4.2
- **提交信息中不得出现 `Co-Authored-By` 等任何 AI 协作署名**
- **⛔ 可以 commit，禁止 push。** 推送由仓库拥有者本人执行

交付评审时提供：阶段号与分支名、完成的任务号、§5 对应行的**实际执行记录**、**主动说明的偏离**、遗留问题、新增依赖及理由。

---

## 7. 评审依据

| 优先级 | 检查内容 |
|---|---|
| 1 | **一期 25 条红线 + 二期 N1~N9 逐条核对** |
| 2 | 需求覆盖：声明完成的任务对应需求是否真的实现 |
| 3 | 分层与依赖方向：`rag/` 是否被污染 |
| 4 | 安全与隐私：日志是否含身份信息、新接口鉴权 |
| 5 | 正确性：边界条件、错误处理、缓存失效、并发 |
| 6 | 可维护性：是否复用已有实现而非另起一套 |

**重点看**：`rag/agent.py`（多工具循环与 N1/N2）、`rag/generator.py`（Prompt 未被放松）、
日志写入路径（N3/N4）、搜索实现（N5/N6）、缓存失效触发点（N7）、
`experiments/`（N8/N9）。

---

## 8. 已知未处理项（二期不修，勿自行改动）

| 项 | 说明 |
|---|---|
| **NFR-PERF-02 首字延迟未达标** | 实测 3.6~7.0s，要求 ≤3s。已确认非 BM25 导致，结构性原因是两次串行 LLM 往返。详见 `修复清单-第二轮.md` 文末，含三个备选方案。**二期不处理**，但阶段 8 导入内容后可用一期已埋好的四段耗时日志（`tool_decision` / `embed` / `search` / `first_token`）重新观测 |
| **部署与 ICP 备案** | 域名已购买，备案进行中。部署属独立事项，见《需求文档》§8.2 |

---

## 9. 阶段完成对照表

| 阶段 | 任务 | 完成 | 自验 | 评审 |
|---|---|---|---|---|
| 4 问答日志 | T4-1 ~ T4-7 | ☐ | ☐ | ☐ |
| 5 多工具 Agent | T5-1 ~ T5-7 | ☐ | ☐ | ☐ |
| 6 全站搜索 | T6-1 ~ T6-6 | ☐ | ☐ | ☐ |
| 7 统计与缓存 | T7-1 ~ T7-5 | ☐ | ☐ | ☐ |
| 8 内容导入 | T8-1 ~ T8-3 | ☐ | ☐ | ☐ |
| 9a 实验框架与一致性 | T9-1 ~ T9-5 | ☐ | ☐ | ☐ |
| 9b 实测与报告 | T9-6 ~ T9-7 | ☐ | ☐ | ☐ |

二期完成后，对照《需求文档》§9.2 的四条二期验收标准做最终确认。
