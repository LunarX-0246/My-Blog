# 全量检查与测试 · 修复清单

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-09-03 |
| **范围** | 一期 + 二期全部功能的端到端实测，含真实内容灌入、前后端联调、对比实验复核 |
| **结论** | 发现并修复 **17 项问题**，其中 3 项属于「数字看着正常、结论却是错的」类型 |

> 编号 F = Full test（全量测试轮），与一期 H/M/L、复审 R、二期 B 区分。

---

## 前置：测试内容

系统此前只有 15 个块的占位内容，检索质量和性能表现都无从判断。
本轮在项目内新建 `seed/` 目录，自建了一套与本项目技术选型高度重合的语料：

```
seed/
├── README.md              用法说明
├── taxonomy.json          文章的分类 / 标签 / 精选归属
├── make_pdf.py            PDF 生成脚本（PyMuPDF）
├── posts/                 7 篇 Markdown 文章
└── docs/                  3 份知识库文档（md / txt / pdf），三级目录结构
```

选题围绕混合检索、切块策略、增量索引、按需检索、上下文压缩、Embedding、引用溯源
—— 好处是**检索结果对不对可以直接判断**，而不是对着无关文本猜「这条算不算准」。

灌入后：文章 8 篇（含「关于」页）、资料 3 份、**块 72 个**，
分类 3 个、标签 14 个、精选 3 篇。

---

## 修复总览

| 编号 | 严重度 | 问题 | 主要文件 |
|---|---|---|---|
| F1 | 🔴 必修 | 对比实验的合成数据**从没进过数据库**，Recall 数字无意义 | `experiments/bench_store.py` |
| F2 | 🔴 必修 | `enable_indexscan = on` ≠ 真的走了 HNSW，pg-hnsw 一行可能压根没用索引 | `experiments/bench_store.py` |
| F3 | 🔴 必修 | 图片粘贴上传会把上传期间新敲的正文整段覆盖 | `components/editor/MarkdownEditor.tsx` |
| F4 | 🟡 应修 | 详情页对不存在的 id / slug 返回 500 而不是 404 | `lib/server-api.ts` + 两个详情页 |
| F5 | 🟡 应修 | 搜索任何中文词都返回全站内容，`无结果` 分支永远走不到 | `services/search_service.py` |
| F6 | 🟡 应修 | 问答日志按日期筛选按 UTC 切日界，东八区选「今天」漏掉大半 | `api/admin.py` |
| F7 | 🟡 应修 | 站点设置字段名写错时静默丢弃，却返回「保存成功」 | `schemas.py` |
| F8 | 🟡 应修 | 「关于」页写死在 JSX 里，博主无法维护（违反 FR-VIEW-24） | `about/page.tsx` + `post_service.py` |
| F9 | 🟡 应修 | SVG 图片以 `image/svg+xml` 直出、无任何防护头 | `api/images.py` |
| F10 | 🟡 应修 | 标签页只列文章不列资料，与首页热门标签计数对不上 | `tags/[slug]/page.tsx` |
| F11 | 🟡 应修 | 热门标签把草稿也算进计数 | `services/taxonomy_service.py` |
| F12 | 🟢 次要 | 文章标题渲染两次（页头一次、正文 h1 一次） | `lib/heading.ts` + 两个详情页 |
| F13 | 🟢 次要 | 对比实验的「内存」一列三个后端读的是同一个进程 RSS | `experiments/bench_store.py` |
| F14 | 🟢 次要 | 搜索片段带着原始 Markdown 记号 | `services/search_service.py` |
| F15 | 🟢 次要 | 搜索高亮逐词 `str.replace`，搜 `mark` 会把标签自己包一层 | `services/search_service.py` |
| F16 | 🟢 次要 | 首页知识库区块仍是「将在后续阶段上线」占位文案 | `(public)/page.tsx` |
| F17 | 🟢 次要 | 兜底话术会被写进问答缓存，一次偶发失败被永久固化 | `api/ask.py` |

---

## 三个「看着正常、其实是错的」

这三条单独拎出来，因为它们不会报错、不会崩溃，只会给出一个**长得很像正确答案的错误数字**。

### F1 · 对比实验的合成数据从没进过数据库

`run_bench` 把扩充到 2000 条的语料喂给了 numpy，
却让 pgvector 那两行继续查库里那 68 行真实数据。

于是 Recall 实际计算的是：**「2000 条里的 top-10」有多少落在「68 条里的 top-10」**。
得到的数字是 **0.805** —— 看着就像一个正常的近似召回率。

暴露它的线索是一处自相矛盾：`pg-seq` 走的是顺序扫描，
理论上**必然精确**、Recall 必须是 1.0。它出现 0.805，说明比较的根本不是同一份数据。

**修法**：把语料装进独立 schema `bench_tmp`
（`CREATE TABLE ... LIKE public.chunks INCLUDING ALL`，连 HNSW 索引一起复制），
把会话 `search_path` 指过去，跑完 `DROP SCHEMA ... CASCADE`。
**生产表 `public.chunks` 全程只读不写**，不会被合成数据污染。

### F2 · `enable_indexscan = on` 不等于走了索引

它只是**允许**规划器用索引，不是强制。数据量小时规划器算出顺序扫描更便宜，
于是 `pg-hnsw` 那一行其实压根没碰 HNSW —— 两行数字几乎一样，
看着像「HNSW 没什么用」。

**修法**：每轮跑之前用 `EXPLAIN` 读一次实际执行计划，
把 `顺序扫描` / `索引扫描(HNSW)` 打进结果表。不猜，直接看。
装载后必须 `ANALYZE`，否则规划器拿的是默认行数估计。

### F3 · 图片上传会吃掉正文

`insertImages` 是异步的：它在上传**开始时**从闭包里抓走 `value`，
`await` 完 N 张图的上传（几百毫秒到几秒），再用这份**旧快照**去拼接：

```ts
onChange(value.slice(0, pos) + snippet + value.slice(pos));
```

用户在上传期间敲的所有字，会被这份旧快照整段覆盖。
表现是「插入图片后，刚才写的一段凭空消失」，不报任何错。

**修法**：用 `useRef` 持有最新正文，上传完成后基于 `valueRef.current` 拼接，
并把光标位置夹到新长度内（用户可能在上传期间删过字）。

---

## 修复明细

### F4 · 详情页把 404 变成 500

后端对不存在的 id / slug 已经正确返回 404（路径参数类型不对是 422），
但 `serverFetch` 把它抛成 `ApiError`，Server Component 里没人接，
访客拿到的是「服务器内部错误」—— 明明只是链接失效或手输错地址。
搜索引擎也会因此把失效链接当 5xx 而不是从索引里剔除。

**修法**：新增 `serverFetchOr404`，遇 404 / 422 调 `notFound()`，
其余错误（后端挂了）仍按错误处理，不伪装成 404。
文章详情页同时把三个并行请求改成「先确认文章存在，再取上下篇 / 相关推荐」——
否则 slug 无效时后两个也会抛出，成为无人接管的 rejection。

验收：`/docs/999` `/posts/不存在` `/docs/abc` 均返回 404，正常页仍 200。

### F5 · 搜索永远搜得到东西

中文分词会切出「的」「不」「了」这类单字，它们几乎出现在每篇内容里。
BM25 的 idf 平滑保证这类词权重极低，但**不为零** ——
于是任何中文查询都能给每一篇内容凑出一个正分。

实测搜「不存在的词xyz」返回**全站 11 条**，且每张卡片都**没有任何高亮片段**
（违反 FR-SEARCH-02），而 FR-SEARCH-04「无结果时引导去 AI 问答」那条分支永远走不到。

**修法**：结果必须真的含有某个查询词才算命中。
用于高亮和判定的词只取长度 ≥ 2 的；查询整个都是单字时（如搜「块」）退回用全部词，
否则会一条都筛不出来。

验收：

| 查询 | 修复前 | 修复后 |
|---|---:|---:|
| `不存在的词xyz` | 11 条，无高亮 | 2 条，均含「存在」且高亮 |
| `zzzqqq` | 0 条 | 0 条（无结果分支可达） |
| `块` | 5 条，无高亮 | 5 条，全部高亮 |
| `RRF` / `ReAct` / `增量索引` | 正常 | 正常 |

### F6 · 日期筛选按 UTC 切日界

`created_at` 存的是 UTC，而博主在管理页选的是自己日历上的日期。
代码把这个日期当成 UTC 日（`replace(tzinfo=utc)`），
东八区下选「今天」会漏掉本地当天 00:00–08:00 的全部记录 ——
早上刚问过的几条筛不出来，界面上没有任何提示，只显示「暂无数据」。

实测：全部 33 条日志都是当天写的，`date_from=今天` 返回 **0 条**。

**修法**：`datetime.combine(d, time.min).astimezone()` ——
作用在 naive datetime 上时正是按本机时区解释再补偏移。

验收：今天起 22 条 / 截至昨天 11 条 / 合计 33 条 / 明天起 0 条。

### F7 · 设置保存的静默失败

`SettingsUpdate` 用 Pydantic 默认配置，未知字段被静默丢弃。
两个字段都可选（少传一个是「不改这项」的正常语义），
这意味着**字段名传错的请求同样什么都不改，却照样返回 `{"ok": true}`** ——
管理页显示「保存成功」而设置纹丝不动，排查起来毫无线索。

**修法**：`model_config = ConfigDict(extra="forbid")`，字段名写错直接 422。

### F8 · 「关于」页无法维护

FR-VIEW-24 要求「内容由博主维护，不需要独立的编辑界面（可作为一篇特殊文章管理）」，
但实现是写死的 JSX，博主改不了。

**修法**：读 slug 为 `about` 的文章，用现有的文章编辑器维护；
文章还没建时显示引导文案而不是白屏。
同时在 `post_service` 引入 `RESERVED_SLUGS`，把它从文章归档、
上下篇、相关推荐里排除（它不是普通博文），管理端仍照常列出；
搜索结果里它的链接指向规范地址 `/about` 而非 `/posts/about`。

### F9 · SVG 的同源脚本执行面

SVG 是 XML，可以内嵌 `<script>`。浏览器直接打开 `/api/images/x.svg` 时，
脚本会在**本站同源**下执行，能读到管理端的会话 Cookie。
实测上传含 `<script>alert(document.cookie)</script>` 的 SVG 成功，
响应头 `Content-Type: image/svg+xml`，**无 CSP、无 nosniff**。

虽然只有博主能上传，但一张从别处复制来的 SVG 就足以中招。

**修法**：图片响应统一加
`Content-Security-Policy: default-src 'none'; style-src 'unsafe-inline'; sandbox`
与 `X-Content-Type-Options: nosniff`。对 `<img>` 正常显示无影响。

### F10 / F11 · 标签计数与标签页对不上

首页热门标签按「文章 + 文档」计数（FR-HOME-03），
但标签页只列文章 —— 首页写着 `RAG 6`，点进去只看到 5 条。
另外热门标签把草稿也算了进去，而标签页不会列出草稿。

**修法**：标签页补上「知识库资料」区块并显示 `共 N 项（文章 x · 资料 y）`；
热门标签计数只数已发布文章。

验收：`/tags/rag` 显示「共 6 项（文章 5 · 资料 1）」，与首页徽标一致。

### F12 · 标题渲染两次

Markdown 文件通常把标题写成开头的 `# xxx`，导入脚本正是从这个 h1 取的标题；
上传文档时博主填的标题也往往照抄它。页头已经用大字号显示过一次，
正文再渲染一次就成了「标题连着标题」。

**修法**：`stripLeadingTitle` 只处理**开头第一个**非空行、且文字与标题完全一致的 h1
（正文中间的 h1 是作者有意为之，不能动）；
配套的 `stripLeadingTitleToc` 同步从目录里去掉那一条 ——
否则目录会指向一个不存在的锚点，点了没反应。

**引用跳转不受影响**：h1 所在块的 `anchor` 本就是 `None`，没有引用指向它。
实测该文章的 5 个 h2 锚点与数据库中的 chunk anchor 逐字一致，目录无死链。

### F13 · 「内存」一列三行读的是同一个数

三个后端跑在同一个进程里、按顺序执行，numpy 的矩阵一旦载入就不释放，
测 pgvector 时读到的 RSS 里仍然含着那块矩阵 ——
三行数字几乎一样（170 / 170 / 170 MB），看着像「pgvector 也吃了 900 MB」，
而 pgvector 的内存其实在 PostgreSQL 服务端进程里，根本不在本进程。

**修法**：两个后端各报各的口径 —— numpy 报常驻矩阵字节数（`ndarray.nbytes`），
pgvector 报 `pg_total_relation_size`（表 + 全部索引）。
20000 块下分别是 78.1 MB 与 292 MB，与理论值吻合（20000 × 1024 × 4B = 78.1 MB）。

### F14 / F15 · 搜索片段的两处毛病

片段是从正文中间随手截的，很容易切进代码围栏、标题井号、列表符号里，
显示出来是 `## 纯向量检索 ```python` 这种夹生文本。
另外高亮是逐词 `str.replace`，第一轮插入的 `<mark>` 会成为后续词的匹配对象 ——
搜 `mark` 时会把标签本身又包一层。

**修法**：片段先过一遍轻量 Markdown 清理（去围栏 / 井号 / 列表符 / 表格竖线 / 强调符），
高亮改为按长度降序拼一个正则 alternation 单趟扫完。

### F16 · 首页占位文案

知识库早已上线，首页仍写着「知识库将在后续阶段上线」。
改为真入口：链到 `/docs`，显示资料份数与最近收录的标题。

### F17 · 兜底话术进了缓存

模型偶尔整段只吐工具调用标记，被过滤层清掉后什么都不剩，
此时下发一句兜底话术。但这句话会连同 `sources` 一起写进问答缓存 ——
一次偶发失败被永久固化，同一个问题此后永远返回这句道歉。

**修法**：加 `degraded` 标记，走了兜底就不写缓存。

---

## 对比实验：修复后的结论

修完 F1 / F2 / F13 后，实验数据才第一次可用。

环境：Windows 11 / Python 3.12 / PostgreSQL 16 + pgvector 0.8.6（Docker，本机）
语料：68 个真实块为基底扰动重采样；查询集 20 条，种子 42。

**2000 块**

| 后端 | P50 (ms) | P95 (ms) | Recall@10 | 内存/存储 (MB) | 实际扫描 |
|---|---:|---:|---:|---:|---|
| numpy | 1.34 | 1.93 | 1.0000 | 7.8 | 内存全扫 |
| pg-seq | 17.23 | 18.71 | 1.0000 | 29.4 | 顺序扫描 |
| pg-hnsw | 5.68 | 6.76 | 1.0000 | 29.4 | 索引扫描(HNSW) |

**20000 块**

| 后端 | P50 (ms) | P95 (ms) | Recall@10 | 内存/存储 (MB) | 实际扫描 |
|---|---:|---:|---:|---:|---|
| numpy | 27.64 | 29.45 | 1.0000 | 78.1 | 内存全扫 |
| pg-seq | 755.43 | 845.64 | 1.0000 | 292.0 | 顺序扫描 |
| pg-hnsw | 64.53 | 130.40 | **0.9600** | 292.0 | 索引扫描(HNSW) |

**HNSW 的收益拐点在千级，代价到万级才出现。**
2000 块时比顺序扫描快 3 倍且召回率仍是 1.0 —— 白赚的；
到 20000 块，顺序扫描退化到 755 ms（已不可用），HNSW 只要 64.5 ms，快 11.7 倍，
代价是召回率掉到 0.96，top-10 里平均漏掉 0.4 条。**这 4% 就是「近似」两个字的价格。**

一致性校验（T9-4）在同一份 20000 条语料上：20 组查询结果集与排序完全一致，
最大分数差 **0.0**。

完整分析见 `backend/experiments/README.md`。

---

## 附录 A · 本轮验证正确、修复时不得破坏的实现

以下均为实测通过，后续改动需保证不回归。

**内容与索引**

- 增量索引：14 次 PUT（改分类 / 标签 / 摘要）后块数与 `embedding_tokens` 纹丝不动，
  正文没变就不重算 —— 指纹比对生效
- 草稿全生命周期：草稿不入公开列表、详情 404 → 发布 → 索引 → 删除 → 无孤儿块
- 孤儿块检测与清理：管理页仅在存在孤儿时显示红色徽标与清理按钮

**问答**

- 事件顺序 `status → sources → delta → done`，`sources` 严格先于 `delta`（A4）
- 三轮多轮对话：无工具标记泄漏；`used_retrieval=False` 那轮如实说「根据前面提到的内容」，
  不谎称检索过（R1 / 规则 4）
- 引用编号连续、不悬空，与来源卡片一一对应（B1）
- 缓存命中：`tokens=0`、`cached=true`；多轮对话不缓存
- 限流三条：单问 1000 字上限、单 IP 每小时次数、阈值可在管理端改
- M2 流中出错：下发友好提示，**不泄漏任何内部细节**（无 key、无 Traceback、无异常类名），
  真实原因只写进仅管理员可见的问答日志

**鉴权**

- 10 个写接口未登录一律 401
- 管理端 7 个页面服务端鉴权（A6），未登录全部跳登录页 —— 不靠前端隐藏入口

**引用跳转**

- 文章锚点：5 个 h2 的 id 与数据库 chunk anchor 逐字一致，目录零死链
- PDF `?page=3` 正确跳到第 3 页（浏览器原生阅读器）
- 文档 `#chunk-N` 自动切到文本视图并滚动定位

**问答日志**

- 字段只有业务信息，**不落 IP / UA / session 等身份信息**（N3）
- 分页无重叠、按时间降序；检索率统计与明细自洽
- 按「是否检索」「是否报错」「日期区间」筛选均正确

**响应式**：375 / 768 / 桌面三档下首页、文档树、PDF 阅读、文章详情均正常。

**构建**：`npm run build` 18 条路由全部通过，**零错误零警告**。

---

## 未覆盖项

**管理端的客户端渲染部分**（列表加载后的表格、编辑器交互、索引页按钮）
未在浏览器中实测。原因：需要把真实会话 Cookie 注入浏览器，
那会把凭据留在对话记录或仓库文件里。

替代覆盖：这些页面的 SSR 骨架已验证渲染正常、鉴权守卫生效，
其背后的每一个接口都已直连测过（文章增删改查、发布 / 下架、AI 摘要、
文档上传 / 改标签 / 删除、索引状态 / 重建 / 清理孤儿、日志筛选、设置读写、图片上传）。

**建议**：登录后手动扫一遍这 7 个页面的实际渲染效果。
