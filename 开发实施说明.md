# 开发实施说明

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-08-31 |
| **读者** | 承担编码工作的人或工具 |

> **本文档回答「怎么把它写出来」。**
> 需求由《需求文档》定义，架构由《技术方案》定义，本机环境由《环境说明》定义。
> 本文档只讲**实施流程**：先做什么、怎么验、怎么交、评审看什么。

---

## 1. 文档体系与优先级

开始任何编码前，**必须先读完这四份文档**：

| 顺序 | 文档 | 回答什么 |
|---|---|---|
| 1 | `需求文档.md` | 做什么。约 110 条带编号需求（`FR-xxx` / `RAG-xxx` / `NFR-xxx`） |
| 2 | `技术方案.md` | 怎么设计。架构、选型、表结构、API、RAG 管线 |
| 3 | `环境说明.md` | 本机环境实际配置（端口、Python 环境、终端限制） |
| 4 | `开发实施说明.md` | 本文档。实施流程与验收 |
| 5 | `Git使用规范.md` | 分支、提交、交付。**含一条硬规则：可以提交，不许推送** |

**冲突时的优先级：需求文档 > 技术方案 > 本文档。**

若在实现中发现某条需求或设计确实不可行，**停下来提出，不要自行改设计后继续**。文档中的每一条约束都有其来由，绕过它往往意味着后面某个功能会失效。

---

## 2. ⚠️ 不可推翻的决策（红线）

以下每一条都是经过讨论确定的，且**恰好都是通用教程/默认写法会做反的地方**。编码时逐条对照。

### 2.1 RAG 相关

| # | 红线 | 常见错误做法 | 为什么不能这么做 |
|---|---|---|---|
| R1 | **回答只能基于检索内容，禁止用模型通用知识补充** | Prompt 里写「若资料中没有提及，请结合你的通用知识予以说明」 | 访客得到的将是通用模型回答而非博主观点，本功能相对直接使用 DeepSeek 就失去全部意义。见 RAG-GEN-01、FR-ASK-15 |
| R2 | **PDF 必须按页解析，保留页码** | `for page in reader.pages: raw_text += page.extract_text()` | 页码在拼接那一刻永久丢失，FR-VIEW-22（引用跳页码）无法实现。必须返回 `[(page_no, text)]` |
| R3 | **按标题层级切块，拼接上下文前缀** | 固定长度滑动窗口（如 `RecursiveCharacterTextSplitter(400, 40)`） | 会把代码块和表格从中间切碎；且丢失标题路径，块脱离上下文后指代全失。见 RAG-CHUNK-01~06 |
| R4 | **必须混合检索：向量 + BM25 + RRF 融合** | 只做 `ORDER BY embedding <=> query LIMIT k` | 技术内容中大量专有名词、函数名、报错信息，纯语义检索容易失准。见 RAG-RETR-01/02 |
| R5 | **按需检索：把检索做成模型可调用的工具** | 每次提问都先检索再生成 | 见 FR-ASK-09、RAG-DEC-01。且此设计是二期扩展更多工具的基础 |
| R6 | **必须做增量索引（内容指纹比对）** | 每次更新全量重算向量 | 直接决定 API 成本。见 RAG-INC-01 |
| R7 | **不引入 LangChain** | 用 LangChain 的 splitter / retriever / chain | 切块、检索、Agent 循环全部自实现是本项目的核心展示点 |
| R8 | **不引入 FAISS / Chroma / Milvus** | | pgvector 已覆盖；numpy 后端用于对比实验 |
| R9 | **服务端不存储访客对话历史** | 服务端 session 保存对话 | 见 FR-ASK-06。历史由前端 localStorage 保存并随请求传入 |

### 2.2 架构相关

| # | 红线 | 常见错误做法 | 为什么 |
|---|---|---|---|
| A1 | **业务逻辑全部在 FastAPI** | 用 Next.js API Routes / Server Actions 写业务 | RAG 资产全是 Python；业务两边漏会导致长期混乱 |
| A2 | **流式问答由浏览器直连 FastAPI，不经 Node 中转** | 前端调 Next.js 的接口，由它转发给后端 | 多一跳会引入隐性缓冲，打字机效果消失，NFR-PERF-02 作废 |
| A3 | **`rag/` 层不得 import FastAPI，也不得 import `services/`** | 在 rag 模块里直接用 `Depends`、读请求对象 | 依赖方向必须是 `api → services → rag`。破坏后 RAG 模块无法脱离 Web 层单独调试 |
| A4 | **`sources` 事件必须先于 `delta` 下发** | 正文流完之后再发来源 | 角标 `[1]` 出现时若无 URL 映射，只能先渲染纯文本再回填（闪烁）或整段缓存后显示（流式效果没了） |
| A5 | **SSR 请求必须转发浏览器 Cookie 给 FastAPI** | 直接 `fetch(api)` 不带 cookie | 症状：站内跳转进管理页正常，刷新页面即跳登录。见技术方案 §8.2 |
| A6 | **管理页鉴权写在 `(admin)/layout.tsx` 统一处理** | 每个页面各写一遍 | 漏写一个即是越权漏洞。见 NFR-SEC-05 |

### 2.3 数据与安全

| # | 红线 | 说明 |
|---|---|---|
| D1 | **文章删除是硬删除**，不是软删除 | 需求明确要求（FR-POST-14），且须级联清理索引块与正文引用的图片 |
| D2 | **上传文件用系统生成的唯一名存储**，不用原始文件名 | 避免中文/空格/特殊字符的路径问题、同名冲突、路径穿越风险。原始文件名仅作记录与下载用 |
| D3 | **知识库「目录」是逻辑路径字符串**，不是物理文件夹 | 物理存储扁平；「移动目录」只是改字符串 |
| D4 | **草稿不进索引、不在前台展示** | FR-POST-10 |
| D5 | **所有密钥走环境变量**，`.env` 不进 git | NFR-SEC-01 |
| D6 | **正文渲染不开 `rehype-raw`** | 防脚本注入，NFR-SEC-04 |

### 2.4 环境相关

| # | 红线 | 说明 |
|---|---|---|
| E1 | **本机数据库端口是 5433，不是 5432** | 5432 被本机 PostgreSQL 18 长期占用且仍在使用。容器内部与生产仍为 5432 |
| E2 | **Python 包一律 `pip install`，不用 `conda install`** | Docker 镜像内是 pip 安装，两边必须一致 |
| E3 | **只在 `blog` conda 环境里装包**，且只用 Anaconda Prompt | 本机未做 `conda init powershell` |
| E4 | **全站纯暗色，不写任何 `dark:` 变体** | Tailwind 色板直接定义为暗色 |
| E5 | **绝不在服务器上执行 `next build`** | Node 构建内存峰值 >1.5 GB，会拖死 2 GiB 的机器 |

---

## 3. 任务分解

按阶段推进，**每个阶段完成后提交评审，通过后再进入下一阶段**。

### 阶段 0 · 项目骨架

| 任务 | 内容 | 完成标准 |
|---|---|---|
| T0-1 | `git init`；`.gitignore`（`.env` / `data/` / `pgdata/` / `node_modules/` / `__pycache__/`）；`.env.example` | `git status` 干净，敏感目录已忽略 |
| T0-2 | `docker-compose.dev.yml`：仅 PostgreSQL，**映射 5433:5432** | `docker compose -f docker-compose.dev.yml up -d` 后可连上 |
| T0-3 | 后端骨架：`requirements.txt`、FastAPI 应用、健康检查接口 | `GET /api/health` 返回 200 |
| T0-4 | Alembic 初始化 + 首个迁移（技术方案 §4.2 全部建表） | `alembic upgrade head` 成功；Navicat 里可见全部表 |
| T0-5 | 前端骨架：`create-next-app`（TS + Tailwind）；Tailwind 暗色色板；`next.config.ts` 的 `/api` 代理 | `npm run dev` 页面可访问，页面背景为暗色 |
| T0-6 | 登录链路：`POST /api/auth/login`、`/api/auth/me`、`/api/auth/logout`；登录页；`(admin)/layout.tsx` 鉴权 | 能登录；**刷新管理页不跳登录**（验证 A5） |

> **T0-6 是阶段 0 的关键验收点。** 它同时打通了前端 → Nginx/代理 → 后端 → 数据库四层，并验证了 Cookie 转发。这条链路通了，后面都是往上堆功能。

### 阶段 1 · 文章

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T1-1 | 文章 CRUD 接口；slug 唯一性；草稿/发布状态 | FR-POST-08~10、13 |
| T1-2 | 分类与标签（编辑界面内可直接新建） | FR-POST-16 |
| T1-3 | Markdown 编辑器：工具栏、实时预览、代码高亮 | FR-POST-01~04 |
| T1-4 | 图片上传：粘贴/拖拽，自动插入语法 | FR-POST-05 |
| T1-5 | 自动保存草稿 + 离开页面确认 | FR-POST-06/07 |
| T1-6 | AI 生成摘要 | FR-POST-11 |
| T1-7 | 硬删除 + 二次确认 + 级联清理图片 | FR-POST-14/15、**D1** |
| T1-8 | 前台：归档页、详情页、TOC、标题锚点、代码块复制、上下篇 | FR-VIEW-06~12 |
| T1-9 | 首页、标签页、关于页 | FR-HOME-01~05、FR-VIEW-23/24 |

> 相关文章推荐（FR-VIEW-13）依赖向量索引，**放到阶段 3d**。

### 阶段 2 · 知识库

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T2-1 | 上传接口：格式校验、50 MB 上限、同名处理、**唯一存储名** | FR-DOC-01~07、**D2** |
| T2-2 | 解析：PDF **按页**、Markdown、txt | FR-DOC-08、**R2** |
| T2-3 | 目录树（由 `dir_path` 现推）、展开折叠 | FR-DOC-10~12、**D3** |
| T2-4 | 文档管理：重命名、移动目录、删除（级联清文件） | FR-DOC-14~18 |
| T2-5 | 前台：目录树 + 内容区布局 | FR-VIEW-15/16 |
| T2-6 | 双视图：原文视图（PDF 阅读器）/ 文本视图（按块带锚点） | FR-VIEW-17~19 |
| T2-7 | 原文件下载 | FR-VIEW-21 |

### 阶段 3a · 索引管线

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T3a-1 | `rag/chunker.py`：按标题切块、上下文前缀、指纹、页码、锚点 | RAG-CHUNK-01~08、**R3** |
| T3a-2 | `rag/embedder.py`：千问 embedding、批量、重试、L2 归一化 | |
| T3a-3 | `rag/store/`：抽象接口 + pgvector 实现 + numpy 实现 | 技术方案 §6.5 |
| T3a-4 | `services/index_service.py`：asyncio 队列、任务表、崩溃恢复 | FR-IDX-01~03 |
| T3a-5 | 增量索引：指纹比对，只算变化的块 | RAG-INC-01~03、**R6** |
| T3a-6 | 索引管理页：状态、重试、全量重建、统计 | FR-IDX-04~09 |
| T3a-7 | 启动自检：向量模型与索引不一致则拒绝启动 | FR-IDX-10 |

### 阶段 3b · 检索

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T3b-1 | `rag/bm25.py`（含倒排索引与分词降级） | RAG-RETR-02 |
| T3b-2 | `rag/retriever.py`：向量 + BM25 + RRF 融合 | RAG-RETR-01、**R4** |
| T3b-3 | `SearchFilter`：按类型、标签、目录、单篇过滤 | RAG-RETR-03 |
| T3b-4 | **命令行自测入口**：可脱离 Web 层直接跑检索 | 见下方说明 |

> **T3b-4 不是可选项。** 检索质量的调试必须能在命令行反复跑，不该受 Web 层牵制。`rag/` 各模块都应可 `python -m app.rag.retriever --query "..."` 独立执行。

### 阶段 3c · 问答

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T3c-1 | `rag/llm.py`：模型调用 + 主备降级 | |
| T3c-2 | `rag/agent.py`：`search_kb` 工具、按需检索判定 | FR-ASK-09、**R5** |
| T3c-3 | `rag/memory.py`：分层上下文压缩（无状态） | RAG-GEN-05、**R9** |
| T3c-4 | `rag/generator.py`：Prompt（含 **R1** 约束）、流式、引用编号 | RAG-GEN-01~04、**R1** |
| T3c-5 | `POST /api/ask` SSE 接口，事件顺序符合 **A4** | FR-ASK-11~13 |
| T3c-6 | 前端问答组件：流式渲染、检索状态标注、引用角标、来源卡片、复制、清空会话 | FR-ASK-01~08、10、16、17 |
| T3c-7 | 「只问这篇文章」入口 | FR-ASK-14、FR-VIEW-14 |
| T3c-8 | 限流、长度上限、每日总量熔断、友好错误提示 | FR-ASK-18~22 |
| T3c-9 | 预设问题 + 站点设置页 | FR-ASK-07 |

### 阶段 3d · 收尾

| 任务 | 内容 | 对应需求 |
|---|---|---|
| T3d-1 | 相关文章推荐（复用向量索引） | FR-VIEW-13 |
| T3d-2 | 响应式适配（手机 / 平板 / 桌面） | FR-VIEW-02/03 |
| T3d-3 | 对照《需求文档》§9.1 逐条自验 | |

---

## 4. 编码约定

| 项 | 约定 |
|---|---|
| 注释语言 | 中文 |
| 注释深度 | 关键处不只说"做了什么"，要说**为什么这么做、哪里容易踩坑** |
| 模块 docstring | 说明该模块职责、与上下游的关系 |
| 类型标注 | Python 侧函数签名带类型标注；前端全 TypeScript，避免 `any` |
| 错误处理 | 面向用户的错误必须是可理解的中文，不暴露技术细节或堆栈 |
| 配置 | 所有可调参数（阈值、批量大小、top-k、模型名）集中在 `config.py`，不散落在代码里 |
| 提交粒度 | 一个任务一次提交，提交信息写明任务号与需求号 |

---

## 5. 每阶段的自验方法

提交评审前，**自己先跑一遍**并记录结果。

| 阶段 | 自验内容 |
|---|---|
| 0 | 登录 → 进管理页 → **刷新页面**（不应跳登录）→ 退出 |
| 1 | 新建 → 写作（插图、AI 摘要）→ 存草稿（确认前台不可见）→ 发布 → 前台阅读（TOC、锚点、代码复制）→ 编辑 → 撤回 → 硬删除（确认图片已清理） |
| 2 | 上传三种格式各一份 → 目录树可见 → 双视图切换 → 下载原文件 → 重命名 → 移动目录 → 删除 |
| 3a | 发布一篇文章 → 索引状态流转到 `indexed` → **改一个错别字重新发布，确认 `chunk_new` 远小于 `chunk_total`**（验证增量索引）→ 人为制造失败（如临时写错 API key）→ 状态为 `failed` 且有原因 → 修复后重试成功 |
| 3b | 命令行跑检索：分别用**语义化问法**和**精确专有名词**各测几条，确认两路召回都在起作用 |
| 3c | 多轮对话（含指代消解）→ 观察是否标注了检索状态 → 点引用角标跳转是否正确（文章跳锚点、PDF 跳页码）→ **问一个知识库里没有的问题，确认不编造** → 超限流后提示是否友好 |
| 3d | 三种屏幕尺寸浏览；对照需求文档 §9.1 十条逐项打勾 |

> **阶段 3a 的增量索引验证和 3c 的"不编造"验证最容易被跳过，但恰恰是最容易做错的两处。**

---

## 6. Git 与交付

### 6.1 分支与提交

- 主分支 `main` 保持可运行
- 每个阶段一个分支：`stage-0-skeleton`、`stage-1-posts`、`stage-2-docs`…
- 提交信息格式：

```
[T1-7] 文章硬删除 + 二次确认

- 删除时级联清理 chunks 与正文引用的图片
- 对应 FR-POST-14 / FR-POST-15
```

### 6.2 交付评审时需要提供

1. **阶段号与分支名**
2. **本阶段完成的任务清单**（哪些 T 号）
3. **自验结果**：§5 对应行的实际执行记录，包括失败过又修好的
4. **主动说明的偏离**：任何与文档不一致的地方及原因
5. **遗留问题**：知道有问题但本阶段没处理的

> 第 4 点尤其重要。**主动说明的偏离不算问题，隐藏的偏离才是。** 评审会逐条对照文档，不一致的地方一定会被发现，提前说明能省掉来回确认。

---

## 7. 代码评审说明

### 7.1 评审依据

按以下顺序检查：

| 优先级 | 检查内容 |
|---|---|
| 1 | **红线（§2）逐条核对** —— 任何一条被违反都必须修改 |
| 2 | **需求覆盖** —— 本阶段声明完成的任务，对应需求编号是否真的实现 |
| 3 | **分层与依赖方向** —— `rag/` 是否被污染，业务逻辑是否漏进前端 |
| 4 | **安全** —— 鉴权、密钥、上传校验、注入防护 |
| 5 | **正确性** —— 边界条件、错误处理、并发与失败路径 |
| 6 | **可维护性** —— 重复代码、配置硬编码、命名 |

### 7.2 评审会重点看的地方

这几处是本项目最容易出问题的：

1. `rag/generator.py` 的 System Prompt —— 是否混入了"可用通用知识"之类的表述（**R1**）
2. `rag/parser.py` —— PDF 页码是否一路保留到块级（**R2**）
3. `rag/chunker.py` —— 是否真的按标题切、代码块是否完整、前缀是否拼对（**R3**）
4. `rag/retriever.py` —— BM25 是否真的参与了融合，还是只是写了没用上（**R4**）
5. `services/index_service.py` —— 指纹比对是否生效，失败路径是否可恢复（**R6**）
6. `api/ask.py` —— SSE 事件顺序（**A4**）
7. `lib/api.ts` —— SSR 的 Cookie 转发（**A5**）
8. `(admin)/layout.tsx` —— 鉴权是否真的覆盖了全部管理页（**A6**）
9. 删除路径 —— 文章/文档删除后，块、图片、原始文件是否都清了（**D1**）

### 7.3 评审节奏

- **按阶段评审**，不要写完整个项目才来一次
- 阶段 0 和阶段 3a 建议做得更细：前者定下所有基础模式（错误处理、鉴权、目录组织），后者是 RAG 正确性的地基，这两处的问题会向后蔓延到所有代码

---

## 8. 使用编码工具的建议

若由 AI 编码工具实施：

1. **先让它读完四份文档再动手**，明确告知优先级（§1）
2. **一次只给一个任务**（一个 T 号），不要让它一次生成整个项目 —— 大批量生成的代码里，红线被违反的概率显著更高，且难以逐条核查
3. **每个任务完成后立即对照 §2 红线自查**，不要攒到阶段末尾
4. **它若提出"更好的方案"**（例如建议引入 LangChain、改用滑动窗口切块、在 Prompt 里放开通用知识），先回到文档确认该决策的原因，再决定是否采纳。这些建议本身通常是通用场景下的合理默认值，只是与本项目的具体约束冲突
5. **不要让它同时改动多个阶段的代码**，否则评审时无法界定范围

---

## 9. 阶段完成对照表

供逐项打勾。

| 阶段 | 任务 | 完成 | 自验 | 评审 |
|---|---|---|---|---|
| 0 | T0-1 ~ T0-6 | ☐ | ☐ | ☐ |
| 1 | T1-1 ~ T1-9 | ☐ | ☐ | ☐ |
| 2 | T2-1 ~ T2-7 | ☐ | ☐ | ☐ |
| 3a | T3a-1 ~ T3a-7 | ☐ | ☐ | ☐ |
| 3b | T3b-1 ~ T3b-4 | ☐ | ☐ | ☐ |
| 3c | T3c-1 ~ T3c-9 | ☐ | ☐ | ☐ |
| 3d | T3d-1 ~ T3d-3 | ☐ | ☐ | ☐ |

一期完成后，对照《需求文档》§9.1 的十条验收标准做最终确认。
