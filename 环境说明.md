# 本机开发环境说明

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-08-31 |
| **适用** | 开发机（Windows 11 Pro 26200） |

> **这份文档记录的是「我这台机器上实际是怎么配的」**，含具体路径与版本，供自己日后重装或排查时对照。
> 面向他人的通用安装步骤请写在 `README.md` 里，不要混进本文档。

---

## 1. 环境总览

| 组件 | 版本 | 位置 / 说明 | 状态 |
|---|---|---|---|
| **Docker Desktop** | 4.88.1（Engine 29.7.2） | `C:\Program Files\Docker\Docker` | ✅ |
| **Docker Compose** | v5.4.0 | 随 Desktop 附带 | ✅ |
| **PostgreSQL（项目用）** | 16.15 + pgvector 0.8.6 | Docker 镜像 `pgvector/pgvector:pg16`（621 MB） | ✅ 已拉取 |
| **Python（项目用）** | 3.12.14 | conda 环境 `blog`，位于 `D:\ENV\Anaconda_envs\envs\blog` | ✅ |
| **Node.js** | v24.14.1 | 全局 | ✅ |
| **npm** | 11.11.0 | 随 Node | ✅ |
| **Git** | 2.53.0.windows.2 | 全局 | ✅ |
| **psql / pg_dump** | 18.3 | `D:\ENV\PostgreSQL\18\bin\`（**不在 PATH**） | ✅ 可用 |
| **Navicat** | — | 数据库图形客户端 | ✅ 已有 |
| 磁盘 | E: 可用 145 GB | 项目位于 `E:\Study\Code\Blog` | ✅ |

**未安装、也不打算装的**：nvm-windows、pnpm/yarn、DBeaver/pgAdmin、本机原生 pgvector 扩展。

---

## 2. 端口分配

| 端口 | 归属 | 说明 |
|---|---|---|
| **5432** | ⚠️ **本机原生 PostgreSQL 18** | 其他项目在用，**本项目不碰** |
| **5433** | 本项目的 PostgreSQL 容器 | 宿主机映射端口 |
| 8000 | FastAPI（后端） | |
| 3000 | Next.js（前端） | |

> **5433 这个选择必须记牢。** 本机已有一个开机自启的 PostgreSQL 18 服务（`postgresql-x64-18`，装在 `D:\ENV\PostgreSQL\18`）长期占用 5432，且仍在被其他项目使用，因此本项目的数据库容器映射到 5433。
>
> **容器内部仍然是 5432**，生产环境（Compose 内网互访）也是 5432 —— 只有本机开发时宿主机侧的映射端口是 5433。这个差异只体现在 `.env` 的连接串里，不影响任何代码。

---

## 3. 关键决策与原因

记录原因，避免几个月后自己推翻自己。

### 3.1 为什么用 Docker 跑 PostgreSQL，而不用本机已有的 PG18

1. **需要 pgvector 扩展。** Windows 上给原生 PostgreSQL 装 pgvector 需要用 MSVC 自行编译，而 PG18 版本很新，扩展兼容性不确定。Docker 镜像里是现成的。
2. **版本要与生产一致。** 生产用 pg16，本机也用 pg16，可避免一整类"本机正常、线上报错"的问题。
3. **不影响现有环境。** PG18 被其他项目使用，不能动。

### 3.2 为什么新建 conda 环境，而不复用 `AgentStudy`

`AgentStudy` 有 **145 个包**，含 `torch 2.6.0`、`transformers`、`faiss-cpu`、`chromadb` 等本项目完全用不到的重型依赖。

关键原因是 **Docker 镜像由 `requirements.txt` 构建**：

- 在混杂环境里开发，`pip freeze` 会把 torch 等一并带进镜像，体积从约 200 MB 膨胀到 3 GB 以上，而目标服务器只有 2 GiB 内存；
- 手工挑拣依赖无法保证不漏，漏掉的部分只会在部署后以 `ImportError` 暴露；
- 本项目要作为简历项目公开，`requirements.txt` 里出现 `torch` 是减分项；
- 双向污染：为本项目升级包可能弄坏学习仓库的代码，反之亦然。

新建的 `blog` 环境目前只有 4 个包（pip / setuptools / wheel / packaging），是干净的基线。

### 3.3 Python 包一律用 pip 装，不用 conda install

Docker 镜像内是 pip 安装。conda 安装的包在版本号与构建方式上可能与 PyPI 不同，会造成"本机跑通、镜像报错"且极难定位的问题。

### 3.4 Node 用 24，暂不引入 nvm

Next.js 15 官方主要在 Node 20/22 LTS 上验证，本机是 v24.14.1。但 Next.js 本身不含 native 模块，选用的 `@uiw/react-md-editor`、`@react-pdf-viewer` 也都是纯 JS，风险很低。

**若 `npm install` 出现 native 模块编译失败，届时再装 nvm-windows 退到 22 LTS**，不会浪费任何前期工作。

---

## 4. 各组件配置详情

### 4.1 Docker Desktop

- 已启动，Engine 正常
- **建议开启开机自启**（设置 → General）。不开的话，`docker` 命令会报连接失败，症状容易被误判为"Docker 装坏了"，实际只是没启动。

已拉取镜像：

```
pgvector/pgvector:pg16    621 MB
```

**已验证可用的能力**（用临时容器实测，测完即删）：

| 验证项 | 结果 |
|---|---|
| `CREATE EXTENSION vector` | ✅ |
| 版本 | PostgreSQL 16.15 + pgvector 0.8.6 |
| `vector(n)` 类型 | ✅ |
| HNSW 索引创建 | ✅ |
| 余弦距离算子 `<=>` | ✅ 结果正确 |

### 4.2 Python 环境

```
环境名：blog
版本：  Python 3.12.14
位置：  D:\ENV\Anaconda_envs\envs\blog
```

**⚠️ 必须用 Anaconda Prompt 开发。**

本机**没有**执行过 `conda init powershell`，`$PROFILE` 中没有 conda 钩子。因此：

| 终端 | `conda create` | `conda activate` |
|---|---|---|
| Anaconda Prompt | ✅ | ✅ |
| 普通 PowerShell | ✅（conda 在 PATH） | ❌ **失败** |
| Docker Desktop 内置终端 | ✅ | ❌ 失败 |

日后若嫌切终端麻烦，可执行 `conda init powershell` 补上（并建议同时 `conda config --set auto_activate_base false`，避免每次自动进 base 环境造成"装错环境"）。这不影响已建好的环境。

### 4.3 Navicat 连接配置

本机有**两个 PostgreSQL**，在 Navicat 里必须区分清楚，建议按下表命名连接：

| 连接名建议 | 主机 | 端口 | 说明 |
|---|---|---|---|
| `本机PG18-勿动` | localhost | **5432** | 其他项目在用 |
| `Blog项目-容器` | localhost | **5433** | 本项目 |

> 两个实例端口只差一位数。连错的典型症状是：在本机 PG18 里建了表、灌了数据，然后纳闷"为什么容器里查不到"。两边都"正常工作"，因此极难察觉。**连接名里写清楚是最省事的防护。**

---

## 5. 日常开发启动流程

```
1. 确认 Docker Desktop 已运行
2. cd E:\Study\Code\Blog
3. docker compose -f docker-compose.dev.yml up -d      # 起数据库
4. 开 Anaconda Prompt → conda activate blog → 启动后端
5. 另开终端 → 启动前端
```

> 第 3、4、5 步的具体命令待项目骨架搭好后补入本节。

---

## 6. 排查清单

按出现频率排序，都是本机环境特有的坑。

| 症状 | 原因 | 处理 |
|---|---|---|
| `docker` 命令报连接失败 | Docker Desktop 没启动 | 启动它；建议设开机自启 |
| `conda activate blog` 报错 | 在普通 PowerShell 里执行 | 换 Anaconda Prompt；或执行 `conda init powershell` |
| 数据库连上了但查不到自己建的表 | **连到 5432（本机 PG18）而非 5433（项目容器）** | 检查连接串端口 |
| `pip install` 装到了别的环境 | 提示符前不是 `(blog)` | `conda activate blog` 后再装 |
| 端口被占用起不来 | 5432 被 PG18 长期占用 | 本项目一律用 5433 |
| `npm install` native 模块编译失败 | Node 24 兼容性 | 装 nvm-windows 退到 22 LTS |
| 出现 `KMP_DUPLICATE_LIB_OK` 类 OpenMP 冲突 | conda 的 MKL 与其他库各带一份 OpenMP | 本项目不用 torch，理论上不会发生；若发生则改用 python.org 的独立 Python 建 venv |

---

## 7. 环境自检命令

重装或换机后，逐条跑一遍确认环境完整。

```bash
docker info --format "{{.ServerVersion}}"
docker images pgvector/pgvector
node -v
npm -v
git --version
conda run -n blog python --version
"D:\ENV\PostgreSQL\18\bin\psql.exe" --version
```

预期输出对照 §1 的版本表。

---

## 8. 尚未配置项

以下待项目开发推进到相应阶段再处理：

| 项 | 时机 |
|---|---|
| `docker-compose.dev.yml` | 搭建项目骨架时 |
| `requirements.txt` 及依赖安装 | 后端骨架时 |
| 前端依赖（`npm install`） | 前端骨架时 |
| `.env`（API 密钥：DeepSeek、千问） | 接入模型调用时 |
| 服务器端环境（Swap、Nginx、证书） | ICP 备案完成、准备上线时 |
