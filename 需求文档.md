# 个人博客与 RAG 知识库问答系统 · 需求文档

| | |
|---|---|
| **版本** | v0.3 |
| **日期** | 2026-08-31 |
| **作者** | LiRuxiang |

> **本文档只描述「系统要做什么」，不涉及技术选型。**
> 技术方案（前后端形态、数据库、部署架构等）在本需求评审通过后另行确定。

---

## 1. 项目概述

### 1.1 一句话定义

一个个人技术博客系统。博主可以在系统内写作文章、上传知识库文档；访客除了阅读，还能通过 AI 问答就站内内容提问，并得到**带可验证来源**的回答。

### 1.2 项目目标

1. 拥有一个可持续维护的个人技术写作阵地。
2. 把「自己写下的文章」与「收集的外部资料」统一变成可检索的知识库。
3. 让访客能够**提问**，而不只是翻找；且每个回答都能追溯到原文。
4. 对博主本人而言，这是一个完整的 RAG / Agent 应用实践项目。

### 1.3 项目定位

**Demo 性质的个人项目**，不是面向多用户的产品。设计上优先考虑「单人可维护、可持续演进」，而非高并发与水平扩展。

### 1.4 非目标（明确不做）

| 不做 | 原因 |
|---|---|
| 多用户注册、角色权限体系 | 系统只有博主一人拥有写权限 |
| 评论系统 | 垃圾内容治理成本远超收益 |
| 所见即所得（富文本）编辑器 | 见 §3.2.1，会破坏 RAG 切块所依赖的结构 |
| 编辑器的键盘快捷键 | 格式操作走工具栏即可，减少实现工作量 |
| 文章版本历史与差异对比 | 收益不匹配实现成本，靠备份即可回退 |
| 知识库文档内容的在线编辑 | 文档是外部资料，系统内只读 |
| 批量上传文档 | 每份文档都要单独填写目录、标题、描述、标签，批量反而更麻烦 |
| AI 回答的「重新生成」按钮 | 不满意时可直接换一种问法重新提问 |
| 本地部署的模型 | 所有模型能力通过云端 API 获得 |
| 亮色主题 | 只做暗色，见 §3.4.1 |
| 内容分「文章 / 笔记」两种类型 | 只保留「文章」一种，降低复杂度 |
| 多语言 | — |
| 独立移动端 App | 通过响应式网页覆盖移动端 |
| 项目作品集页面 | 待有作品可展示时再考虑 |

### 1.5 角色定义

| 角色 | 身份 | 权限 |
|---|---|---|
| **博主** | 唯一，需登录 | 全部功能：写作、上传、删除、索引管理、查看日志与统计 |
| **访客** | 匿名，无需登录 | 阅读文章、浏览知识库、搜索、使用 AI 问答 |

### 1.6 已确定的前提约束

1. **开发阶段全部在本机进行**，上线部署是一期功能完成之后的独立事项，不阻塞开发。
2. 目标运行环境为博主自有的云服务器（2 核 / 2 GiB 内存 / 40 GiB 磁盘，Ubuntu 22.04）。虽然暂不部署，但**设计时必须按此配置的资源上限来约束方案**。
3. **服务器上不部署任何模型**，所有模型能力通过云端 API 获得。
4. 大语言模型使用 **DeepSeek**；文本向量化使用 **千问 text-embedding 系列**。
5. 视觉风格参考 [Ting Lab](https://blog.wengtingxing.xyz)：纯暗色、克制、以排版为主。
6. 服务器位于中国大陆（华中·武汉），绑定自有域名需完成 ICP 备案；备案流程与开发并行推进。

---

## 2. 核心概念

| 术语 | 含义 |
|---|---|
| **文章** | 博主在系统内写作的博客内容，以 Markdown 格式存储 |
| **知识库文档** | 博主上传的外部资料（PDF / Markdown / txt），系统内只读 |
| **内容** | 「文章」与「知识库文档」的统称，两者都是 RAG 的数据源 |
| **块 (chunk)** | 内容被切分后的最小检索单元 |
| **索引** | 由所有块的向量表示与关键词倒排组成的检索结构 |
| **引用** | AI 回答中指向某个块的可点击标记，可跳转到原文对应位置 |
| **会话** | 访客与 AI 的一次连续多轮对话 |
| **按需检索** | 由模型自行判断某个问题是否需要查询知识库，而非每问必检 |

---

## 3. 功能需求

优先级约定：**P0** = 一期必须交付；**P1** = 二期交付，一期不实现。

### 3.1 认证与权限

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-AUTH-01 | P0 | 系统提供**单一博主账号**的登录入口。账号与密码在**部署时通过配置写入**，系统内**不提供注册功能**，界面上也没有创建账号的入口 |
| FR-AUTH-02 | P0 | 登录状态在浏览器内保持；超过设定时长后自动失效，需重新登录 |
| FR-AUTH-03 | P0 | 以下区域必须登录后访问：文章写作与管理、知识库上传与删除、索引管理、问答日志、访问统计 |
| FR-AUTH-04 | P0 | 未登录访客访问受保护区域时，跳转到登录页，而非展示错误 |
| FR-AUTH-05 | P0 | 提供退出登录 |
| FR-AUTH-06 | P0 | 登录失败有次数限制，超限后临时锁定，防止暴力破解 |

### 3.2 文章创作与管理

#### 3.2.1 编辑器

> **关于编辑器形态的说明**
>
> 系统提供**功能完整的 Markdown 编辑器**（带格式工具栏、实时预览），而**不是**所见即所得的富文本编辑器。
>
> 原因：富文本存储为 HTML，标题层级、代码块、列表结构埋在标签中，切块时需要反解析且极易错乱；而整个 RAG 切块策略建立在「按标题层级切分、把标题路径拼入检索文本」之上，这个结构基础不能丢。此外，技术博客必须频繁贴代码，富文本编辑器对代码块的处理普遍很差（自动转换引号、吃掉缩进、粘贴携带样式）。

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-POST-01 | P0 | 提供 Markdown 编辑器，内容以 **Markdown 格式**存储 |
| FR-POST-02 | P0 | 编辑器提供**格式工具栏**（标题、加粗、斜体、链接、列表、引用、代码块、表格等） |
| FR-POST-03 | P0 | 编辑器提供**实时预览**，所见即最终渲染效果 |
| FR-POST-04 | P0 | 代码块支持**语法高亮**，且在编辑与预览中效果一致 |
| FR-POST-05 | P0 | 支持**图片粘贴与拖拽上传**，上传后自动在光标处插入 Markdown 图片语法 |
| FR-POST-06 | P0 | **自动保存草稿**：停止输入若干秒后或定时自动保存，并提示最近保存时间 |
| FR-POST-07 | P0 | 离开页面时若存在未保存修改，给出确认提示 |

#### 3.2.2 文章属性

| 属性 | 必填 | 说明 |
|---|---|---|
| 标题 | 是 | — |
| URL 别名 (slug) | 是 | 用于文章链接；可由标题自动生成，允许手工修改；全站唯一 |
| 摘要 | 是 | 列表页与来源卡片展示；**支持由 AI 根据正文一键生成**（见 FR-POST-11） |
| 分类 | 是 | **单选**，大类划分（如「前端开发」「系统设计」「AI 与开发」） |
| 标签 | 否 | **多选**，细粒度主题；同时作为 RAG 的检索过滤字段 |
| 正文 | 是 | Markdown |
| 状态 | 是 | 草稿 / 已发布 |
| 精选标记 | 否 | 手动打标，用于首页「精选文章」区块 |
| 发布时间 | 自动 | 首次发布时记录 |
| 更新时间 | 自动 | 每次保存已发布文章时更新 |
| 阅读时长 | 自动 | 按正文字数估算 |
| 浏览次数 | 自动 | 见 FR-STAT-01 |

#### 3.2.3 管理操作

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-POST-08 | P0 | 新建、编辑、保存、发布文章 |
| FR-POST-09 | P0 | 文章列表页（仅博主可见），支持按状态、分类、标签筛选，按时间排序 |
| FR-POST-10 | P0 | **草稿不在前台展示，也不进入 RAG 索引**（避免半成品被 AI 当作观点引用） |
| FR-POST-11 | P0 | 提供「AI 生成摘要」按钮，根据正文自动生成摘要，博主可再编辑 |
| FR-POST-12 | P0 | 编辑已发布文章后**立即生效**，并自动触发重新索引与更新时间变更；不设「二次发布」流程 |
| FR-POST-13 | P0 | 已发布文章可**撤回为草稿**，撤回后同时从前台与索引中移除 |
| FR-POST-14 | P0 | 删除文章采用**硬删除**，数据不保留；删除时**必须同步移除其在索引中的所有块**，并清理其正文引用的已上传图片 |
| FR-POST-15 | P0 | 硬删除不可恢复，执行前必须有**二次确认**，确认提示中包含文章标题 |
| FR-POST-16 | P0 | 分类与标签可在文章编辑界面内直接新建，不需要单独的管理页面 |

### 3.3 知识库文档管理

> **定位说明**：知识库文档是博主上传的**外部资料**，在系统内**只读**。系统不提供对文档内容的编辑能力，但可以删除、重命名、移动目录。

#### 3.3.1 上传

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-DOC-01 | P0 | 支持上传 **PDF / Markdown / txt** 三种格式 |
| FR-DOC-02 | P0 | 单文件大小上限 **50 MB** |
| FR-DOC-03 | P0 | 一次上传一个文件 |
| FR-DOC-04 | P0 | 上传时需指定**目录路径**，提供已有路径的输入提示；不做「新建文件夹」的独立交互 |
| FR-DOC-05 | P0 | 上传时可填写**显示标题**与**一句话描述**；描述会作为检索上下文的一部分 |
| FR-DOC-06 | P0 | 上传时可选择**标签**，与文章共用同一套标签体系 |
| FR-DOC-07 | P0 | 同目录下存在同名文件时，提示并让博主选择**覆盖**（替换内容并重新索引）或**另存**（自动加后缀） |
| FR-DOC-08 | P0 | 上传后自动解析为纯文本，并进入索引流程 |
| FR-DOC-09 | P0 | 上传后**保留原始文件**，供前台原文视图展示与下载使用 |

#### 3.3.2 目录组织

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-DOC-10 | P0 | 知识库以**目录树**形式组织与展示，支持多级嵌套 |
| FR-DOC-11 | P0 | 目录结构由文档的路径属性推导得出，**不存在空目录** |
| FR-DOC-12 | P0 | 目录树支持展开 / 折叠，并记住展开状态 |
| FR-DOC-13 | P0 | 文档的**目录路径将作为检索上下文的一部分**拼入其每个块（详见 §4.2） |

> **「目录」是逻辑目录，不是服务器上的物理文件夹**
>
> 目录只是文档的一个属性 —— 一个路径字符串（如 `八斗学院/week10-RAG`），用于在界面上组织和展示「哪个目录下有哪些文件」。
>
> 服务器上文件的实际存放方式与此**完全无关**：物理存储可以是扁平的，所有文件放在同一个位置，各自使用系统生成的唯一名称。
>
> 因此「移动目录」只是修改一个字符串，**不涉及任何文件的实际移动**；「删除目录」这个操作也不存在 —— 目录下最后一份文档被删除或移走后，该目录自然消失。

#### 3.3.3 文档属性

| 属性 | 可否修改 | 说明 |
|---|---|---|
| 原始文件名 | 否 | 上传时的文件名，仅作记录，并在下载时用作下载文件名 |
| 存储名称 | 否 | 文件在服务器上实际使用的名称，由系统生成且唯一 |
| 显示标题 | **是** | 界面上展示的名字，与文件名无关 |
| 目录路径 | **是** | 逻辑路径，如 `八斗学院/week10-RAG` |
| 描述 | **是** | 一句话说明这份文档是什么，会并入检索上下文 |
| 标签 | **是** | 多选，与文章共用同一套标签 |
| 原始格式 | 否 | PDF / Markdown / txt |
| 文件大小 | 否 | 自动 |
| 上传时间 | 否 | 自动 |
| 解析后文本 | 否 | 供 RAG 索引与「文本视图」使用 |
| 浏览次数 | 否 | 自动 |
| 索引状态 | 否 | 见 §3.7 |

> **文件名的三个概念不要混淆**
>
> - **原始文件名** —— 你上传的那个文件叫什么。只作为记录保存在数据中，下载时用它当下载文件名，让你拿到的还是熟悉的名字。
> - **存储名称** —— 文件在服务器上实际叫什么。**由系统生成唯一名称，不使用原始文件名**。原因有三：中文、空格与特殊字符会带来路径处理问题；不同目录下的同名文件会互相覆盖；直接采用用户提供的文件名存在路径穿越的安全风险。上传后不可更改。
> - **显示标题** —— 界面上展示的名字，随时可改。**「重命名」改的就是这一项，不会动服务器上的任何文件。**

#### 3.3.4 管理操作

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-DOC-14 | P0 | 文档可**重命名**（修改显示标题） |
| FR-DOC-15 | P0 | 文档可**移动目录**（修改路径），移动后需更新其块的检索上下文 |
| FR-DOC-16 | P0 | 文档可**删除**，删除时**必须同步移除其在索引中的所有块**，并删除已存储的原始文件 |
| FR-DOC-17 | P0 | 文档内容在系统内**不可编辑** |
| FR-DOC-18 | P0 | 文档管理列表支持按目录、格式、索引状态筛选 |

### 3.4 前台内容浏览

#### 3.4.1 全局

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-VIEW-01 | P0 | 全站采用**纯暗色主题**，不提供亮色模式与主题切换 |
| FR-VIEW-02 | P0 | 全站**响应式**，适配手机、平板、桌面 |
| FR-VIEW-03 | P0 | 移动端上，知识库目录树可退化为可折叠列表；**写作页面不保证移动端体验** |
| FR-VIEW-04 | P0 | 全局导航包含：首页、文章、知识库、AI 问答、关于 |
| FR-VIEW-05 | P1 | 全局搜索入口在导航中随时可达 |

#### 3.4.2 首页

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-HOME-01 | P0 | 首页自上而下依次包含：主视觉区 → 精选文章 → **AI 问答入口面板** → 最新文章 → 热门标签 → **知识库入口** |
| FR-HOME-02 | P0 | 「精选文章」展示手动标记为精选的文章（建议 3 篇） |
| FR-HOME-03 | P0 | 「热门标签」按**关联内容数量**排序，不做浏览量统计 |
| FR-HOME-04 | P0 | 首页的 AI 问答面板为**可折叠**的完整交互组件，并提供跳转到独立问答页的入口 |
| FR-HOME-05 | P0 | 「知识库入口」展示文档总数与部分目录，引导访客了解站内有哪些资料 |

#### 3.4.3 文章列表与详情

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-VIEW-06 | P0 | 文章归档页展示全部已发布文章，含分类、标题、摘要、日期、阅读时长、标签 |
| FR-VIEW-07 | P0 | 支持按**分类**与**标签**筛选；标签页可通过独立链接访问 |
| FR-VIEW-08 | P0 | 文章详情页展示：分类、标题、摘要、发布/更新时间、阅读时长、标签、正文 |
| FR-VIEW-09 | P0 | 文章详情页提供**目录（TOC）**，标注当前阅读位置，点击可跳转 |
| FR-VIEW-10 | P0 | 正文中的**代码块支持语法高亮与一键复制** |
| FR-VIEW-11 | P0 | 每个标题**自带锚点**，供 AI 回答的引用精确跳转 |
| FR-VIEW-12 | P0 | 文章详情页底部提供**上一篇 / 下一篇**导航 |
| FR-VIEW-13 | P0 | 文章详情页底部提供**相关文章推荐**（基于已有的向量索引计算内容相似度） |
| FR-VIEW-14 | P0 | 文章详情页提供**「只问这篇文章」**的 AI 问答入口（见 FR-ASK-14） |

#### 3.4.4 知识库浏览

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-VIEW-15 | P0 | 知识库页面采用**左侧目录树 + 右侧内容区**布局 |
| FR-VIEW-16 | P0 | 文档详情展示元信息：显示标题、目录路径、描述、标签、原始格式、文件大小、上传时间 |
| FR-VIEW-17 | P0 | 文档正文提供**两种视图**并可切换：**原文视图**（默认）与**文本视图** |
| FR-VIEW-18 | P0 | **原文视图**：PDF 使用**原生 PDF 阅读器**呈现，支持翻页、缩放、页内查找；Markdown 以渲染后的样式呈现；txt 以纯文本呈现 |
| FR-VIEW-19 | P0 | **文本视图**：展示解析后的纯文本，按块渲染，**每个块自带锚点** |
| FR-VIEW-20 | P0 | 长文档提供内部目录或段落导航 |
| FR-VIEW-21 | P0 | 文档详情提供**下载原始文件**的入口，下载时使用上传时的原始文件名 |
| FR-VIEW-22 | P0 | AI 引用跳转到某份文档时：**PDF 跳转到原文视图的对应页码**；Markdown / txt 跳转到**文本视图的对应块锚点** |

> **关于两种视图的说明**
>
> 采用原生阅读器查看 PDF，阅读体验明显更好，但带来一个必须正视的副作用：**访客看到的是原始 PDF，而 AI 检索到的是解析后的文本，两者可能对不上** —— 解析结果在文字顺序、表格还原、页眉页脚剔除上都可能与原版面存在差异。
>
> 因此保留「文本视图」作为对照通道：它展示的正是 AI 实际读到的内容。需要逐字核对某条引用时，切到文本视图即可。
>
> 另外，PDF 阅读器无法定位到段落级别，**引用跳转在 PDF 上只能精确到页**。这要求每个 PDF 块必须记录其所在页码（见 RAG-CHUNK-07）。

#### 3.4.5 关于页

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-VIEW-23 | P0 | 提供「关于」页，介绍博主身份、技术方向、能力与联系方式 |
| FR-VIEW-24 | P0 | 「关于」页内容由博主维护，不需要独立的编辑界面（可作为一篇特殊文章管理） |

### 3.5 AI 问答

#### 3.5.1 入口与形态

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-ASK-01 | P0 | 提供**两个入口**：首页嵌入的可折叠面板 + 独立的全屏问答页 |
| FR-ASK-02 | P0 | 两个入口使用**同一套问答能力**，交互一致 |
| FR-ASK-03 | P0 | **访客无需登录即可使用** |
| FR-ASK-04 | P0 | 支持**多轮对话**，模型能理解上下文中的指代（如「那它呢」「上面说的那个方法」） |
| FR-ASK-05 | P0 | 提供**清空会话**按钮，清空后重新开始新的对话 |
| FR-ASK-06 | P0 | 对话历史**仅保存在访客本人的浏览器中**；服务端不存储任何访客的对话内容 |
| FR-ASK-07 | P0 | 提供 **3–4 个预设问题**引导冷启动，由博主手工配置，可随时修改 |
| FR-ASK-08 | P0 | 输入框展示**字数计数**，单次提问上限 1000 字 |

#### 3.5.2 检索与回答

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-ASK-09 | P0 | 采用**按需检索**：由模型自行判断当前问题是否需要查询知识库，而非每问必检 |
| FR-ASK-10 | P0 | 界面上**必须明确标注本次回答是否使用了知识库**。使用了则展示参考来源；未使用则明确提示「本次未检索知识库」 |
| FR-ASK-11 | P0 | 回答**流式输出**，逐字呈现 |
| FR-ASK-12 | P0 | 回答中的论断需带**引用角标**（如 `[1]` `[2]`），点击可跳转到原文的具体位置（文章标题锚点 / 文档页码或块锚点） |
| FR-ASK-13 | P0 | 回答下方列出**来源卡片**：标题、来源类型（文章 / 知识库文档）、命中的片段摘录、跳转链接 |
| FR-ASK-14 | P0 | 支持**限定范围提问**：从文章详情页发起时，检索范围限定为该篇文章 |
| FR-ASK-15 | P0 | **检索不到相关内容时**，明确告知「知识库中没有相关内容」，并推荐 2–3 篇最接近的内容；**严禁使用模型自身的通用知识强行作答** |
| FR-ASK-16 | P0 | 回答提供**复制**按钮 |
| FR-ASK-17 | P0 | 检索、生成过程中展示清晰的状态提示（如「正在检索知识库…」「正在生成…」） |
| FR-ASK-18 | P0 | 任何环节出错（API 超时、限流、余额不足）都给出**可理解的提示**，而非技术错误信息 |

#### 3.5.3 用量与滥用防护

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-ASK-19 | P0 | 按访客 IP **限流**（建议每小时 10 次），超限时给出友好提示与恢复时间 |
| FR-ASK-20 | P0 | 单次提问长度上限 1000 字（同 FR-ASK-08） |
| FR-ASK-21 | P0 | 设置**全站每日调用总量上限**，超出后问答功能自动降级为不可用并提示，保护 API 成本 |
| FR-ASK-22 | P0 | 上述阈值均可通过配置调整，不需要改代码 |
| FR-ASK-23 | P1 | 对**重复问题做缓存**，命中缓存时直接返回，不重复调用模型 |

### 3.6 搜索

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-SEARCH-01 | P1 | 提供全站关键词搜索，**同时覆盖文章与知识库文档** |
| FR-SEARCH-02 | P1 | 搜索结果展示：标题、**关键词高亮的匹配片段**、来源类型、日期、跳转链接 |
| FR-SEARCH-03 | P1 | 支持按来源类型（文章 / 文档）与标签筛选结果 |
| FR-SEARCH-04 | P1 | 无结果时，引导访客改用 AI 问答 |
| FR-SEARCH-05 | P1 | 搜索仅覆盖已发布内容，草稿不可被搜到 |

> **设计意图**：搜索与 AI 问答互补 —— 搜索回答「你写没写过这个」，问答回答「你是怎么说的」。

### 3.7 索引管理（仅博主）

> 这是整个系统的运维核心。内容从「写完 / 上传完」到「AI 能检索到」之间存在一条异步链路，链路会失败，必须让博主随时看得见、修得了。

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-IDX-01 | P0 | 内容发布或上传后，索引流程**异步执行**，不阻塞用户操作 |
| FR-IDX-02 | P0 | 每项内容具备明确的**索引状态**：未索引 / 排队中 / 索引中 / 已索引 / 失败 |
| FR-IDX-03 | P0 | 索引失败时**记录失败原因**，并在界面上可见 |
| FR-IDX-04 | P0 | 提供索引管理页面，列出全部内容及其索引状态，支持按状态筛选 |
| FR-IDX-05 | P0 | 失败的索引任务支持**一键重试** |
| FR-IDX-06 | P0 | 支持**全量重建索引**（用于切块策略调整、向量模型更换等场景），并展示进度 |
| FR-IDX-07 | P0 | 索引管理页展示统计信息：内容总数、块总数、索引最近更新时间、当前使用的向量模型与维度 |
| FR-IDX-08 | P0 | 内容更新时采用**增量索引**：仅对发生变化的块重新计算向量，未变化的块复用已有结果 |
| FR-IDX-09 | P0 | 内容删除或撤回时，其对应的块**必须从索引中移除** |
| FR-IDX-10 | P0 | 系统启动时校验索引与当前配置的向量模型是否一致；不一致则**明确报错**，不允许带着错误的索引静默运行 |

### 3.8 问答日志（仅博主）

> **设计意图**：这是后续优化检索质量的**唯一真实依据**。自建的评测题集是模拟分布，真实访客提出的问题才是真实分布。

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-LOG-01 | P1 | 记录每一次问答：时间、问题、是否检索、命中的块及相似度分数、最终回答、耗时、token 消耗 |
| FR-LOG-02 | P1 | 提供日志列表页面，按时间倒序，支持展开查看单次问答的完整细节 |
| FR-LOG-03 | P1 | 支持筛选：是否检索、是否报错、时间范围 |
| FR-LOG-04 | P1 | 展示汇总统计：总提问次数、检索命中率、平均耗时、累计 token 消耗 |
| FR-LOG-05 | P1 | 日志中**不记录任何可定位到具体访客身份的信息**（IP 仅用于限流计数，不落库） |

### 3.9 访问统计（仅博主）

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-STAT-01 | P1 | 记录每篇文章与每份文档的**浏览次数** |
| FR-STAT-02 | P1 | 在博主的内容列表中展示浏览次数，可按此排序 |

---

## 4. RAG 管线需求

本章描述数据从「内容」到「可被检索」再到「生成回答」的完整流程要求。

### 4.1 数据流

```
文章（已发布）                      知识库文档（PDF / Markdown / txt）
      │                                        │
      │                                    解析为纯文本
      │                                        │
      └────────────────┬───────────────────────┘
                       ↓
                    切分为块
                       ↓
              为每块生成检索上下文
                       ↓
                  计算向量表示
                       ↓
              建立向量索引 + 关键词倒排
                       ↓
        ┌──────────────┴───────────────┐
     语义检索                       关键词检索
        └──────────────┬───────────────┘
                    融合排序
                       ↓
                 组装为回答依据
                       ↓
                大语言模型生成回答
                       ↓
              回答 + 可跳转的引用来源
```

### 4.2 切块要求

| 编号 | 需求 |
|---|---|
| RAG-CHUNK-01 | 文章按**标题层级**切分（以二级、三级标题为界），保持语义完整 |
| RAG-CHUNK-02 | 单块超过长度上限时，按段落进行二次切分 |
| RAG-CHUNK-03 | **代码块与表格必须整块保留**，不得从中间切开 |
| RAG-CHUNK-04 | 每个块必须携带**检索上下文前缀**，用于补偿脱离原文后丢失的语境 |
| RAG-CHUNK-05 | 文章块的上下文前缀为：`文章标题 > 一级标题 > 二级标题` |
| RAG-CHUNK-06 | 文档块的上下文前缀为：`目录路径 > 文档标题 > 章节标题`，文档描述亦并入其中 |
| RAG-CHUNK-07 | 每个块必须记录其在原文中的**定位信息**，以支持引用跳转：文章块记录**标题锚点**；PDF 块记录**所在页码**与文本视图锚点；Markdown / txt 块记录**文本视图锚点** |
| RAG-CHUNK-08 | 每个块继承所属内容的**分类与标签**，作为检索过滤依据 |

> **说明**：上下文前缀是提升检索质量最直接、成本最低的手段。一个块被切出来后即脱离上下文，「它」「这个方法」「上面提到的」全部失去指代；标题路径与目录路径是纯字符串拼接就能获得的语境补偿。

### 4.3 检索要求

| 编号 | 需求 |
|---|---|
| RAG-RETR-01 | 同时进行**语义检索**与**关键词检索**，并对两路结果做融合排序 |
| RAG-RETR-02 | 关键词检索不可省略 —— 技术内容中大量存在专有名词、函数名、报错信息、参数名，这类词语义检索容易失准 |
| RAG-RETR-03 | 支持**范围过滤**：按内容类型、标签、目录路径、指定单篇内容进行过滤 |
| RAG-RETR-04 | 检索结果需返回**相似度分数**，供日志记录与调试使用 |
| RAG-RETR-05 | 检索只覆盖**已发布**的内容，草稿与已删除内容不参与检索 |

### 4.4 按需检索的判定

| 编号 | 需求 |
|---|---|
| RAG-DEC-01 | 由大语言模型自行判断当前问题是否需要检索知识库，而非规则硬编码 |
| RAG-DEC-02 | 判定结果必须**对用户可见**（对应 FR-ASK-10） |
| RAG-DEC-03 | 架构上应把「检索」设计为模型可调用的一项能力，以便后续扩展更多能力 |

> **演进说明**：一期只需要「检索」这一项能力。但架构预留后，二期加入「列出内容清单」「查看目录结构」「按时间筛选」等能力时无需重构。这些能力用于回答检索本身答不了的**元数据类问题**（如「你写过几篇关于 RAG 的」「最近一个月更新了什么」）。

### 4.5 生成要求

| 编号 | 需求 |
|---|---|
| RAG-GEN-01 | 回答必须**严格基于检索到的内容**，不得使用模型自身的通用知识补充 |
| RAG-GEN-02 | 回答中的每个关键论断都应标注引用来源编号 |
| RAG-GEN-03 | 检索结果不足以回答时，明确说明并推荐相近内容（对应 FR-ASK-15） |
| RAG-GEN-04 | 回答需与博客内容保持一致的语气与术语口径 |
| RAG-GEN-05 | 多轮对话中，需对历史上下文做**分层压缩**：最近若干轮保留完整过程，更早的轮次仅保留问题与结论，超出预算则淘汰最早的轮次 |

> **说明**：检索返回的原文片段通常很长，若把每轮的完整检索结果都累积到上下文中，几轮之后就会超出模型的上下文窗口。分层压缩的依据是：**刚发生的事需要细节，久远的事只需要结论**。

### 4.6 增量索引

| 编号 | 需求 |
|---|---|
| RAG-INC-01 | 为每个块计算内容指纹；重新索引时对比指纹，仅对**新增与变化**的块调用向量化服务 |
| RAG-INC-02 | 原内容中已不存在的块，需从索引中移除 |
| RAG-INC-03 | 全量重建时忽略指纹，重新计算全部块 |

> **说明**：博客内容会被反复修改。若每次修改都全量重算向量，会造成大量重复的 API 调用与成本浪费。实际情况下，修改一篇文章通常只有少数几个块发生变化。

---

## 5. 数据模型（概念层）

> 仅描述实体、关键属性与关系，不涉及具体存储方案。

| 实体 | 关键属性 | 关系 |
|---|---|---|
| **文章** | 标题、slug、摘要、正文、状态、精选标记、发布/更新时间、阅读时长、浏览次数、索引状态 | 属于一个分类；拥有多个标签；拥有多个块 |
| **知识库文档** | 原始文件名、**存储名称**、显示标题、**目录路径（逻辑路径字符串）**、描述、格式、大小、上传时间、解析后文本、浏览次数、索引状态 | 拥有多个标签；拥有多个块 |
| **分类** | 名称、slug | 拥有多篇文章 |
| **标签** | 名称、slug | 关联多篇文章与多份文档 |
| **块** | 序号、正文、检索上下文前缀、原文定位信息（标题锚点 / **页码** / 文本视图锚点）、内容指纹 | 属于一篇文章或一份文档 |
| **向量索引** | 块的向量表示、向量模型标识、维度、构建时间 | 对应全部块 |
| **上传图片** | 文件名、路径、上传时间 | 被文章正文引用 |
| **问答日志** | 时间、问题、是否检索、命中块与分数、回答、耗时、token 消耗 | — |
| **索引任务** | 目标内容、状态、失败原因、创建时间、完成时间 | 对应一篇文章或一份文档 |
| **预设问题** | 文本、排序 | — |

---

## 6. 页面清单

### 6.1 公开页面

| 页面 | 归属 | 内容 |
|---|---|---|
| 首页 | 一期 | 主视觉 → 精选文章 → AI 问答面板 → 最新文章 → 热门标签 → 知识库入口 |
| 文章归档 | 一期 | 全部已发布文章，支持分类 / 标签筛选 |
| 文章详情 | 一期 | 正文 + TOC + 标签 + 上下篇 + 相关推荐 + 「只问这篇」入口 |
| 标签页 | 一期 | 某个标签下的全部文章与文档 |
| 知识库 | 一期 | 左侧目录树 + 右侧文档内容 |
| 文档详情 | 一期 | 原文视图 / 文本视图切换 + 元信息 + 原文件下载 |
| AI 问答 | 一期 | 全屏问答界面 |
| 关于 | 一期 | 博主介绍 |
| 登录 | 一期 | 博主登录入口 |
| 搜索结果 | 二期 | 关键词匹配结果，含高亮片段 |

### 6.2 受保护页面（需登录）

| 页面 | 归属 | 内容 |
|---|---|---|
| 文章管理 | 一期 | 文章列表，含状态、索引状态 |
| 文章编辑 | 一期 | Markdown 编辑器 + 属性设置 |
| 知识库管理 | 一期 | 文档列表，支持上传、重命名、移动、删除 |
| 索引管理 | 一期 | 索引状态总览、重试、全量重建、统计 |
| 站点设置 | 一期 | 预设问题配置、限流阈值配置 |
| 问答日志 | 二期 | 日志列表与详情、汇总统计 |

---

## 7. 非功能需求

### 7.1 容量预期（Demo 规模）

| 项 | 预期 |
|---|---|
| 文章数量 | 10 – 30 篇 |
| 知识库文档 | 10 – 50 份 |
| 块总数 | 数百至数千 |
| 日均访问 | 几十 PV |
| 日均问答 | 几次至几十次 |

### 7.2 性能目标

| 编号 | 需求 |
|---|---|
| NFR-PERF-01 | 普通页面首屏渲染在 **1 秒**内完成 |
| NFR-PERF-02 | AI 问答的**第一个字在 3 秒内出现**（后续输出速度由流式呈现掩盖） |
| NFR-PERF-03 | 单篇内容的索引在 **30 秒**内完成 |
| NFR-PERF-04 | 搜索结果在 **500 毫秒**内返回（随搜索功能在二期交付） |

### 7.3 安全

| 编号 | 需求 |
|---|---|
| NFR-SEC-01 | 所有 API 密钥通过**环境变量或受保护的配置文件**提供，**严禁写入代码或提交到版本库** |
| NFR-SEC-02 | 上线后全站启用 HTTPS（本机开发阶段不适用） |
| NFR-SEC-03 | 上传文件需校验格式与大小，防止恶意文件 |
| NFR-SEC-04 | 文章正文渲染需防范脚本注入 |
| NFR-SEC-05 | 受保护页面与接口必须做服务端鉴权，不能仅靠前端隐藏入口 |

### 7.4 成本控制

| 编号 | 需求 |
|---|---|
| NFR-COST-01 | 通过增量索引避免重复的向量化调用 |
| NFR-COST-02 | 通过限流与每日总量熔断控制模型调用成本 |
| NFR-COST-03 | 索引管理页需展示累计 token 消耗，使成本可见 |

### 7.5 可维护性与备份

| 编号 | 需求 |
|---|---|
| NFR-OPS-01 | 内容数据（文章、文档元信息）、上传文件、索引产物三者可**独立备份** |
| NFR-OPS-02 | 提供一键导出全部内容的能力，导出格式需可读、可迁移 |
| NFR-OPS-03 | 系统关键操作（索引失败、API 调用失败、限流触发）需记录日志便于排查 |
| NFR-OPS-04 | 部署与更新流程应尽量简单，可由单人完成 |

---

## 8. 分阶段交付

### 8.1 一期（全部 P0 需求，在本机完成）

| 阶段 | 内容 | 交付标志 |
|---|---|---|
| **阶段 0** | 本地开发环境就绪 | 项目骨架跑起来，本机浏览器可访问最小页面；API 密钥以环境变量方式接入并验证可用 |
| **阶段 1** | 内容管理与前台浏览 | 能登录、写文章（含插图与 AI 摘要）、发布；前台可阅读，含 TOC 与上一篇 / 下一篇导航 |
| **阶段 2** | 知识库 | 能上传文档、以目录树浏览、原文视图与文本视图均可查看、可下载原文件 |
| **阶段 3** | 索引管线与 AI 问答 | 索引状态可见可重试，问答可用，引用可跳转；相关文章推荐（FR-VIEW-13）依赖向量索引，随本阶段一并交付。一期功能完成 |

### 8.2 上线部署（与开发并行，不阻塞一期）

| 事项 | 说明 |
|---|---|
| **ICP 备案** | 尽早启动，周期通常 1–3 周。备案与开发互不阻塞，但**未备案则域名无法解析到服务器**，是上线的硬前置 |
| **服务器部署** | 在一期功能于本机验收通过后进行。含：运行环境搭建、HTTPS 证书、进程守护、反向代理 |
| **数据迁移** | 本机开发期间产生的文章、上传文件、索引产物需能完整迁移到服务器（对应 NFR-OPS-01、NFR-OPS-02） |

> **本机开发的一点约束**：本机性能远好于目标服务器，容易掩盖资源问题。涉及内存占用与并发的方案，判断依据应始终是 §1.6 中的目标服务器配置，而不是本机跑得动。

### 8.3 二期（全部 P1 需求及后续设想）

| 内容 | 对应需求 |
|---|---|
| 全站关键词搜索 | FR-SEARCH-01 ~ 05、FR-VIEW-05 |
| 问答日志 | FR-LOG-01 ~ 05 |
| 访问统计 | FR-STAT-01 ~ 02 |
| 问答结果缓存 | FR-ASK-23 |
| 模型可调用能力扩展（列出内容清单、查看目录结构、按时间筛选等） | RAG-DEC-03 的演进 |
| RSS、项目作品集页面 | 尚未立项 |

---

## 9. 验收标准

### 9.1 一期验收

**在本机环境完成验收**，以下各项需同时满足：

1. 博主可完成完整创作闭环：登录 → 新建 → 写作（含插图、AI 生成摘要）→ 存草稿 → 发布 → 前台可见 → 编辑 → 撤回 → 硬删除（带二次确认）。
2. 博主可完成完整知识库闭环：上传（三种格式）→ 目录树中可见 → 前台可在**原文视图与文本视图之间切换查看** → 下载原文件 → 重命名 → 移动目录 → 删除。
3. 内容发布 / 上传后，索引状态可见；人为制造失败后可在管理页看到原因并成功重试。
4. 删除任一内容后，AI 问答不再引用其内容；文章删除后其正文引用的图片也已清理。
5. 访客无需登录即可完成多轮问答，回答流式输出、标注是否检索、引用可点击跳转到原文正确位置（文章跳标题锚点，**PDF 跳对应页码**）。
6. 提问知识库中不存在的内容时，系统明确说明「没有相关内容」，不编造答案。
7. 从文章详情页发起的提问，检索范围确实限定在该篇文章内。
8. 超过限流阈值后，问答给出友好提示而非报错。
9. 全站在手机、平板、桌面三种尺寸下均可正常浏览。
10. 全站为纯暗色，无样式错乱。

### 9.2 二期验收

1. 搜索能同时返回文章与文档结果，关键词有高亮，无结果时引导至 AI 问答。
2. 问答日志记录完整，可查看单次问答的检索命中详情与汇总统计。
3. 文章与文档的浏览次数被正确记录，并可在管理列表中排序。
4. 重复提问命中缓存，不产生新的模型调用。

---

## 10. 待确认与风险

| 项 | 说明 |
|---|---|
| **ICP 备案** | 服务器位于中国大陆，绑定域名前必须完成备案，周期通常 1–3 周。因开发在本机进行，备案不阻塞一期，但它是上线的硬前置，建议尽早启动 |
| **本机与服务器的环境差异** | 本机性能远高于目标服务器（2 核 / 2 GiB）。本机跑得顺畅不代表服务器上可行，尤其是内存占用与并发能力，需在方案设计时按目标配置约束 |
| **PDF 解析质量** | 复杂排版的 PDF（多栏、表格、图表）解析结果可能较差，而系统内不提供编辑能力。需在上传前确认文档质量，或后续考虑增加解析结果修正能力 |
| **原文视图与 AI 所见不一致** | 访客在原文视图看到的是原始 PDF，AI 检索到的是解析文本，两者可能存在差异。「文本视图」是缓解手段而非根治。若差异过大，会削弱「答案可验证」这一核心价值 |
| **PDF 引用只能精确到页** | PDF 阅读器无法定位到段落级别，引用在 PDF 上只能跳到页码；需要逐字核对时依赖文本视图 |
| **文章硬删除不可恢复** | 采用硬删除，误删后无法在系统内恢复，只能依赖备份。已通过 FR-POST-15 的二次确认缓解 |
| **文件大小上限 50 MB** | 单份大文档可能切出上千个块，一次性消耗较多向量化调用；建议在索引管理页关注 token 消耗 |
| **一期没有问答日志** | 问答日志属二期。一期上线后无法回看访客问了什么、检索命中如何，检索质量的调优会缺少真实依据 |
| **「关于」页的维护方式** | 暂定作为一篇特殊文章管理。若需要 Ting Lab 那种分区块的复杂排版，一篇 Markdown 可能撑不住，届时需单独设计 |
| **预设问题的更新** | 内容增长后需要同步更新，否则会引导访客问到答不上来的问题 |

---

*本文档为需求阶段产物，技术方案另行确定。*
