# 部署说明

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-09-01 |
| **目标服务器** | 阿里云轻量应用服务器 · 2 核 / 2 GiB / 40 GiB · Ubuntu 22.04 · 华中1（武汉） |

> 本文档是**可照着执行的操作清单**。设计依据见《技术方案》§11。
>
> 部署分两个阶段：**备案前的过渡部署**（现在就能做）与**备案后的正式上线**。
> 二者共用同一套编排，只切换 `.env` 中的两个变量。

---

## 0. 为什么分两个阶段

ICP 备案要 1–3 周，但**它只卡住三件事**：

| 卡住的 | 不卡住的 |
|---|---|
| 域名解析到服务器 | 服务器初始化（swap / Docker / 防火墙） |
| HTTPS 证书（Let's Encrypt 需验证域名归属） | 镜像构建与上传 |
| 通过 80 / 443 对外提供服务 | 用**高位端口 + IP** 访问验证 |

> **一个合规限制**：大陆地域服务器未备案时，不得通过 80 端口对外提供网站服务 ——
> 这不只针对域名，直接用 IP 访问 80 端口同样会被拦。但**高位端口不受此限**，
> 因此过渡期用 `8080` 自测完全可行。

**先做过渡部署的价值**：它会一次性暴露一整类本机测不出来的问题 ——
2 GiB 内存是否真的够、`proxy_buffering off` 是否配对、CRLF 是否混进容器、镜像能否正常运行。
这些假设积压越久，一次爆发时越难拆。

---

## 1. 服务器初始化（一次性）

SSH 登录后依次执行。

### 1.1 系统更新与时区

```bash
sudo apt-get update && sudo apt-get upgrade -y
sudo timedatectl set-timezone Asia/Shanghai
```

### 1.2 配置 4 GB Swap（**必须**）

```bash
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
sudo sysctl vm.swappiness=20
echo 'vm.swappiness=20' | sudo tee -a /etc/sysctl.conf
```

验证：`free -h` 应显示 Swap 4.0Gi。

> Swap 不是给常驻内存用的，是给**峰值兜底**：解析大 PDF、批量建索引时会有内存尖峰，
> 有 swap 才不会被 OOM Killer 直接杀进程。2 GiB 的机器上这是必要保险。

### 1.3 安装 Docker

```bash
sudo apt-get install -y ca-certificates curl gnupg
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" \
  | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
sudo systemctl enable --now docker
```

把当前用户加入 docker 组，免去每条命令加 sudo（需重新登录生效）：

```bash
sudo usermod -aG docker $USER
```

### 1.4 配置 Docker 日志轮转（**必须**）

Docker 默认日志无上限，能把 40 GB 磁盘吃光。

```bash
sudo tee /etc/docker/daemon.json > /dev/null <<'EOF'
{
  "log-driver": "json-file",
  "log-opts": { "max-size": "20m", "max-file": "3" }
}
EOF
sudo systemctl restart docker
```

### 1.5 防火墙放行

在**阿里云控制台 → 轻量应用服务器 → 防火墙**添加规则（不是服务器内的 ufw）：

| 端口 | 用途 | 阶段 |
|---|---|---|
| 22 | SSH | 一直 |
| **8080** | 过渡期访问 | 备案前 |
| 80 / 443 | 正式访问 | 备案后 |

> **5432 绝不放行。** 数据库只在 Docker 内网可达，`docker-compose.yml` 也没有映射它的端口。

---

## 2. 过渡部署（备案前，现在可做）

### 2.1 本机构建镜像

**必须在本机构建，绝不在服务器上执行 `next build`** —— Node 构建内存峰值超过 1.5 GB，
会直接拖死 2 GiB 的机器（红线 E5）。

```bash
cd E:\Study\Code\Blog
docker build -t blog-api:latest ./backend
docker build -t blog-web:latest ./frontend
```

实测体积：`blog-api` 约 601 MB，`blog-web` 约 338 MB。

### 2.2 上传镜像

**方式 A · 打包传输**（首次部署最简单，无需额外服务）

```bash
docker save blog-api:latest blog-web:latest | gzip > blog-images.tar.gz
scp blog-images.tar.gz root@8.148.70.178:/opt/blog/
```

服务器上：

```bash
gunzip -c /opt/blog/blog-images.tar.gz | docker load
```

**方式 B · 阿里云容器镜像服务（ACR）**

首次配置稍麻烦，但之后每次更新只推送变化的层，比整包传输快得多。
长期迭代建议切到这个方式。

### 2.3 上传项目文件

服务器上只需要编排与配置文件，**不需要源代码**：

```bash
scp docker-compose.yml root@8.148.70.178:/opt/blog/
scp -r nginx root@8.148.70.178:/opt/blog/
scp .env root@8.148.70.178:/opt/blog/        # ⚠️ 含密钥，确认传输通道安全
```

> `.env` 是唯一含密钥的文件。传上去后确认权限：`chmod 600 /opt/blog/.env`

### 2.4 切到过渡模式

编辑服务器上的 `/opt/blog/.env`，改这两项：

```
NGINX_CONF=nginx.http.conf
HTTP_PORT=8080
```

> 不改的话 nginx 会去加载 `/etc/letsencrypt/live/yourdomain.com/fullchain.pem`，
> 文件不存在 → **nginx 启动失败 → 整个 compose 起不来**。

### 2.5 启动

```bash
cd /opt/blog
mkdir -p data pgdata
docker compose up -d
docker compose ps          # 四个服务都应为 running / healthy
```

后端容器启动时会自动执行 `alembic upgrade head` 建表，无需手动迁移。

### 2.6 验证

```bash
# 后端健康检查
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/api/health     # 期望 200

# 前端首页
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/               # 期望 200
```

浏览器访问 `http://8.148.70.178:8080`，逐项确认：

| # | 检查项 | 说明 |
|---|---|---|
| 1 | 首页正常渲染，纯暗色无样式错乱 | |
| 2 | 登录后进管理页，**刷新页面不跳登录** | 验证 SSR Cookie 转发（A5）在真实 Nginx 后仍正常 |
| 3 | **AI 问答是逐字出现，不是转圈后整段蹦出** | ★ 验证 `proxy_buffering off` 确实生效 —— 这是本机永远测不出的一项 |
| 4 | 点击引用角标能跳转到原文正确位置 | |
| 5 | 上传一份 PDF，能解析、能在原文视图翻页 | |

> 第 3 项是过渡部署最重要的收获。缓冲问题**只在部署后出现**，
> 且症状容易被误判为"后端流式写错了"。

### 2.7 ★ 实测内存，校准预算表

```bash
docker stats --no-stream
free -h
```

《技术方案》§11.4 的内存预算表**全部是估算，从未在真机验证过**：

| 组件 | 预估 | 实测 |
|---|---|---|
| Ubuntu 基础 | 150–200 MB | ＿＿ |
| Docker daemon | 50–80 MB | ＿＿ |
| Nginx | ~20 MB | ＿＿ |
| Next.js | 150–200 MB | ＿＿ |
| FastAPI + numpy + BM25 | 200–250 MB | ＿＿ |
| PostgreSQL（调参后） | 250–350 MB | ＿＿ |
| **合计** | 820–1100 MB | ＿＿ |

把实测值填进去，并回填到《技术方案》§11.4。**这是这趟部署最有价值的产出**。

若实测明显超出预算，优先调整顺序：
PostgreSQL 的 `shared_buffers` → Next.js 的内存上限 → 考虑是否需要更大规格。

---

### 2.8 首次上线的数据准备（**只做一次**）

> 这一节容易被漏掉：服务起来了、页面能打开，但站点是**空的** ——
> 没有文章、没有资料、没有管理员能登进去的账号。

#### 2.8.1 为什么生产库是空的

`.gitignore` 把 `backend/data/`（上传的文档与配图）和 `pgdata/`（数据库）都排除在外，
**这是对的** —— 它们是运行时数据，不该进版本控制。
代价是：代码推上去了，内容一份都不会跟着过去，需要在服务器上重新准备。

#### 2.8.2 生成生产环境的密钥

**不要直接把本机的 `.env` 原样传上去。**
本机那份里的 `SESSION_SECRET` 和 `ADMIN_PASSWORD_HASH` 是开发用的，
可能出现在过命令历史、备份、截图里。生产环境重新生成：

```bash
# 会话签名密钥（换一个新的）
python -c "import secrets; print(secrets.token_urlsafe(48))"

# 管理员密码哈希（输入不回显）
python -c "import bcrypt,getpass; print(bcrypt.hashpw(getpass.getpass('密码: ').encode(), bcrypt.gensalt()).decode())"
```

分别填进服务器上 `/opt/blog/.env` 的 `SESSION_SECRET` 与 `ADMIN_PASSWORD_HASH`，
然后 `docker compose up -d api` 让后端重新读取。

> 换 `SESSION_SECRET` 会让所有已签发的会话失效 —— 首次部署时无所谓，
> 后续再换就得重新登录一次。

#### 2.8.3 灌入内容

项目内的 `seed/` 目录是一套自包含的演示语料（7 篇文章 + 3 份资料），
**用途是让系统随时能被完整演示**，不是规定你必须发这些内容。
如果一上来就要发自己的文章，把下面第 1 步换成你自己的 Markdown 目录即可。

**① 导入文章**

导入脚本在后端容器里跑，语料需要先送进容器：

```bash
# 本机：把 seed 传上去
scp -r seed root@8.148.70.178:/opt/blog/

# 服务器：拷进容器再导入
docker cp /opt/blog/seed blog-api:/tmp/seed
docker compose exec api python -m app.services.import_service --dir /tmp/seed/posts --dry-run
docker compose exec api python -m app.services.import_service --dir /tmp/seed/posts
```

先跑 `--dry-run` 看一遍标题与 slug 对不对，再去掉该参数实际导入。
导入会**同步触发向量化**，会真实消耗千问 embedding 的额度。

**② 上传知识库资料**

浏览器打开 `/admin/docs` 逐个上传 `seed/docs/` 下的文件，
目录路径按它所在的子目录填（如 `八斗学院/week10-RAG`）。

> 走页面上传而不是命令行，是因为文档需要解析、切块、写 `stored_name`，
> 这套流程只在上传接口里有。

**③ 设置分类 / 标签 / 精选**

按 `seed/taxonomy.json` 记录的归属，在 `/admin/posts` 里逐篇设置。
（3 个分类、14 个标签、3 篇精选；文档的标签在 `/admin/docs` 里设。）

**④ 建「关于」页**

在 `/admin` 新建一篇文章，**slug 必须填 `about`**，写上你自己的介绍后发布。
这个 slug 已在 `post_service.RESERVED_SLUGS` 中登记，
不会出现在文章归档、上下篇与相关推荐里，只用来渲染 `/about`。

**⑤ 配预设问题**

`/admin/settings` 里填 3–4 条，**要和实际灌进去的内容对得上** ——
预设问题问的是站里没有的东西，访客第一次点就得到「知识库中没有相关内容」，
观感比不放预设问题还差。

**⑥ 核对索引**

```bash
curl -s http://127.0.0.1:8080/api/health
```

然后登录 `/admin/index` 确认：

- 每篇文章、每份文档都是 `indexed`，没有 `failed`
- **孤儿块为 0**（有的话点「清理」）
- 块总数与内容量级相符（`seed/` 全量灌完约 72 块）

#### 2.8.4 冒烟检查

上线前在浏览器里走一遍，任何一条不通都不要进入第 3 节：

| # | 检查 | 预期 |
|---|---|---|
| 1 | 首页 | 精选 3 篇、最新列表、热门标签、知识库入口都有内容 |
| 2 | 点开一篇文章 | 正文渲染正常、目录可点、上下篇与相关推荐有内容 |
| 3 | 知识库 → 打开 PDF | 原文视图能翻页，「下载原文件」可下载 |
| 4 | AI 问答 | 点一个预设问题，能流式出字、带来源卡片，点来源能跳到原文对应位置 |
| 5 | 搜索一个站里没有的词 | 返回 0 条并引导去 AI 问答 |
| 6 | 退出登录后访问 `/admin` | 跳登录页（不是报错、也不是能看见内容） |

---

## 3. 正式上线（备案通过后）

### 3.1 域名解析

在阿里云 DNS 添加 `A` 记录，指向 `8.148.70.178`。等待生效（`ping yourdomain.com` 返回该 IP）。

### 3.2 签发 HTTPS 证书

先临时停掉 nginx 容器腾出 80 端口：

```bash
cd /opt/blog
docker compose stop nginx
sudo apt-get install -y certbot
sudo certbot certonly --standalone -d yourdomain.com
```

证书签发到 `/etc/letsencrypt/live/yourdomain.com/`，`docker-compose.yml` 已挂载该目录。

### 3.3 替换正式配置

编辑 `nginx/nginx.conf`，把两处 `yourdomain.com` 换成实际域名（`server_name` 与证书路径）。

改 `.env`：

```
NGINX_CONF=nginx.conf
HTTP_PORT=80
```

重启：

```bash
docker compose up -d nginx
```

### 3.4 防火墙调整

控制台放行 80 / 443；**关闭 8080**（过渡期入口不再需要暴露）。

### 3.5 证书自动续期

Let's Encrypt 证书 90 天有效，配置自动续期：

```bash
sudo crontab -e
```

加入（每月 1 日凌晨 3 点续期并重启 nginx）：

```
0 3 1 * * certbot renew --pre-hook "docker compose -f /opt/blog/docker-compose.yml stop nginx" --post-hook "docker compose -f /opt/blog/docker-compose.yml start nginx"
```

### 3.6 上线后

- 用域名重跑一遍 §2.6 的五项验证
- 补 README 的截图与在线地址
- 对照《需求文档》§9.1 / §9.2 做最终验收

---

## 4. 日常运维

### 4.1 更新部署

```bash
# 本机
docker build -t blog-api:latest ./backend
docker build -t blog-web:latest ./frontend
docker save blog-api:latest blog-web:latest | gzip > blog-images.tar.gz
scp blog-images.tar.gz root@8.148.70.178:/opt/blog/

# 服务器
cd /opt/blog
gunzip -c blog-images.tar.gz | docker load
docker compose up -d
docker image prune -f            # 清理被替换掉的旧镜像层
```

### 4.2 备份（对应 NFR-OPS-01）

数据由两部分组成，**必须一起备份**：

```bash
cd /opt/blog
# 1. 数据库
docker compose exec -T postgres pg_dump -U blog blog | gzip > backup/db_$(date +%F).sql.gz
# 2. 上传的文件与配图
tar czf backup/files_$(date +%F).tar.gz data/
```

> 向量数据存在 PostgreSQL 的 `chunks.embedding` 列里，包含在数据库备份中，无需单独处理。

### 4.3 排查清单

| 症状 | 可能原因 | 处理 |
|---|---|---|
| `docker compose up` 后 nginx 反复重启 | 加载了不存在的证书 | 确认 `.env` 的 `NGINX_CONF` 与当前阶段匹配 |
| **AI 问答不是逐字出现** | Nginx 缓冲未关 | 确认生效的配置里有 `proxy_buffering off` |
| 容器启动报「no such file or directory」但文件明明存在 | 脚本 CRLF 换行混进镜像 | `.gitattributes` 已强制 LF，检查是否被绕过 |
| 后端启动即退出 | 迁移失败或启动自检不通过 | `docker compose logs api` 看具体原因 |
| 刷新管理页跳登录 | SSR Cookie 未转发 | 检查 Nginx 是否正确传递了请求头 |
| 内存告警 / 进程被杀 | 超出 2 GiB | `docker stats` 定位；确认 swap 已启用 |
| 磁盘吃紧 | 镜像层与日志堆积 | `docker image prune -f`；确认日志轮转已配置 |

### 4.4 常用命令

```bash
docker compose ps                    # 服务状态
docker compose logs -f api           # 跟踪后端日志
docker stats --no-stream             # 各容器资源占用
free -h                              # 内存与 swap
df -h                                # 磁盘
docker compose restart api           # 重启单个服务
```

---

## 5. 当前进度

| 阶段 | 状态 |
|---|---|
| 应用代码 | ✅ 一期 + 二期功能全部完成，端到端实测通过（见 `修复清单-全量测试.md`） |
| Dockerfile 与编排 | ✅ 本机构建实测通过（2026-09-03，含依赖变更后的 `npm ci` 全新安装） |
| 过渡期 Nginx 配置 | ✅ `nginx/nginx.http.conf` |
| **服务器初始化（§1）** | ✅ **已完成**，实测结果见下 |
| **过渡部署（§2）** | 🚧 **卡在镜像获取，见下** |
| 首次数据准备（§2.8） | ⬜ 待部署后执行 |
| ICP 备案 | 🕐 进行中 |
| 正式上线（§3） | ⬜ 待备案完成 |

> **构建验证记录（2026-09-03）**
>
> 全量测试轮移除了三个前端依赖、`package-lock.json` 随之变更，因此重新构建验证：
> 容器内 `npm ci` 缓存失效后全新安装（32.8s）、`npm run build` 在 node:22-alpine 上
> 通过（50.8s），后端亦构建成功。
>
> 实测体积：`blog-api` 601 MB、`blog-web` 335 MB、`pgvector:pg16` 621 MB、
> `nginx:alpine` 94 MB，打包上传约 **1.65 GB**（gzip 后更小）。
>
> 今后**每次改动依赖或 Dockerfile 后都应在本机重构一次再部署** ——
> 本机构建失败比服务器上失败好收拾得多，何况服务器目前还拉不动 Docker Hub。

> **全栈冒烟已在本机跑通（2026-09-03）**
>
> 不只构建成功，四个服务实际起来跑了一遍：
>
> - 后端容器 CMD 里的 `alembic upgrade head` 在**全新空库**上从 0001 一路跑到 0003 ✅
>   （这正是服务器首次启动会发生的事）
> - nginx 路由正确：`/api/*` 打到 FastAPI、其余打到 Next.js standalone ✅
> - 未登录访问 `/admin` → 307 跳登录页，鉴权在生产构建下同样生效 ✅
> - **空库不报错**：文章 / 文档 / 标签 / 预设问题都返回空集合，`/about` 在
>   还没有 `about` 文章时显示引导文案而非崩溃 ✅ —— 这就是服务器刚部署完的状态

### ⚠️ 本机千万别直接 `docker compose up`

`docker-compose.yml`（生产）与 `docker-compose.dev.yml`（开发）
**挂的是同一个 `./pgdata` 目录**。开发库常年跑在上面，此时再起生产 compose，
就会有第二个 postgres 进程指向同一份数据目录。

PostgreSQL 通常靠 `postmaster.pid` 挡住这种情况，但 Windows 上 bind mount 的锁语义
并不可靠 —— 一旦挡不住就是数据损坏，而本地库里的内容重灌一次还要再花一遍
embedding 的 API 费用。

服务器上只有生产 compose 这一份，不存在这个问题；**这纯粹是本机的坑**。
本机若要验证生产编排，用一份临时覆盖文件把数据库换成一次性命名卷：

```yaml
# smoke.override.yml（放在项目外，别提交）
services:
  postgres:
    container_name: blog-postgres-smoke
    volumes: [smokepg:/var/lib/postgresql/data]
  api:
    container_name: blog-api-smoke
    volumes: [smokedata:/app/data]
  web:    { container_name: blog-web-smoke }
  nginx:  { container_name: blog-nginx-smoke, ports: ["8081:80"] }
volumes: { smokepg: , smokedata: }
```

```bash
NGINX_CONF=nginx.http.conf docker compose -p blogsmoke   -f docker-compose.yml -f smoke.override.yml up -d
# 验完务必带 -v 拆掉，把一次性卷也删干净
NGINX_CONF=nginx.http.conf docker compose -p blogsmoke   -f docker-compose.yml -f smoke.override.yml down -v
```

### 服务器实测结果（2026-09-02）

| 项 | 实测值 |
|---|---|
| 系统 | Ubuntu 22.04.5，内核已更新并重启 |
| **可用内存** | **1.6 GiB（1638 MB）** —— 控制台标称 2 GiB，内核与固件占去约 400 MB |
| Swap | 4.0 GiB ✅，`vm.swappiness=20` ✅ |
| 磁盘 | 40 GB，可用 30 GB ✅ |
| Docker | 29.7.2 + Compose v5.5.0 ✅ |
| apt 源 | 已换阿里云 `mirrors.aliyun.com/docker-ce` ✅ |
| 日志轮转 | `json-file`，max-size 20m / max-file 3 ✅ |

> **⚠️ 内存预算需修正**：《技术方案》§11.4 按 2048 MB 估算，实际只有 **1638 MB**。
> 当前系统占用 307 MB（未跑任何容器），可分配给容器约 **1331 MB**，
> 而 compose 中各服务 `limits` 合计 **1164 MB** —— **余量仅约 167 MB**。
>
> `limits` 是上限而非预留，实际占用通常更低，且有 4 GB swap 兜底，
> 但比原计划紧得多，必须以 §2.7 的实测为准。

### 当前阻塞：服务器无法从 Docker Hub 拉取镜像

```
docker pull nginx:alpine
→ dial tcp 157.240.11.40:443: i/o timeout
```

`157.240.11.40` 属于 Facebook 的 IP 段 —— `registry-1.docker.io` 被解析到了无关地址，
是 **DNS 污染**而非网络慢，等再久也不会通。

**本机不受影响**（已验证可正常 pull），因此有两条解法：

| 方案 | 做法 | 评价 |
|---|---|---|
| **A** 配镜像加速器 | 阿里云控制台 → 容器镜像服务 → 镜像工具 → 镜像加速器，取专属地址写入 `daemon.json` 的 `registry-mirrors` | 服务器同在阿里云，最可能可用；配好后长期受益 |
| **B** 本机打包全部镜像上传 | 见下方命令，约 600–700 MB | **确定可行**，完全绕开服务器网络 |

方案 B 的打包命令（在本机执行）：

```bash
cd E:\Study\Code\Blog
docker build -t blog-api:latest ./backend
docker build -t blog-web:latest ./frontend
docker save blog-api:latest blog-web:latest pgvector/pgvector:pg16 nginx:alpine | gzip > blog-all.tar.gz
```

建议先试 A（两分钟），不通立即转 B。

> **已知易错点**：`registry-mirrors` 必须填**自己账号的专属地址**。
> 照抄占位符会被 URL 编码后请求失败（403 Forbidden）。
