# 部署说明

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-09-01 |
| **目标服务器** | 阿里云轻量应用服务器 · 2 核 / 2 GiB / 40 GiB · Ubuntu 22.04 · 华中1（武汉） |

> 本文档是**可照着执行的操作清单**。设计依据见《技术方案》§11。
>
> 部署分两个阶段：**备案前的过渡部署**（现在就能做）与**备案后的正式上线**。
> 二者共用同一套编排，只切换 `.env` 中的两个变量。

---

## 0. 为什么分两个阶段

ICP 备案要 1–3 周，但**它只卡住三件事**：

| 卡住的 | 不卡住的 |
|---|---|
| 域名解析到服务器 | 服务器初始化（swap / Docker / 防火墙） |
| HTTPS 证书（Let's Encrypt 需验证域名归属） | 镜像构建与上传 |
| 通过 80 / 443 对外提供服务 | 用**高位端口 + IP** 访问验证 |

> **一个合规限制**：大陆地域服务器未备案时，不得通过 80 端口对外提供网站服务 ——
> 这不只针对域名，直接用 IP 访问 80 端口同样会被拦。但**高位端口不受此限**，
> 因此过渡期用 `8080` 自测完全可行。

**先做过渡部署的价值**：它会一次性暴露一整类本机测不出来的问题 ——
2 GiB 内存是否真的够、`proxy_buffering off` 是否配对、CRLF 是否混进容器、镜像能否正常运行。
这些假设积压越久，一次爆发时越难拆。

---

## 1. 服务器初始化（一次性）

SSH 登录后依次执行。

### 1.1 系统更新与时区

```bash
sudo apt-get update && sudo apt-get upgrade -y
sudo timedatectl set-timezone Asia/Shanghai
```

### 1.2 配置 4 GB Swap（**必须**）

```bash
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
sudo sysctl vm.swappiness=20
echo 'vm.swappiness=20' | sudo tee -a /etc/sysctl.conf
```

验证：`free -h` 应显示 Swap 4.0Gi。

> Swap 不是给常驻内存用的，是给**峰值兜底**：解析大 PDF、批量建索引时会有内存尖峰，
> 有 swap 才不会被 OOM Killer 直接杀进程。2 GiB 的机器上这是必要保险。

### 1.3 安装 Docker

```bash
sudo apt-get install -y ca-certificates curl gnupg
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" \
  | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
sudo systemctl enable --now docker
```

把当前用户加入 docker 组，免去每条命令加 sudo（需重新登录生效）：

```bash
sudo usermod -aG docker $USER
```

### 1.4 配置 Docker 日志轮转（**必须**）

Docker 默认日志无上限，能把 40 GB 磁盘吃光。

```bash
sudo tee /etc/docker/daemon.json > /dev/null <<'EOF'
{
  "log-driver": "json-file",
  "log-opts": { "max-size": "20m", "max-file": "3" }
}
EOF
sudo systemctl restart docker
```

### 1.5 防火墙放行

在**阿里云控制台 → 轻量应用服务器 → 防火墙**添加规则（不是服务器内的 ufw）：

| 端口 | 用途 | 阶段 |
|---|---|---|
| 22 | SSH | 一直 |
| **8080** | 过渡期访问 | 备案前 |
| 80 / 443 | 正式访问 | 备案后 |

> **5432 绝不放行。** 数据库只在 Docker 内网可达，`docker-compose.yml` 也没有映射它的端口。

---

## 2. 过渡部署（备案前，现在可做）

### 2.1 本机构建镜像

**必须在本机构建，绝不在服务器上执行 `next build`** —— Node 构建内存峰值超过 1.5 GB，
会直接拖死 2 GiB 的机器（红线 E5）。

```bash
cd E:\Study\Code\Blog
docker build -t blog-api:latest ./backend
docker build -t blog-web:latest ./frontend
```

实测体积：`blog-api` 约 601 MB，`blog-web` 约 338 MB。

### 2.2 上传镜像

**方式 A · 打包传输**（首次部署最简单，无需额外服务）

```bash
docker save blog-api:latest blog-web:latest | gzip > blog-images.tar.gz
scp blog-images.tar.gz root@8.148.70.178:/opt/blog/
```

服务器上：

```bash
gunzip -c /opt/blog/blog-images.tar.gz | docker load
```

**方式 B · 阿里云容器镜像服务（ACR）**

首次配置稍麻烦，但之后每次更新只推送变化的层，比整包传输快得多。
长期迭代建议切到这个方式。

### 2.3 上传项目文件

服务器上只需要编排与配置文件，**不需要源代码**：

```bash
scp docker-compose.yml root@8.148.70.178:/opt/blog/
scp -r nginx root@8.148.70.178:/opt/blog/
scp .env root@8.148.70.178:/opt/blog/        # ⚠️ 含密钥，确认传输通道安全
```

> `.env` 是唯一含密钥的文件。传上去后确认权限：`chmod 600 /opt/blog/.env`

### 2.4 切到过渡模式

编辑服务器上的 `/opt/blog/.env`，改这两项：

```
NGINX_CONF=nginx.http.conf
HTTP_PORT=8080
```

> 不改的话 nginx 会去加载 `/etc/letsencrypt/live/yourdomain.com/fullchain.pem`，
> 文件不存在 → **nginx 启动失败 → 整个 compose 起不来**。

### 2.5 启动

```bash
cd /opt/blog
mkdir -p data pgdata
docker compose up -d
docker compose ps          # 四个服务都应为 running / healthy
```

后端容器启动时会自动执行 `alembic upgrade head` 建表，无需手动迁移。

### 2.6 验证

```bash
# 后端健康检查
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/api/health     # 期望 200

# 前端首页
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/               # 期望 200
```

浏览器访问 `http://8.148.70.178:8080`，逐项确认：

| # | 检查项 | 说明 |
|---|---|---|
| 1 | 首页正常渲染，纯暗色无样式错乱 | |
| 2 | 登录后进管理页，**刷新页面不跳登录** | 验证 SSR Cookie 转发（A5）在真实 Nginx 后仍正常 |
| 3 | **AI 问答是逐字出现，不是转圈后整段蹦出** | ★ 验证 `proxy_buffering off` 确实生效 —— 这是本机永远测不出的一项 |
| 4 | 点击引用角标能跳转到原文正确位置 | |
| 5 | 上传一份 PDF，能解析、能在原文视图翻页 | |

> 第 3 项是过渡部署最重要的收获。缓冲问题**只在部署后出现**，
> 且症状容易被误判为"后端流式写错了"。

### 2.7 ★ 实测内存，校准预算表

```bash
docker stats --no-stream
free -h
```

《技术方案》§11.4 的内存预算表**全部是估算，从未在真机验证过**：

| 组件 | 预估 | 实测 |
|---|---|---|
| Ubuntu 基础 | 150–200 MB | ＿＿ |
| Docker daemon | 50–80 MB | ＿＿ |
| Nginx | ~20 MB | ＿＿ |
| Next.js | 150–200 MB | ＿＿ |
| FastAPI + numpy + BM25 | 200–250 MB | ＿＿ |
| PostgreSQL（调参后） | 250–350 MB | ＿＿ |
| **合计** | 820–1100 MB | ＿＿ |

把实测值填进去，并回填到《技术方案》§11.4。**这是这趟部署最有价值的产出**。

若实测明显超出预算，优先调整顺序：
PostgreSQL 的 `shared_buffers` → Next.js 的内存上限 → 考虑是否需要更大规格。

---

## 3. 正式上线（备案通过后）

### 3.1 域名解析

在阿里云 DNS 添加 `A` 记录，指向 `8.148.70.178`。等待生效（`ping yourdomain.com` 返回该 IP）。

### 3.2 签发 HTTPS 证书

先临时停掉 nginx 容器腾出 80 端口：

```bash
cd /opt/blog
docker compose stop nginx
sudo apt-get install -y certbot
sudo certbot certonly --standalone -d yourdomain.com
```

证书签发到 `/etc/letsencrypt/live/yourdomain.com/`，`docker-compose.yml` 已挂载该目录。

### 3.3 替换正式配置

编辑 `nginx/nginx.conf`，把两处 `yourdomain.com` 换成实际域名（`server_name` 与证书路径）。

改 `.env`：

```
NGINX_CONF=nginx.conf
HTTP_PORT=80
```

重启：

```bash
docker compose up -d nginx
```

### 3.4 防火墙调整

控制台放行 80 / 443；**关闭 8080**（过渡期入口不再需要暴露）。

### 3.5 证书自动续期

Let's Encrypt 证书 90 天有效，配置自动续期：

```bash
sudo crontab -e
```

加入（每月 1 日凌晨 3 点续期并重启 nginx）：

```
0 3 1 * * certbot renew --pre-hook "docker compose -f /opt/blog/docker-compose.yml stop nginx" --post-hook "docker compose -f /opt/blog/docker-compose.yml start nginx"
```

### 3.6 上线后

- 用域名重跑一遍 §2.6 的五项验证
- 补 README 的截图与在线地址
- 对照《需求文档》§9.1 / §9.2 做最终验收

---

## 4. 日常运维

### 4.1 更新部署

```bash
# 本机
docker build -t blog-api:latest ./backend
docker build -t blog-web:latest ./frontend
docker save blog-api:latest blog-web:latest | gzip > blog-images.tar.gz
scp blog-images.tar.gz root@8.148.70.178:/opt/blog/

# 服务器
cd /opt/blog
gunzip -c blog-images.tar.gz | docker load
docker compose up -d
docker image prune -f            # 清理被替换掉的旧镜像层
```

### 4.2 备份（对应 NFR-OPS-01）

数据由两部分组成，**必须一起备份**：

```bash
cd /opt/blog
# 1. 数据库
docker compose exec -T postgres pg_dump -U blog blog | gzip > backup/db_$(date +%F).sql.gz
# 2. 上传的文件与配图
tar czf backup/files_$(date +%F).tar.gz data/
```

> 向量数据存在 PostgreSQL 的 `chunks.embedding` 列里，包含在数据库备份中，无需单独处理。

### 4.3 排查清单

| 症状 | 可能原因 | 处理 |
|---|---|---|
| `docker compose up` 后 nginx 反复重启 | 加载了不存在的证书 | 确认 `.env` 的 `NGINX_CONF` 与当前阶段匹配 |
| **AI 问答不是逐字出现** | Nginx 缓冲未关 | 确认生效的配置里有 `proxy_buffering off` |
| 容器启动报「no such file or directory」但文件明明存在 | 脚本 CRLF 换行混进镜像 | `.gitattributes` 已强制 LF，检查是否被绕过 |
| 后端启动即退出 | 迁移失败或启动自检不通过 | `docker compose logs api` 看具体原因 |
| 刷新管理页跳登录 | SSR Cookie 未转发 | 检查 Nginx 是否正确传递了请求头 |
| 内存告警 / 进程被杀 | 超出 2 GiB | `docker stats` 定位；确认 swap 已启用 |
| 磁盘吃紧 | 镜像层与日志堆积 | `docker image prune -f`；确认日志轮转已配置 |

### 4.4 常用命令

```bash
docker compose ps                    # 服务状态
docker compose logs -f api           # 跟踪后端日志
docker stats --no-stream             # 各容器资源占用
free -h                              # 内存与 swap
df -h                                # 磁盘
docker compose restart api           # 重启单个服务
```

---

## 5. 当前进度

| 阶段 | 状态 |
|---|---|
| Dockerfile 与编排 | ✅ 已完成，本机构建实测通过 |
| 过渡期 Nginx 配置 | ✅ `nginx/nginx.http.conf` |
| 服务器初始化 | ⬜ §1 |
| 过渡部署与验证 | ⬜ §2 |
| ICP 备案 | 🕐 进行中 |
| 正式上线 | ⬜ §3，待备案完成 |
