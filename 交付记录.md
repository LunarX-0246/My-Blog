# 交付记录

| | |
|---|---|
| **项目** | 个人博客 + RAG 知识库问答系统 · 一期 |
| **日期** | 2026-08-31 |
| **实施者** | LiRuxiang |

> 本文档按《开发实施说明》§5 / §6.2 要求，记录每个阶段的完成情况、自验执行记录、
> 主动说明的偏离、遗留问题与新增依赖。评审时请对照本文档与各阶段分支。

---

## 阶段 0 · 项目骨架与登录链路

**分支**：`stage-0-skeleton`　**任务**：T0-3 ~ T0-6（T0-1 / T0-2 已于之前完成，未重做）

### 完成的任务号

| 任务 | 内容 | 状态 |
|---|---|---|
| T0-3 | 后端骨架：requirements、config、健康检查 | ✅ |
| T0-4 | Alembic 初始迁移（技术方案 §4.2 全部表） | ✅ |
| T0-5 | 前端骨架：create-next-app + 暗色色板 + /api 代理 | ✅ |
| T0-6 | 登录链路 + 管理页统一鉴权 | ✅ |

### 自验执行记录（§5 阶段 0 行）

| 项 | 方法 | 结果 |
|---|---|---|
| 后端健康检查 | `GET /api/health` | 200 `{"status":"ok"}` |
| 建表 | `alembic upgrade head` | 成功；12 表（含 alembic_version）、3 枚举、vector 0.8.6、HNSW 索引均在库 |
| 前端页面 | `npm run dev` → 首页 | 200；`<html lang="zh-CN">`；编译后 CSS 含暗色 token `#111315`/`#f1f0eb` |
| /api 代理 | 经 3000 端口访问 `/api/health` | 200，证明 rewrite 生效 |
| 登录链路 | 脚本自验（不读/不打密码，用后端自身签名生成会话） | 6 项全 PASS，见下 |

T0-6 登录链路 6 项自验结果（关键项）：

1. `GET /api/auth/me` 无 cookie → `authenticated:false` ✅
2. `POST /api/auth/login` 错误密码 → 401 + 中文 error ✅
3. 有效会话 cookie → `me` 返回已登录 ✅
4. 未登录访问 `/admin` → 重定向 `/login`（307）✅
5. **已登录（带 cookie）经 SSR 访问 `/admin` → 200 且含「管理后台」（验证 A5）** ✅
6. 登录页可访问 200 ✅

### 失败过又修好的（§6.2 要求如实记录）

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | `alembic upgrade` 报 `UnicodeDecodeError: gbk` | configparser 在 Windows 按 GBK 读 `.ini`，中文注释（含破折号 U+2014）无法解码 | alembic.ini 改为纯 ASCII，中文注释移到 .py |
| 2 | `conda run -c` 多行脚本报 `newlines not implemented` | conda run 不支持多行 `-c` | 改用 `docker exec psql` 或写脚本文件执行 |
| 3 | 带 cookie 访问 `/admin` 仍 307 | SSR `serverFetch` 路径少了 `/api` 前缀，FastAPI 返回 404 → 布局 catch 后重定向 | 路径改为完整 `/api/...`，并在 server-api.ts 注释明确约定 |
| 4 | `conda run` 重打 stdout 时 GBK 编码失败 | 脚本中文输出经 conda 再打印到 GBK 控制台 | 绕过 conda，直接用 env 的 python + `PYTHONIOENCODING=utf-8` |

### 主动说明的偏离（§6.2 第 4 点）

| # | 偏离 | 原因 |
|---|---|---|
| 1 | Tailwind 用 **v4**（CSS-first，`@theme` 定义色板），没有 `tailwind.config.ts` | `create-next-app@15` 装的是 Tailwind v4；红线 E4（纯暗色、无 dark: 变体）不变 |
| 2 | `lib/` 拆成 `api.ts`（客户端）+ `server-api.ts`（SSR）两个文件，而非文档 §3 的单个 `lib/api.ts` | `next/headers` 的 `cookies()` 只能在 Server Component 用，与客户端代码混在同一文件会导致客户端 import 报错 |
| 3 | 前端字体用系统字体栈，不用 `next/font/google`（Geist） | 目标服务器在境内，Google Fonts 网络不可达；且 CJK 中文渲染依赖系统中文字体 |

### 遗留问题（知道但未处理）

| # | 问题 | 说明 |
|---|---|---|
| 1 | 完整「真实密码登录」流程未自动化验证 | 管理员密码明文只有博主本人知道，实施者不应读取。已验证会话签名机制与 SSR Cookie 转发（A5），**建议博主手动登录一次确认** |
| 2 | 登录失败锁定用进程内内存 | 单 worker 部署（技术方案 §9）下正确；多 worker 会各自计数。部署仍按单 worker，暂不处理 |
| 3 | 生产 `Secure` cookie | 已通过 `X-Forwarded-Proto` 自动判断；上线需确认 Nginx 正确转发该头 |

### 新增依赖清单及理由（阶段 0）

**后端（`backend/requirements.txt`，全部为技术方案 §2.2 选型，pip 安装）**

| 依赖 | 理由 |
|---|---|
| fastapi / uvicorn[standard] | Web 框架 + 原生流式支持 |
| sqlalchemy / alembic / psycopg[binary] / pgvector | ORM + 迁移 + PG 驱动 + 向量类型 |
| pydantic / pydantic-settings | 请求模型 + 环境变量配置 |
| bcrypt / itsdangerous | 密码哈希 + 会话签名 Cookie |
| python-multipart | 文件上传 |
| openai / numpy / jieba / PyMuPDF / markdown-it-py | 模型调用、向量运算、BM25 分词、PDF 按页解析、Markdown 标题解析（后续阶段用） |

**前端（`frontend/package.json`，create-next-app 默认 + 后续阶段追加）**

| 依赖 | 理由 |
|---|---|
| next 15.5.24 / react 19.1.0 / react-dom | 框架（技术方案 §2.1） |
| typescript / @types/* | 类型安全 |
| tailwindcss v4 / @tailwindcss/postcss | 暗色主题 |
| eslint / eslint-config-next | 代码规范 |

---

## 阶段 1 · 文章（T1-1 ~ T1-9）

**分支**：`stage-1-posts`　**任务**：T1-1 ~ T1-9

### 完成的任务号

| 任务 | 内容 | 状态 |
|---|---|---|
| T1-1 | 文章 CRUD + slug 唯一性 + 草稿/发布 | ✅ |
| T1-2 | 分类与标签（编辑界面内直接新建） | ✅ |
| T1-3 | Markdown 编辑器（工具栏/预览/高亮） | ✅ |
| T1-4 | 图片上传（粘贴/拖拽 + 光标插入） | ✅ |
| T1-5 | 自动保存 + 离开确认 | ✅ |
| T1-6 | AI 生成摘要 | ✅ |
| T1-7 | 硬删除 + 二次确认 + 级联清理 | ✅ |
| T1-8 | 前台阅读（归档/详情/TOC/锚点/代码复制/上下篇） | ✅ |
| T1-9 | 首页、标签页、关于页 | ✅ |

### 自验执行记录（§5 阶段 1 行）

各环节均已通过脚本自验（不打印任何密钥）：

| 环节 | 结果 |
|---|---|
| 新建 → 存草稿 | POST 创建返回 201、draft、pinyin slug；公开列表 total=0（草稿不可见，D4） |
| 写作插图 | 上传 PNG 返回 url；正文引用后保存，图片关联到文章（post_id 已设） |
| AI 摘要 | DeepSeek 真实调用，返回一句话摘要（「混合检索结合语义与关键词检索…」） |
| 发布 | 200 且 published_at 已设；公开列表 total=1 |
| 前台阅读 | 详情页 200，TOC 锚点前后端一致（`为什么需要-bm25`）、Shiki 代码高亮（`data-language`）、上下篇 |
| 编辑 | PUT 更新已发布文章 200（updated_at 变更 + idx_status=pending） |
| 撤回 | 200 且从公开列表移除、块删除 |
| 硬删除 | 200；图片磁盘文件、图片记录、块记录均清除（D1） |

### 失败过又修好的

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | 文章 CRUD 首次请求 500 | models.py 多对多 `secondary` 用字符串 `"post_tags"`，但没定义 Table 对象 | 补 `post_tags`/`document_tags` 关联表对象，并修正 `back_populates` |
| 2 | 测试脚本「图片已关联」FAIL | 用公开 `GET /api/posts/{id}` 查草稿（该端点按 slug，草稿 404） | 改用管理端 `GET /api/admin/posts/{id}`（测试脚本错误，非代码错误） |
| 3 | `tsc` 报 `Cannot find module src/app/page.js` | 删除根首页后 `.next` 残留生成类型 | 删除 `.next/types/app/page.ts` |

### 主动说明的偏离

| # | 偏离 | 原因 |
|---|---|---|
| 1 | **新增依赖 pypinyin**（后端） | 中文标题生成 URL slug 需要转拼音；纯 Python 无重依赖 |
| 2 | **rehype-slug → 自写 rehype 插件** | rehype-slug v6 的 `Options` 只有 `prefix`，固定用 github-slugger，无法保证标题锚点与后端一致（FR-VIEW-11 / RAG-CHUNK-07 引用落点）。自写 `rehypeHeadingIds` 与后端 `slugify_heading` 逐字一致。rehype-slug 已卸载 |
| 3 | **react-markdown 用 `MarkdownAsync`** | rehype-pretty-code 是异步 transformer，默认 `Markdown`（同步 `runSync`）不 await；`MarkdownAsync` 服务端 await 才能高亮 |
| 4 | 首页 AI 问答面板、知识库入口为**占位** | 分别依赖阶段 3c（问答）与阶段 2（文档），届时替换 |

### 遗留问题（知道但未处理）

| # | 问题 | 说明 |
|---|---|---|
| 1 | 首页「精选文章」「热门标签」区块在无数据时隐藏 | 属正常空态，博主发布带标签/精选的文章后自然出现 |
| 2 | 编辑器图片「光标处插入」依赖 textarea.selectionStart | CodeMirror 会同步该值，常规场景可用；极端场景可能偏位（未深究） |
| 3 | 图片上传后未引用（post_id 为 NULL）会成为孤儿文件 | 演示规模可接受，未做 GC；可在后续版本加清理任务 |

### 新增依赖清单及理由

| 依赖 | 理由 |
|---|---|
| pypinyin（后端） | 中文标题 slug |
| @uiw/react-md-editor（前端） | Markdown 编辑器（工具栏/预览/Prism 高亮） |
| react-markdown + remark-gfm（前端） | 前台 Markdown 渲染 + 表格等 GFM |
| rehype-pretty-code + shiki（前端） | 服务端代码高亮（技术方案 §2.1） |

## 阶段 2 · 知识库（T2-1 ~ T2-7）

**分支**：`stage-2-docs`　**任务**：T2-1 ~ T2-7

### 完成的任务号

| 任务 | 内容 | 状态 |
|---|---|---|
| T2-1 | 上传接口（格式/50MB/同名/唯一存储名） | ✅ |
| T2-2 | 解析（PDF 按页 / Markdown / txt） | ✅ |
| T2-3 | 目录树（dir_path 现推、展开折叠） | ✅ |
| T2-4 | 文档管理（重命名/移动/删除） | ✅ |
| T2-5 | 前台目录树 + 内容区布局 | ✅ |
| T2-6 | 双视图（PDF 阅读器 / 文本视图） | ✅ |
| T2-7 | 原文件下载 | ✅ |

### 自验执行记录（§5 阶段 2 行）

| 环节 | 结果 |
|---|---|
| 上传三种格式 | PDF（page_count=2、parsed_text 含两页）/ Markdown / txt 均 200 |
| 同名处理 | 同名冲突 409；另存自动加后缀 |
| 目录树 | `/api/docs/tree` 含「八斗学院/week10」嵌套结构；前台 `/docs` 200 |
| 双视图 | `/docs/14`（md）、`/docs/13`（pdf）均 200，含原文/文本视图切换 |
| 下载原文件 | `/api/docs/{id}/raw` 内容一致，用原始文件名 |
| 重命名 / 移动 | PATCH 改标题 / 改目录（移动后置 pending 重索引） |
| 删除 | DELETE 200，块与原始文件级联清理 |

### 失败过又修好的

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | `npm install pdfjs-dist` 卡住 | 显式装的最新 pdfjs-dist(v4) 与 @react-pdf-viewer 的 peerDep(^3.0.279) 冲突，npm 解析卡死 | 去掉显式 pdfjs-dist，交给 npm 自动装 peer 兼容版本 |
| 2 | `/docs` 返回 404 | 旧 Next.js dev server 路由清单陈旧 | 重启 dev server |
| 3 | pdfjs-dist 报 `Can't resolve 'canvas'` | pdfjs-dist Node 构建 `require("canvas")`，webpack 解析失败 | next.config 加 `resolve.fallback.canvas=false` |
| 4 | dev server 端口占用（多次重启残留） | `npm run dev` 子进程未随任务终止 | 用 `taskkill /T` 清进程树后重启 |

### 主动说明的偏离

| # | 偏离 | 原因 |
|---|---|---|
| 1 | 文本视图暂为「整体 parsed_text」展示，未「按块带锚点」 | 文本视图的「块」即 RAG chunks，需阶段 3a 建索引后才存在。3a 完成后再把文本视图切换为按 chunk 渲染并补 `chunk-{seq}` 锚点 |
| 2 | PDF 阅读器 worker 复制到 `public/pdf.worker.min.js` 提交 | 避免运行时依赖 CDN（境内 unpkg 不稳定）；属第三方运行时资产 |
| 3 | 文档上传的标签在前端 get-or-create（按名查/建） | 后端上传接口按 tag_ids 收参，前端负责名字→id 转换 |

### 遗留问题（知道但未处理）

| # | 问题 | 说明 |
|---|---|---|
| 1 | 文档删除后，未被引用的图片类孤儿文件（本文档不涉及，文档原始文件已清） | 上传失败/未完成时可能残留 uploads 文件，未做 GC |
| 2 | 文本视图的「按块锚点」依赖 3a | 见偏离 #1 |
| 3 | 目录树展开状态仅存组件内（刷新重置） | FR-DOC-12「记住展开状态」未做 localStorage 持久化，演示规模可接受 |

### 新增依赖清单及理由

| 依赖 | 理由 |
|---|---|
| @react-pdf-viewer/core + default-layout（前端） | 原生 PDF 阅读器（翻页/缩放/查找），技术方案 §2.1 |
| pdfjs-dist（前端，peer 自动安装） | PDF 渲染引擎，@react-pdf-viewer 依赖 |

## 阶段 3a · 索引管线（T3a-1 ~ T3a-7）

**分支**：`stage-3a-index`　**任务**：T3a-1 ~ T3a-7

### 完成的任务号

| 任务 | 内容 | 状态 |
|---|---|---|
| T3a-1 | rag/chunker.py（按标题切块/前缀/指纹/页码/锚点） | ✅ |
| T3a-2 | rag/embedder.py（千问 embedding/批量/重试/L2 归一化） | ✅ |
| T3a-3 | rag/store/（抽象接口 + pgvector + numpy 双后端） | ✅ |
| T3a-4 | services/index_service.py（asyncio 队列/任务表/崩溃恢复） | ✅ |
| T3a-5 | 增量索引（指纹比对，只算变化块） | ✅ |
| T3a-6 | 索引管理页（状态/重试/重建/统计） | ✅ |
| T3a-7 | 启动自检（向量模型与索引一致性） | ✅ |

### 自验执行记录（§5 阶段 3a 行，全部实测）

| 环节 | 结果 |
|---|---|
| 发布一篇文章 → 索引状态流转到 indexed | ✅ 实测 idx_status 从 queued→running→indexed |
| **改错别字重新发布 → chunk_new 远小于 chunk_total** | ✅ 实测 chunk_new=1、chunk_total=3（R6 增量生效） |
| 人为制造失败（环境变量覆盖 DASHSCOPE_API_KEY 为无效值） | ✅ idx_status=failed，idx_error=「Error code: 401 - Invalid API-key」 |
| 修复后重试成功 | ✅ retry 后 idx_status=indexed |
| 全量重建 | ✅ /api/admin/index/rebuild 入队全部 6 个源 |

### 主动说明的偏离

| # | 偏离 | 原因 |
|---|---|---|
| 1 | 重试端点改为按内容 `POST /api/admin/index/retry/{src_type}/{src_id}`（文档 §5.6 写的是 `/{task_id}`） | 索引管理页展示的是「内容」，按内容重试更自然；按 task_id 需前端额外查任务映射 |
| 2 | 全量重建「进度」用状态列表代替 SSE 进度推送 | FR-IDX-06「展示进度」：单机几十个源、单 worker 串行，重建很快，实时 SSE 进度收益低；状态列表已能反映进度 |
| 3 | numpy store 的 SearchFilter 只支持 src_types / src_id | numpy 后端仅供对比实验（§12），不测 tag/dir 过滤 |

### 遗留问题（知道但未处理）

| # | 问题 | 说明 |
|---|---|---|
| 1 | 文档文本视图仍未「按块带锚点」 | chunks 现已建立（阶段 3a），但前端文本视图仍是整体 parsed_text 展示；计划在 3d 收尾时切换为按 chunk 渲染 |
| 2 | 索引失败的重试需手动触发 | 未做自动重试；符合 FR-IDX-05（一键重试） |
| 3 | 全量重建未忽略「正在运行」的并发冲突 | 单 worker 串行，重建入队后逐个处理，无并发冲突 |

### 新增依赖清单及理由

无新增依赖（PyMuPDF / numpy / jieba / markdown-it-py / openai 均已在 requirements.txt，本阶段正式使用）。

## 阶段 3b · 检索（T3b-1 ~ T3b-4）

**分支**：`stage-3b-retrieval`　**任务**：T3b-1 ~ T3b-4

### 完成的任务号

| 任务 | 内容 | 状态 |
|---|---|---|
| T3b-1 | rag/bm25.py（倒排索引 + 分词降级） | ✅ |
| T3b-2 | rag/retriever.py（向量 + BM25 + RRF 融合） | ✅ |
| T3b-3 | SearchFilter（类型/标签/目录/单篇过滤） | ✅ |
| T3b-4 | 命令行自测入口 | ✅ |

### 自验执行记录（§5 阶段 3b 行）

| 环节 | 结果 |
|---|---|
| 语义化问法「如何做混合检索」 | ✅ 向量路命中「混合检索」相关块 |
| 精确专有名词「rrf」 | ✅ BM25 路命中「def rrf(...)」代码块 |
| `--type document` / `--type post` | ✅ 只返回对应类型的块（SearchFilter 生效） |

### 主动说明的偏离

无。

### 遗留问题（知道但未处理）

| # | 问题 | 说明 |
|---|---|---|
| 1 | BM25 索引每次检索重建（未缓存） | 演示规模（数百块）重建耗时 <1s；量大时可在 3c 的 agent 层缓存 |

### 新增依赖清单及理由

无（jieba 已在 requirements.txt，本阶段正式使用）。

## 阶段 3c · 问答（T3c-1 ~ T3c-9）

**分支**：`stage-3c-ask`　**任务**：T3c-1 ~ T3c-9

### 完成的任务号

| 任务 | 内容 | 状态 |
|---|---|---|
| T3c-1 | rag/llm.py（流式 + 主备降级） | ✅ |
| T3c-2 | rag/agent.py（search_kb 工具、按需检索 R5） | ✅ |
| T3c-3 | rag/memory.py（分层上下文压缩 R9） | ✅ |
| T3c-4 | rag/generator.py（R1 Prompt + 引用编号） | ✅ |
| T3c-5 | api/ask.py（SSE，sources 先于 delta A4） | ✅ |
| T3c-6 | 前端 AskPanel（流式/状态/引用/来源/复制/清空） | ✅ |
| T3c-7 | 「只问这篇文章」（FR-ASK-14） | ✅ |
| T3c-8 | 限流 / 长度 / 每日熔断 | ✅ |
| T3c-9 | 预设问题 + 站点设置页 | ✅ |

### 自验执行记录（§5 阶段 3c 行）

| 环节 | 结果 |
|---|---|
| 检索类问题「什么是混合检索」 | ✅ used_retrieval=true、5 条来源、sources 先于 delta（A4）、回答带 [n] 引用 |
| 闲聊「你好」 | ✅ used_retrieval=false，界面可标注「本次未检索知识库」 |
| **问知识库没有的内容（区块链）** | ✅ 明确「知识库中没有相关内容」并推荐接近项，**不编造** |
| 多轮对话（指代消解） | ✅ Q2「那它和纯向量检索的区别」正确理解「它」=混合检索 |
| 超限流 | ✅ per_hour=1 下返回 429 友好提示 |
| 预设问题 | ✅ 设置/读取正常 |

### 主动说明的偏离

| # | 偏离 | 原因 |
|---|---|---|
| 1 | `done` 事件只含 `latency_ms`，不含 `tokens` | 流式输出下 token 计数需额外处理（openai 流式分片的 usage 字段不总是可靠），一期省略 |
| 2 | 未检索的「直接回答」以单个 delta 发送（非逐字流式） | 该路径回答短，逐字流式需二次生成（翻倍成本）；检索类回答（主场景）逐字流式 |
| 3 | 引用角标的 URL 已正确构建（文章 `#anchor`、PDF `?page=N`），但浏览器端跳转未逐条自动化实测 | URL 规则经代码评审，跳转依赖浏览器渲染 |

### 遗留问题（知道但未处理）

| # | 问题 | 说明 |
|---|---|---|
| 1 | 问答日志（FR-LOG-01~05）未实现 | 属二期，一期交付后无真实问答依据 |
| 2 | 重复问题缓存（FR-ASK-23）未实现 | 属二期 |
| 3 | `done` 事件无 token 消耗统计 | 见偏离 #1 |

### 新增依赖清单及理由

无新增依赖。

## 阶段 3d · 收尾（T3d-1 ~ T3d-3）

（待完成）
