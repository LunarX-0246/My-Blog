# 一期评审 · 修复清单

| | |
|---|---|
| **版本** | v1.0 |
| **日期** | 2026-08-31 |
| **评审范围** | 一期全部代码（T0-3 ~ T3d-3，43 个任务，32 个提交） |
| **结论** | 主体质量良好，25 条红线大部分真守住；**4 项必修，4 项应修，3 项次要** |

> **给执行修复者**：本清单每条都给出了文件、行号、当前行为、为什么错、怎么改、以及**可执行的验收标准**。
> 修完请逐条填写「验收结果」，与《开发实施说明》§6.2 的交付要求一致。
>
> 修复顺序建议：**H 类全部 → M 类 → L 类**。H1/H4 关系到产品定位是否成立，优先级最高。

---

## 修复总览

| 编号 | 严重度 | 问题 | 主要文件 |
|---|---|---|---|
| H1 | 🔴 必修 | 未检索路径绕开 R1，可能输出通用知识回答 | `rag/agent.py` |
| H2 | 🔴 必修 | 未检索路径不是流式输出 | `rag/agent.py`、`api/ask.py` |
| H3 | 🔴 必修 | 全量重建索引未忽略指纹，形同虚设 | `services/index_service.py` |
| H4 | 🔴 必修 | 知识库文档的引用跳转完全失效 | `docs/[id]/page.tsx`、`PdfViewer.tsx`、`DualView.tsx` |
| M1 | 🟡 应修 | BM25 每次查询全量重建，威胁首字延迟 | `rag/retriever.py` |
| M2 | 🟡 应修 | 流式过程中出错静默截断，`error` 事件从未实现 | `api/ask.py` |
| M3 | 🟡 应修 | 完全没有 token 统计 | `rag/llm.py`、`api/ask.py`、`api/admin.py` |
| M4 | 🟡 应修 | `status` 事件时序错位 | `api/ask.py`、`rag/agent.py` |
| L1 | 🟢 次要 | 标题锚点去重规则前后端不一致 | `rag/markdown.py` |
| L2 | 🟢 次要 | `_split_long` 类型标注与实际返回不符 | `rag/chunker.py` |
| L3 | 🟢 次要 | 交付报告任务数写错（36 → 实为 43） | `交付记录.md` |

---

# 🔴 H 类 · 必修

## H1 · 未检索路径绕开 R1 约束

**文件**：`backend/app/rag/agent.py:17`（`AGENT_PROMPT`）、`agent.py:72-73`

**当前行为**

```python
# agent.py:72
if not resp.tool_calls:
    return AgentResult(used_retrieval=False, direct_answer=resp.content or "")
```

这个 `direct_answer` 是在 `AGENT_PROMPT` 下生成的，而该 prompt 写的是「闲聊、常识性问题不需要检索，**直接回答**」——**不含任何 R1 约束**。带 R1 的 `SYSTEM_PROMPT` 只在检索路径（`agent.py:87`）生效。

**为什么必须改**

违反 RAG-GEN-01「回答必须严格基于检索到的内容，不得使用模型自身的通用知识补充」。

失败场景：访客问「什么是 RAG」→ 模型判定为常识问题、不检索 → 用模型自身的通用知识作答。而博客里恰好有讲 RAG 的文章。**访客在这个博客上得到的是一个通用回答**，这正是本项目立项时明确要杜绝的（见《需求文档》§1.2 目标 3、FR-ASK-15）。

这不是违反了 R5（按需检索本就要求存在不检索路径），而是落在 **R1 与 R5 的交界处，而交界处没有任何约束**。

**怎么改**

不检索路径也必须在受约束的 prompt 下生成。新增一个 `NO_RETRIEVAL_PROMPT`，允许范围严格限定为：

- 问候、致谢、告别
- 关于助手本身的元问题（"你是谁"、"你能回答什么"）

除此之外的任何实质性问题，一律回复「知识库中没有相关内容」并引导换个问法，**不得动用模型自身的通用知识作答**。

不要把判定逻辑写死成关键词规则——仍由模型判断，但**约束它在不检索时能说什么**。

**验收标准**

| # | 操作 | 期望 |
|---|---|---|
| 1 | 问「你好」 | 正常寒暄，`used_retrieval=false` |
| 2 | 问「你能回答什么」 | 说明自己基于站内内容作答，`used_retrieval=false` |
| 3 | 问一个知识库里**没有**但模型自己知道的常识题（如「Python 的 GIL 是什么」） | **不得给出通用知识解释**；应回复知识库无相关内容 |
| 4 | 问一个知识库里**有**的技术问题 | 正常检索并带引用作答 |

> 第 3 条是本项修复的核心验收点，必须实测并记录原始回答。

---

## H2 · 未检索路径不是流式输出

**文件**：`backend/app/api/ask.py:106`、`backend/app/rag/agent.py:73`

**当前行为**

```python
# ask.py:106
yield _sse("delta", {"text": result.direct_answer or ""})
```

整段答案作为**一个** `delta` 事件发出。且 `direct_answer` 来自非流式的 `llm.chat_with_tools`，用户是先干等完整生成、再一次性看到全文。

**为什么必须改**

违反 FR-ASK-11「回答流式输出，逐字呈现」。该需求没有区分是否检索，两条路径都必须流式。

**怎么改**

`run_agent` 在未检索时不要直接返回成品文本，而是返回可供流式生成的 `final_messages`（system 用 H1 新增的 `NO_RETRIEVAL_PROMPT`），由 `ask.py` 统一走 `llm.stream_chat`。

改完后两条路径应共用同一段流式代码，`ask.py` 里不再有"整段 yield"的分支。

**验收标准**

- 浏览器中提问一个不触发检索的问题，观察答案是**逐字出现**而非整段闪现
- `ask.py` 中不再存在 `yield _sse("delta", {"text": <完整答案>})` 这种一次性写法

---

## H3 · 全量重建索引未忽略指纹

**文件**：`backend/app/services/index_service.py:197`（`rebuild_all`）、`:140`（`_index_source`）、`:153`

**当前行为**

```python
def rebuild_all() -> int:
    """全量重建索引（FR-IDX-06、RAG-INC-03）：忽略指纹，重算全部块。"""
    ...
    for src_type, src_id in sources:
        enqueue(src_type, src_id)      # ← 走的仍是带指纹比对的 _index_source
```

**docstring 声称"忽略指纹"，但代码中不存在任何忽略指纹的分支。**

**为什么必须改**

违反 RAG-INC-03、FR-IDX-06。

失败场景恰恰是这个功能存在的唯一理由——**更换 embedding 模型**：

```
换模型 → 块文本未变 → 指纹未变 → to_embed 为空 → 全部复用旧模型算出的向量
```

再叠加 FR-IDX-10 的启动自检（模型与索引不一致则拒绝启动），会形成死循环：

```
自检拒绝启动 → 需要重建索引 → 重建什么也没重算 → 自检仍然拒绝启动
```

**怎么改**

给索引流程加一条 force 路径：

1. `_index_source(db, src_type, src_id, *, force: bool = False)`，`force=True` 时跳过指纹复用，`to_embed = new_chunks`
2. `enqueue(src_type, src_id, *, force: bool = False)`，force 标记随任务持久化（`index_tasks` 加一列，或队列元组携带）
3. `rebuild_all()` 以 `force=True` 入队
4. 重建前应先清空该源的旧块，避免残留

**验收标准**

| # | 操作 | 期望 |
|---|---|---|
| 1 | 对已索引内容点「全量重建」 | 任务的 `chunk_new == chunk_total`（全部重算） |
| 2 | 普通发布/更新已索引文章（改一个错别字） | `chunk_new < chunk_total`（增量仍然生效，**不要为了修 H3 把 R6 弄坏**） |
| 3 | 修改 `.env` 的 `EMBEDDING_MODEL` 后重建 | 重建后启动自检通过 |

> 第 2 条是回归验证：H3 的修复**不得破坏 R6**。

---

## H4 · 知识库文档的引用跳转完全失效

**文件**：`frontend/src/app/(public)/docs/[id]/page.tsx:32-35`、`frontend/src/components/doc/PdfViewer.tsx`、`frontend/src/components/doc/DualView.tsx`

**当前行为**

后端已生成正确的引用 URL（`api/ask.py:56,58`）：

```python
url = f"/docs/{s.src_id}?page={s.page_no}"      # PDF
url = f"/docs/{s.src_id}#chunk-{s.seq}"          # Markdown / txt
```

**前端三处都不认**：

| 文件 | 问题 |
|---|---|
| `PdfViewer.tsx` | 只用了 `defaultLayoutPlugin`，**没有 `pageNavigationPlugin`、没有 `initialPage`**，`?page=` 被完全忽略 |
| `docs/[id]/page.tsx:32-35` | 文本视图是 `<pre>{doc.parsed_text}</pre>` 整块渲染，**没有任何 `id="chunk-N"`**，`#chunk-N` 没有落点 |
| `DualView.tsx` | 视图选择是客户端 `useState`，带 `#chunk-N` 进入页面也不会自动切到文本视图 |

**为什么必须改**

- FR-VIEW-19「文本视图：按块渲染，每个块自带锚点」**未实现**
- FR-VIEW-22「PDF 跳转到对应页码；Markdown/txt 跳转到对应块锚点」**整条失效**

后果：点击知识库来源的引用角标，只会跳到文档页顶部，**静默失败，没有任何报错**。

**这是一期 P0 未完成项，不是可延后的遗留问题。** 「答案可溯源」是本项目相对通用对话产品的唯一核心优势（《需求文档》§1.2），它在知识库这一半上目前是坏的。

**怎么改**

**① 后端**：`GET /api/docs/{id}` 增加按块返回的字段，例如

```
chunks: [{ seq, content, page_no, anchor }]
```

数据已存在于 `chunks` 表，按 `seq` 排序返回即可。保留 `parsed_text` 以兼容现有渲染。

**② 文本视图**：改为遍历 `chunks` 渲染，每块外层带 `id={"chunk-" + seq}`。

**③ PDF 视图**：引入 `@react-pdf-viewer/page-navigation` 的 `pageNavigationPlugin`，从 URL 读 `?page=N`，通过 `jumpToPage(N-1)`（0 基）跳转。

**④ 视图自动切换**：页面根据 URL 决定初始视图 —— 带 `?page=` 用原文视图，带 `#chunk-` 用文本视图。`DualView` 需接受 `initialView` prop。

**验收标准**

| # | 操作 | 期望 |
|---|---|---|
| 1 | 问一个命中 **PDF** 文档的问题，点引用角标 | 打开该文档并**停在正确页码**，不是第 1 页 |
| 2 | 问一个命中 **Markdown/txt** 文档的问题，点引用角标 | **自动切到文本视图**并滚动到对应块 |
| 3 | 查看文本视图 HTML | 每个块外层存在 `id="chunk-N"` |
| 4 | 问一个命中**文章**的问题，点引用角标 | 跳到对应标题锚点（回归验证，原本正常） |

---

# 🟡 M 类 · 应修

## M1 · BM25 每次查询全量重建

**文件**：`backend/app/rag/retriever.py:102-103`

**当前行为**

```python
chunks = load_chunks(db, flt)          # 全量拉块
bm25 = BM25([c.content for c in chunks])   # 全量 jieba 分词 + 建倒排
```

**每一次提问**都要重做一遍，且发生在 LLM 开始生成之前。

**实测数据**（使用项目自身的 `BM25` 实现）

| 块数 | 建索引耗时 | 检索本身 |
|---:|---:|---:|
| 500 | 198 ms | 0.8 ms |
| 2000 | 809 ms | 3.5 ms |
| 5000 | **1930 ms** | 7.9 ms |

**为什么应修**

《需求文档》§7.1 预期块总数为「数百至数千」。叠加**串行的两次 LLM 调用**（工具判定必须完整返回后才能开始流式生成）与一次 embedding API 调用，NFR-PERF-02「首字 3 秒内出现」在目标规模下大概率无法满足。

当前库中仅 14 个块，该问题完全测不出来。

**怎么改**（择一）

- **方案 A（推荐）**：进程内缓存 BM25 索引，索引任务完成时失效重建。命中缓存时每次查询只做一次 `search`（毫秒级）
- **方案 B**：把倒排索引持久化（如与向量产物同目录），启动时加载
- 带 `flt` 的查询可在全量索引上检索后再过滤，避免为每种过滤组合各建一份索引

**验收标准**

- 灌入 ≥1000 个块后，连续提问 5 次，记录每次「从请求发出到收到第一个 `delta`」的耗时
- 首次可以较慢（冷启动建索引），**后续 4 次的 BM25 环节不应再有百毫秒级开销**
- 内容更新后，新块能被检索到（缓存失效正确）

---

## M2 · 流式过程中出错静默截断

**文件**：`backend/app/api/ask.py:77-107`（`gen()`）

**当前行为**

`gen()` 内**没有 try/except**。技术方案 §5.5 定义的 `event: error` 从未被发出过。

限流、参数错误在进入流之前抛 `ApiError` 是正确的（429 正常返回）。但 **API 超时、余额不足、模型报错都发生在流中** —— 恰恰是最需要给用户提示的场景，目前表现为 SSE 流直接断开。

**为什么应修**

违反 FR-ASK-19「任何环节出错都给出可理解的提示，而非技术错误信息」。

**怎么改**

`gen()` 整体包 try/except，捕获后 `yield _sse("error", {"message": <面向用户的中文提示>})` 再正常结束流。异常详情写日志，**不下发给前端**。

前端 `lib/stream.ts` 与 `AskPanel` 需处理 `error` 事件并展示。

**验收标准**

| # | 操作 | 期望 |
|---|---|---|
| 1 | 临时把 `DEEPSEEK_API_KEY` 改错，提问 | 前端收到 `event: error` 并显示可理解的中文提示，不是空白或转圈卡死 |
| 2 | 检查后端日志 | 有完整异常堆栈 |
| 3 | 检查前端显示 | **不含**堆栈、模型名、URL 等技术细节 |

---

## M3 · 完全没有 token 统计

**文件**：`backend/app/rag/llm.py`、`backend/app/api/ask.py:107`、`backend/app/api/admin.py`

**当前行为**

- `llm.py` 从未读取 `response.usage`
- `done` 事件只有 `latency_ms`，技术方案 §5.5 规定的 `tokens` 字段缺失
- 索引管理页无累计 token 展示

**为什么应修**

违反 NFR-COST-03「索引管理页需展示累计 token 消耗，使成本可见」与 FR-IDX-07。在按量计费且服务器资源受限的场景下，成本可见是明确要求。

**怎么改**

1. `llm.chat` / `chat_with_tools` / `stream_chat` 读取并返回 `usage`（流式需在 `create` 时开启 `stream_options={"include_usage": True}`）
2. `ask.py` 的 `done` 事件带上 `{"tokens": {"prompt": n, "output": m}}`
3. 索引管理页展示累计消耗（一期可只统计索引环节的 embedding 用量；问答用量的持久化属二期问答日志范畴）

**验收标准**

- `done` 事件的 data 中含非零的 `tokens.prompt` 与 `tokens.output`
- 索引管理页可见累计 token 数值

---

## M4 · `status` 事件时序错位

**文件**：`backend/app/api/ask.py:78,82,99`

**当前行为**

```python
yield _sse("status", {"stage": "deciding"})
result = agent.run_agent(...)                    # 工具判定 + 检索全部在此完成
yield _sse("status", {"stage": "retrieving"})    # 事后才发
```

实际观感：整个耗时段一直显示「判断中」，检索早已结束才闪一下「正在检索」，随即出正文。

**为什么应修**

FR-ASK-18 要求「检索、生成过程中展示清晰的状态提示」。当前提示与真实进度不对应，等同于没有。

**怎么改**

让 `run_agent` 支持进度回调（或改为生成器分阶段 yield），使 `ask.py` 能在**真正开始检索之前**发出 `retrieving`，在开始生成之前发出 `generating`。

**验收标准**

- 提问一个触发检索的问题，观察状态提示按 `判断中 → 正在检索 → 正在生成` 的真实顺序推进
- 每个状态的停留时长与该环节的实际耗时相符

---

# 🟢 L 类 · 次要

## L1 · 标题锚点去重规则前后端不一致

**文件**：`backend/app/rag/markdown.py`（`slugify_heading`）、`frontend/src/lib/slug.ts`（`rehypeHeadingIds`）

前端对重复标题追加 `-1`、`-2`，后端**不去重**。同一篇文章存在两个同名标题时，指向第二个的引用会跳到第一个。

**怎么改**：后端 `build_toc` / 切块生成 anchor 时采用与前端一致的去重计数。

**验收**：造一篇含两个同名 `## 说明` 的文章，确认第二节的块 anchor 为 `说明-1`，且引用能跳到第二节。

## L2 · `_split_long` 类型标注与实际返回不符

**文件**：`backend/app/rag/chunker.py:84`

声明 `-> list[ChunkData]`，实际返回 `list[str]`；`anchor` 与 `page_no` 两个参数从未使用。运行结果正确（调用方自行包装），但类型标注是错的，违反《开发实施说明》§4 的类型标注约定。

**怎么改**：改为 `-> list[str]`，删除未使用参数。

## L3 · 交付报告任务数写错

**文件**：`交付记录.md`

报告称「36 个任务」，实际 T0-3 ~ T3d-3 共 **43 个**。43 个任务在 `交付记录.md` 中均有记录，工作量没有缺失，仅统计数字有误。

**怎么改**：更正为 43。

---

# 附：本次评审确认「做得对」的部分

修复时**不要动这些**，它们是逐条验证过的：

| 项 | 验证结论 |
|---|---|
| R2 PDF 页码 | 从解析到块级全程保留，无中途拼接 |
| R3 切块 | 真按 h2/h3 切分，代码围栏不被切开，上下文前缀正确 |
| R4 混合检索 | BM25 **真的参与** RRF 融合；公式 `1/(k+rank+1)` 正确；两路应用同一 filter |
| R6 增量索引 | 指纹比对真实生效，消失的块会被删除 |
| R7/R8 | 无 LangChain / FAISS / Chroma |
| A1 | 无 Next.js API Routes、无 Server Actions |
| A3 | `rag/` 层未 import FastAPI 或 services |
| A5 | SSR Cookie 转发实现正确 |
| A6 | 管理页鉴权统一在 `(admin)/layout.tsx` |
| D1 | 硬删除三处级联（chunks / images 行 / 磁盘文件）齐全 |
| D2 | UUID 存储名；`/images/{stored_name}` 有路径穿越防御 |
| E4 | 全站无 `dark:` 变体 |
| 后端鉴权 | 所有写接口均挂 `Depends(get_current_admin)`，非仅前端隐藏 |
| FR-IDX-10 | 启动自检已实现，模型不一致时拒绝启动 |
| T3b-4 | 命令行自测入口可用 |

---

# 附：关于自验数据量的说明

本次一期自验全部在 **14 个块**（3 篇文章 + 3 份文档）的数据集上完成。

这足以证明**代码路径连通**，但不足以证明**检索质量**——14 个块的语料下，任何查询都几乎必然命中目标块，融合排序的效果无从体现。M1 的性能问题同样在该规模下完全无法暴露。

修复完成后，建议先导入数十篇真实内容再复测 R4（混合检索质量）与 M1（首字延迟），否则这两项的验收结论仍然不具备参考价值。

---

# 修复完成后

按《开发实施说明》§6.2 提交复审，并提供：

1. 每条修复项的**验收结果**（对照本清单各项的验收标准逐条填写）
2. H3 第 2 条、H4 第 4 条两项**回归验证**结果
3. 任何未修复项及原因
